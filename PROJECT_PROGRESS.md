# سجل تقدم مشروع مزرعة الغزيز

## الحالة الحالية

- الحالة: قيد التنفيذ
- الخطوة الحالية: اختبار تنزيل PDF المباشر (html2canvas/jsPDF) على المتصفحات الرئيسية + إقفال السنة المالية على قاعدة بيانات فيها حركات فعلية عبر أكثر من سنة
- آخر تحديث: 2026-09-08

## تم تنفيذه

- قائمة اختيار الحساب في تبويب "دفتر الأستاذ" (`reports/accounting-reports.html`/`accounting-reports-page.js`) مقصورة الآن على **كل الحسابات الفرعية القابلة للترحيل والحركة** (`isPostable !== false` — نفس قيد `getPostableAccounts()`/نموذج القيد اليدوي)، بلا اشتراط وجود حركة فعلية عليها بعد (تجربة أولى قصرتها على ما عليه حركة فعلية فقط، ثم عُدِّلت بطلب صاحب المشروع لتشمل كل الأوراق القابلة للترحيل حتى بلا حركة). الرؤوس التجميعية (`isPostable === false`) وحدها مستبعدة لأنه لا يُرحَّل عليها مباشرة أصلاً. الحساب المختار افتراضيًا أول حساب مؤهَّل حسب الكود؛ لا حسابات مؤهَّلة ⇐ رسالة "لا توجد حسابات فرعية قابلة للترحيل بعد" بدل جدول فارغ.
- توسيع عمود **"رقم الأرشفة"** ليشمل **دفتر الأستاذ لأي حساب** في `reports/accounting-reports.html` (تبويب "دفتر الأستاذ"، `accounting-reports-page.js` — يعمل لكل حسابات الشجرة، لا الصندوق/البنك فقط)، بالإضافة لدفتر الصندوق والبنك (`accounting/cash-ledger.html` + `cash-ledger-page.js`) المضاف قبله: نفس الآلية الحيّة المستخدمة في `expense-list.html`/`revenue-list.html`/`purchase-list.html` — محسوب من `entry.weeklyBatchId` ← `WeeklyBatches.batchNumber` (تحميل `weekly-batch-service.js` جديدًا في كلا الصفحتين)، بلا حقل مُدخَل يدويًا. '-' لحركة قيدها لم يُدرَج بعد ضمن أرشفة أسبوعية.
- **اسم المزرعة وشعارها قابلان للتخصيص من الإعدادات وينعكسان تلقائيًا في كل مكان**: قسم "بيانات المزرعة" في `settings/settings.html` صار يضم أيضًا رفع شعار (صورة تُصغَّر إلى 320×320 كحد أقصى عبر canvas — نفس أسلوب صورة الموظف — وتُخزَّن Base64) بجانب حقل الاسم الموجود أصلاً، وزر "استخدام الشعار الافتراضي" لإعادة التعيين. كلاهما يُخزَّن في نفس مفتاح localStorage الموجود أصلاً (`alGhazeezFarmSettings.farmName`/`.logoBase64`) — لا مخزن IndexedDB جديد. **لا ملف service مشترك جديد** (كان يتطلب إضافة وسم `<script>` في ~80 ملف HTML): كل مستهلك (`sidebar.js` لشعار/اسم الشريط الجانبي وعنوان تبويب كل صفحة محمية، `login-page.js` لصفحة الدخول، `export-utils.js` لترويسة كل تصدير Excel/PDF، `animal-card-print.js` لبطاقة الحيوان المطبوعة) يقرأ نفس المفتاح مباشرة من `localStorage` بدالة قراءة صغيرة مكرَّرة محليًا (موثّقة بتعليق في كل موضع يشير لنظيراتها) بدل الاعتماد على ملف واحد. الحفظ في `settings-page.js` يستدعي `renderSidebar('settings')` فورًا فينعكس التغيير في نفس الصفحة بلا حاجة لإعادة تحميل.
- إضافة عمود **"رقم القيد"** في `revenues/revenue-list.html` (`revenue-list-page.js`) — بعد أن تبيّن أن `reports/revenue-reports.html` مجمّعة حسب البند (صف = إجمالي معاملات كتير، لا معاملة واحدة، فـ"رقم القيد" بلا معنى هناك)، اتفقنا على إضافته لصفحة السجل بدلاً منها. رابط `<a>` داخل الخلية (بـ`event.stopPropagation()`، نفس نمط زر "📎 مرفقات" الموجود أصلاً) بدل `onRowClick` على مستوى الجدول، لأن نقر الصف مخصّص أصلاً لفتح نموذج تعديل الإيراد (`revenue-form.html?id=`) — لا تعارض بين الفتحين.
- توسيع عمود **"رقم القيد"** (مع نقر الصف لفتح القيد في `accounting/journal-entry-form.html`) ليشمل التقارير التي فيها صف = عملية واحدة بقيد محاسبي مباشر: `reports/sales-report.html` (قسم "سجل المبيعات" فقط — بيع فردي `Revenues.journalEntryId`، بيع جملة `BulkSaleBatches.journalEntryId` لكل أسطر نفس عملية البيع؛ قسم "تكلفة الرأس" استُثني عمدًا لأنه رصيد تراكمي لحياة الحيوان كلها بلا قيد واحد يمثّله)، `reports/custody-reports.html` (تبويبات "جميع العهد/المفتوحة/المتأخرة/حسب الفترة/عهد المعدات/العهد النقدية" — `CustodyItems.journalEntryId`؛ قيد الإصدار فقط لا التسويات اللاحقة، وتبويبات "حسب الموظف/حسب نوع العهدة/الأرصدة المستحقة" المجمّعة استُثنيت لأنها إجماليات بلا قيد واحد لكل صف)، و`reports/asset-reports.html` (تبويبات "سجل الأصول/الإهلاك/القيمة الدفترية" بقيد الاقتناء `FixedAssets.journalEntryId`، "الإهلاك الشهري" بقيد كل حركة إهلاك، و"المباعة والمستبعدة" بقيد البيع/الاستبعاد نفسه — من `FixedAssetTransactions.journalEntryId`؛ تبويبا "حسب النوع/حسب الموقع" المجمّعان و"تقرير الجرد" بلا أثر محاسبي استُثنيا). ⚠️ **لم يُضَف عمدًا** لتقارير المصروفات/الإيرادات/المشتريات (`expense-reports.html`/`revenue-reports.html`/`purchase-reports.html`): هذه الصفحات مبنية على تجميع حسب البند (صف = بند كامل بعدد/إجمالي معاملات، لا معاملة واحدة) وليست سجلاً بمعاملة واحدة لكل صف أصلاً — "رقم القيد" لا معنى له على صف مجمَّع؛ صفحات القوائم `expenses/expense-list.html`/`revenues/revenue-list.html`/`purchases/purchase-list.html` (سجل معاملة بمعاملة) هي المكان المناسب لإضافته لو احتاجه صاحب المشروع لاحقًا.
- إضافة عمود **"رقم القيد"** في كشف حساب العميل/المورّد/الشريك (`parties/party-statement.html`): كل سطر (فاتورة آجلة/سند قبض أو صرف/مرتجع أو خصم/ضخ أو سحب فلوس/مساهمة أصل) يعرض الآن رقم القيد المحاسبي المرتبط (`journalEntryId` على السجل المصدر، الذي أُضيف تمريره عبر `getPartyLedgerEntries()` في `party-service.js`)، والنقر على الصف يفتح القيد نفسه في `accounting/journal-entry-form.html` — نفس نمط "دفتر الصندوق والبنك" (`cash-ledger.html`) الذي كان يعرضه أصلاً. لا تغيير في `computePartyBalance()`/منطق الأرصدة نفسه.
- تصدير PDF أصبح **تنزيلاً مباشرًا لملف حقيقي** بضغطة واحدة بدل فتح نافذة معاينة طباعة تتطلب من المستخدم اختيار "حفظ كـ PDF" يدويًا: `exportRowsToPdf`/`exportSectionsToPdf` (`js/components/export-utils.js`) تعيد بناء كل `.report-page` خارج الشاشة داخل نفس مستند الصفحة الحالية (بدل نافذة/تبويب معزول)، تلتقطها بمكتبة `html2canvas` (تصوير الـ HTML المُنسَّق فعليًا بما فيه العربي RTL وخط Cairo الموروث تلقائيًا من الصفحة الحالية)، ثم تُلصقها في مستند `jsPDF` يُنزَّل عبر `pdf.save()`. أُضيف `html2canvas@1.4.1` و`jspdf@2.5.2` (CDN، jsDelivr) لكل الصفحات الـ42 التي تحمّل `export-utils.js`. الدالتان صارتا `async` (كل الأخطاء مُغلَّفة داخليًا بـ`showToast` فلا حاجة لتعديل أي من نقاط الاستدعاء الأربعين الحالية). **مقايضة مقصودة**: حجم ملف أكبر (صور JPEG) بدل نص PDF متجهي، تفاديًا لمشاكل تشكيل الخط العربي في مكتبات PDF النصية — نفس السبب الذي دفع لاختيار حوار الطباعة أصلاً، لكن بالتصوير بدل التضمين النصي هذه المرة فأصبح التنزيل المباشر ممكنًا.
- إضافة **"إقفال السنة المالية"** (`accounting/fiscal-year-close.html` + `fiscal-year-close-page.js` + `fiscal-year-service.js` + مخزن `FiscalYears` جديد، `DB_VERSION` → 27): بطلب صاحب المشروع، إقفال محاسبي تقليدي — قيد إقفال حقيقي يُصفّر كل حسابات الإيرادات والمصروفات المرحّلة لسنة ميلادية كاملة (عبر `computeIncomeStatement()` الموجودة أصلاً، فتبقى نتيجة الإقفال مطابقة لما تعرضه قائمة الدخل لنفس السنة) وينقل صافي الربح/الخسارة إلى `32103 صافي ربح السنة الحالية`؛ رصيد متبقٍ فيه من إقفال سنة أقدم يُرحَّل أولاً بقيد مستقل إلى `32101 أرباح سنوات سابقة`. **لا "نسخ"/"ترحيل" فعلي لأي بيانات أخرى** (قطيع/مخزون/ذمم/عهد/نقدية) — التطبيق دفتر أستاذ مستمر أصلاً فتستمر تلك الأرصدة كما هي بلا أي أثر لهذه الميزة عليها؛ فقط حسابات الإيرادات/المصروفات (وهي ما يُقفل تقليديًا في نهاية كل سنة مالية) تتأثر. `closeFiscalYear()` تقفل أيضًا الأشهر الاثني عشر للسنة تلقائيًا (`period-service.js`)، و`reopenFiscalYear()` تعكس كل ذلك بالكامل (حذف ناعم لقيود الإقفال/الترحيل + إعادة فتح الأشهر). الإقفال/إعادة الفتح مقصوران على مدير النظام وإلزاميًا بالترتيب الزمني (سنة تلو الأخرى) تفاديًا لتضارب سلسلة 32101↔32103. لا حاجة لتعديل `computeBalanceSheet()`/`computeIncomeStatement()` أنفسهما — صافي الدخل الحيّ فيهما (تراكمي منذ البداية) يعكس تلقائيًا نتيجة السنة الحالية غير المُقفلة فقط بمجرد وجود قيد الإقفال.
- إضافة **"تقرير التسمين"** (`reports/fattening-reports.html` + `fattening-reports-page.js`، رابط جديد في مجموعة "التقارير" بالقائمة الجانبية): فلتر دفعة واحدة أو الكل + حالة (نشطة/مغلقة/الكل)، KPI موجز (عدد الدفعات، عدد الحيوانات، إجمالي تكلفة العلف والأدوية، متوسط تكلفة الكيلو الإجمالي، إجمالي الزيادة في الوزن، إجمالي الربح الفعلي)، ثم قسمان دائمًا معًا: "ملخص الدفعات" (تكلفة لكل دفعة) و"تفصيل كل حيوان" (نفس التفاصيل المعروضة في نافذة `herd/fattening.html` لكن لكل الدفعات المفلترة معًا في جدول واحد). القسمان يُصدَّران معًا بضغطة واحدة (Excel: ورقتان منفصلتان، PDF: قسمان متتاليان) عبر `exportSectionsToExcel`/`exportSectionsToPdf`، بنفس نمط `inventory-reports.html`. لا حساب جديد — يعيد استخدام `computeFatteningBatchResults()` نفسها من `fattening-service.js`.
- إضافة **"سجل التسمين"** ضمن إدارة القطيع (`herd/fattening.html` + `fattening-service.js` + مخزن `FatteningBatches` جديد، `DB_VERSION` → 26): بطلب صاحب المشروع لحساب تكلفة العلف لكل رأس عند تسمين ذكور للبيع، بنمط "دفعة جماعية" (مجموعة حيوانات تتغذى مع بعض بنفس الفترة، وليس تسجيل مستقل لكل حيوان). كل دفعة (`batchNumber` تلقائي 'FTN-0001'…) تحمل قائمة حيوانات بتاريخ دخول/خروج ووزن دخول/خروج لكل واحد. تكلفة العلف **لا تُسجَّل من صفحة جديدة** بل عبر حقل اختياري جديد `linkedFatteningBatchId` على `Expenses` (قسم "ربط بدفعة تسمين" في `expense-form.html`، مستقل تمامًا عن ربط المخزون `linkedInventoryItemId` الموجود ويمكن استخدام الاثنين معًا لنفس المصروف) — بلا أي أثر محاسبي إضافي، مجرد "وسم" لتجميع لاحق. `computeFatteningBatchResults()` توزّع إجمالي تكلفة العلف المرتبطة بالدفعة على كل حيوان حسب نصيبه من إجمالي "أيام التغذية" لكل حيوانات الدفعة (وليس بالتساوي على الرؤوس)، وتحسب حيًا لكل حيوان: الزيادة في الوزن (من `WeightRecords` الموجود مسبقًا أو الوزن المُدخَل يدويًا عند الإضافة/الإخراج)، التكلفة الصحية المصاحبة (`HealthRecords.cost` ضمن فترة تواجده)، تكلفة الكيلو الواحد المُكتسَب، وسعر البيع/الربح الفعلي (`Revenues.animalId` المطابق). صفحة الدفعة بنمط "نموذج أعلى + جدول" (بدء دفعة جديدة + جدول الدفعات)، والتفاصيل (إضافة/إخراج حيوان، إغلاق/إعادة فتح الدفعة) في نافذة منبثقة واحدة (`hideFooter: true`، بنفس نمط تفاصيل العهد). رابط "سجل التسمين" جديد في `herd-form.html` (يظهر لكل حيوان بصرف النظر عن الجنس/الحالة، بنفس نمط سجل الوزن/السجل الصحي) يفتح `fattening.html?animalId=` مفلترًا على دفعات ذلك الحيوان فقط. لا تعميم بعد لصلاحيات `hasActionPermission` الدقيقة على هذه الصفحة (تعمل بمنطق "وصول لوحدة القطيع = وصول كامل"، نفس بقية صفحات القطيع الفرعية غير المُعمَّمة).
  - ⚠️ توضيح لاحق بطلب صاحب المشروع: "ربط بدفعة تسمين" على المصروف **ليس مقصورًا على العلف** — أي مصروف مشترك للدفعة كلها (أدوية، فيتامينات، أو غيرها) يُربط بنفس الحقل ويُوزَّع بنفس الآلية. أُعيدت تسمية الدوال/الحقول الداخلية لذلك (`computeBatchFeedCost` → `computeBatchLinkedCost`، `totalFeedCost` → `totalLinkedCost`، `allocatedFeedCost` → `allocatedCost`) وتحديث كل التسميات المعروضة ("تكلفة العلف" → "تكلفة العلف والأدوية")، وأُضيف عمود "تكلفة صحية فردية" منفصل في جدول تفاصيل الدفعة يعرض `HealthRecords.cost` الخاص بكل حيوان بمفرده (علاج/تحصين لحيوان بعينه لا مشترك بين الدفعة) بجانب التكلفة المشتركة الموزَّعة.
- إضافة "استيراد من ملف" في سجل القطيع (`herd/herd-list.html` + `herd-list-page.js`): زر جديد (مقيّد بصلاحية `add`) يفتح نافذة بها تنزيل قالب CSV (رأس أعمدة ثابت الترتيب + صف مثال) ورفع ملف Excel/CSV (`XLSX.read` من مكتبة `xlsx-js-style` المُحمَّلة أصلاً للتصدير). القراءة تعتمد على **ترتيب الأعمدة** لا نص العنوان الحرفي؛ كل صف يُعاد تعيين حقوله المكافئة لنموذج تكويد حيوان (النوع/الجنس/الحالة الصحية/المصدر عبر خرائط عربي↔قيمة، تاريخ مرن YYYY-MM-DD أو DD/MM/YYYY أو خلية تاريخ Excel حقيقية، الحظيرة بمطابقة الاسم النصي) ويُتحقق منه (كود مكرر داخل الملف أو مع سجلات موجودة، نوع/جنس/حالة/مصدر غير معروف، رقم غير صالح) قبل عرض معاينة بجدول يوضح كل صف صالح/به خطأ. زر "معاينة واستيراد" يتحول بعد التحليل إلى "تأكيد الاستيراد (عدد الصفوف الصالحة)" وينشئ الحيوانات فعليًا (تكويد تلقائي بنفس `generateNextAnimalCode` لأي كود فارغ) عند الضغط عليه ثانية — الصفوف الخاطئة تُستبعد بصمت مع بيان السبب في المعاينة فقط، بلا أي أثر محاسبي إضافي (نفس سلوك تكويد حيوان فردي من `herd-form.html`).
- إضافة نوع ثالث **"شريك"** في جهات التعامل (`Parties.type`)، يبيع/يشتري مثل عميل/مورّد لكن على حساب واحد موحّد **"جاري الشريك" (3010)** بدل `112`/`211`: `createParty()`/`updateParty()` يضبطان تلقائيًا مفتاح الربط الديناميكي `partyAccount:<id>` على `3010` عند إنشاء/تحويل شريك، فيعمل البيع/الشراء الآجل معه من نفس نموذجَي `revenue-form.html`/`purchase-form.html` (بند "شركاء" في قائمة الطرف) **بلا أي تعديل** في خدمتي `revenue-service.js`/`purchase-service.js` — الأولوية الأعلى لمفتاح `partyAccount:<id>` كانت مبنية في الكود مسبقًا. صفحة جديدة `parties/partner-capital.html` لضخ فلوس (مساهمة) أو سحب فلوس بواسطة الشريك (`createCapitalTransaction()` في `party-service.js`، أنواع جديدة `capitalIn`/`capitalOut` في `PartyTransactions`)، وخيار دفع جديد `partnerContribution` في نموذج الأصول الثابتة (مساهمة أصل من شريك، `fixed-asset-service.js`). `computePartyBalance()`/`getPartyLedgerEntries()` عُمِّمتا بفرع مخصص للشريك يجمع مبيعات/مشتريات آجلة + ضخ/سحب فلوس + مساهمات أصول في كشف حساب واحد (`party-statement.html`/`party-card.html`)؛ `isPartyOverdue()` تتجاهل الشريك (لا مفهوم "متأخر" لحساب جاري)، و`createReturnOrDiscount()` ترفضه صراحة (غير مدعوم بعد). 5 مفاتيح جديدة في "ربط العمليات بالحسابات" (`partnerControl` + نقدي/بنك لكل من ضخ وسحب الفلوس). تقارير الأطراف القديمة (`party-reports.html`) والتنبيهات (سقف ائتمان/تأخر سداد) بقيت مقصورة على عميل/مورّد فقط بقرار واعٍ.
- إضافة "الأرشفة الأسبوعية للقيود" (`accounting/weekly-batches.html` + `weekly-batch-service.js` + مخزن `WeeklyBatches` جديد، `DB_VERSION` → 25): بطلب صاحب المشروع تجميع كل قيود فترة (أسبوع مثلاً) تحت رقم أرشفة واحد للطباعة/المراجعة، **بلا** فك ربط القيود عن عملياتها المصدر ولا دمجها في قيد واحد فعلي — تبيّن أن الحل الأخف (تعديل شكلي فوق الآلية القائمة) أفضل من إعادة هيكلة محرك الترحيل المزدوج بالكامل لأن الأخير كان سيكسر تسوية البنوك (مطابقة سطر بسطر) ودفتر الصندوق الحي وقفل الفترات. `createWeeklyBatch()` يرحّل أي قيد "مسودة" ضمن الفترة (نفس آلية "مسودة/مُرحَّل" الموجودة أصلاً في `accounting-service.js`) ثم يضع `weeklyBatchId` على كل قيد غير مؤرشف — كل قيد يبقى بأثره المحاسبي المستقل. رابط جديد في القائمة الجانبية ضمن مجموعة "المحاسبة".
- إعادة هيكلة شجرة الحسابات بطلب صاحب المشروع:
  - حذف المجموعات الثلاث كاملةً بأوراقها: "6 تكاليف الإنتاج والتربية"، "7 مصروفات التشغيل والإدارة" (شامل فرع الإهلاك 75*)، و"8 إيرادات ومصروفات أخرى" — الشجرة الآن 150 حسابًا بدل ~206.
  - إضافة حساب "3010 جاري الشريك" تحت حقوق الملكية مباشرة (ورقة قابلة للترحيل).
  - ترحيل تلقائي لمرة واحدة (`_removeRetiredSystemAccountsIfNeeded` في seed.js) يحذف ناعمًا الحسابات المحذوفة من أي قاعدة بيانات قائمة **ما عدا** المستخدم منها في قيود تاريخية (يبقى كجذر مستقل حفاظًا على سلامة التقارير).
  - تنظيف مجموعة الحسابات النظامية فقط (system-only) من الأكواد الملغاة.
- ⚠️ تبعات متوقعة (مقصودة): نموذجا المصروفات والمشتريات بلا بنود حتى يضيف صاحب المشروع بنوده من شاشة شجرة الحسابات؛ وبعض مفاتيح "ربط العمليات بالحسابات" الافتراضية (الإهلاك/أرباح بيع أصل/فروقات الصندوق/إعدام العهد/البدائل العامة) معلّقة دون حساب — الخدمات ترمي رسالة واضحة أو تتخطى الترحيل لحين اختيار بدائل من شاشة الربط.

- تكويد القطيع حسب النوع: الأغنام تبدأ بـ `SH-` والماعز بـ `GO-` (بدل `GZ-` الموحدة) — `generateNextAnimalCode(type)` بعدّاد مستقل لكل بادئة، والأكواد القديمة `GZ-xxxx` تبقى كما هي. مطبّق في تكويد حيوان جديد (مع إعادة توليد تلقائية عند تغيير النوع بشرط عدم التعديل اليدوي)، والتكويد التلقائي للمواليد (يرث بادئة نوع الأم)، والتحويل من مخزون التجارة للقطيع.

- إلغاء أشرطة التبويبات العلوية (الأيقونات المتجاورة) من صفحات المحاسبة التسع والمخازن والجملة — التنقل الآن حصريًا عبر القائمة الجانبية (بقيت فقط التبويبات الداخلية داخل صفحات التقارير)، وأُضيف رابط "ربط العمليات بالحسابات" للقائمة الجانبية حتى لا تُيتم الصفحة.
- سلوك القائمة الجانبية: الضغط على عنوان أي مجموعة يفتح شاشتها الرئيسية مباشرة (أول رابط فيها) بدل فتح قائمة كل الصفحات الفرعية — والسهم المجاور للعنوان وحده يطوي/يفتح قائمة الصفحات الفرعية عند الحاجة (المجموعة التي تحتوي الصفحة الحالية تبقى مفتوحة تلقائيًا).
- إعادة تنظيم القائمة الجانبية: مجموعة منسدلة مستقلة "الجرد" تضم صفحات الجرد الأربع (جرد الصندوق/جرد المخازن/الجرد الحيواني/جرد الأصول الثابتة) بأيقونة موحدة، ومجموعة "التسويات" تضم التسويات المحاسبية وتسوية البنوك — كل رابط يحتفظ بصلاحية وحدته الأصلية (المجموعات عرض فقط بلا مفاتيح صلاحية جديدة).
- إضافة "الجرد الحيواني" (`herd/animal-count.html` + `animal-count-service.js` + مخزن `AnimalCounts`): جلسة جرد نهاية السنة المالية لكل حيوانات القطيع الحية — لكل حيوان قيمة تقديرية وحالة (موجود/مفقود) قابلة للتغيير الفردي (قائمة لكل صف أثناء الجلسة، وقائمة مباشرة في المحضر تُحفظ فورًا عند التغيير)، مع إجماليات حية، فلترة مركّبة (نوع/سلالة/فترة أعمار بالأشهر/بحث)، تطبيق جماعي للسعر أو الحالة على الفلترة الحالية، حفظ القطيع كاملًا بلا فقدان المعروض المفلتر، وتعديل الجلسات المحفوظة (قيم/حالات دفعة واحدة) + طباعة/تصدير/حذف ناعم. توثيقي بلا أثر محاسبي، مع لقطة من بيانات كل حيوان وقت الجرد. رابط في القائمة الجانبية (وحدة القطيع) + زر في سجل القطيع + إجراء سريع (+).
- تحكم مطلق في الربط المحاسبي: قسمان جديدان في شاشة "ربط العمليات بالحسابات" — "تكويد تفصيلي لكل بند" (لكل بند مصروف/مشترى: مخزون مرتبط يُرسمَل عليه الشراء بدل المصروف + حساب الدفع لكل طريقة نقدي/تحويل/آجل؛ ولكل بند إيراد: حساب التحصيل لكل طريقة والآجل قابل للربط بـ"العملاء - السوق" أو "الأفراد") و"حسابات الأطراف" (حساب ذمم مخصص لكل عميل/مورّد) — بتراجع تدريجي في الخدمات: حساب الطرف > حساب البند > العام > الافتراضي، ومفتاحا `animalSaleSheep`/`animalSaleGoat` لاقتراح بند بيع الحيوان تلقائيًا حسب النوع في نموذج الإيراد.
- توسيع "ربط العمليات بالحسابات" ليشمل كل عمليات النظام بتفصيل طرق الدفع (78 مفتاحًا بدل 58): مصروف (نقدي/تحويل/معلّق)، مشترى (نقدي/تحويل/آجل/معلّق)، إيراد (نقدي/تحويل/آجل)، اقتناء أصل (نقدي/تحويل/آجل)، بيع أصل (نقدي/تحويل/آجل)، سند قبض/سند صرف (نقدي/بنك) — بالإضافة لمفاتيح الجملة التفصيلية (مبيعات ذكور/إناث أغنام وماعز + تكلفة كل نوع) والعهد والمخزون والتسويات. كل خدمة ترحيل تستهلك مفتاحها التفصيلي الخاص.
- استبدال شجرة الحسابات المسطحة القديمة بشجرة هرمية كاملة (أب/فرعي + قائمة مالية + قابل للترحيل) — `_NEW_CHART_ACCOUNTS` في `seed.js` (~206 حسابات: أصول/خصوم/حقوق ملكية/إيرادات/تكلفة مبيعات/تكاليف إنتاج/مصروفات تشغيل/إيرادات ومصروفات أخرى).
  - للقواعد الموجودة مسبقًا: تُستبدل الشجرة القديمة بالجديدة لمرة واحدة — إضافة كل الحسابات الجديدة + إعادة توجيه سطور القيود وروابط `categoryAccountId` من كل حساب قديم للجديد (`ACCOUNT_CODE_MIGRATIONS`) + حذف القديمة ناعمًا + ترحيل أكواد الصندوق/البنك النصية في العهد/التسويات/جرد الصندوق. الحسابات المخصصة تبقى كجذور مستقلة.
  - ترحيل أتمتة كل الوحدات على الأكواد الجديدة: الصندوق 11101/البنك 11102، العهد 12105-12107، ذمم 112/211، مخزون 11301-11303، مخزون حيواني 1140101-1140204 (بندًا ببند في الجملة)، إيرادات بيع الحيوانات 411*/412*، تكلفة المبيعات 51101-51104، الإهلاك 75101-75104، أرباح/خسائر بيع أصل 82101/81101، خسائر العهد 81105، فروقات جرد 81104.
  - عرض شجري في `chart-of-accounts.html` (أعمدة: الأب/القائمة المالية/قابل للترحيل) + نموذج بحقول الهرم + فلتر قائمة مالية.
  - تقارير محاسبية هرمية (ميزان مراجعة/قائمة دخل/ميزانية برؤوس تجميعية وأوراق) + قيد يدوي على الأوراق القابلة للترحيل فقط.
  - حقل اختيار الحيوان في نموذج الإيراد يظهر مع أي حساب 411*/412* (`isAnimalSaleAccount`).
- إضافة "تسوية شاملة" في سجل العهد: زر بجانب كل موظف له رصيد متبقٍ في جدول "كشف حساب العهد" يفتح نافذة تسوّي دفعة واحدة أكثر من عهدة قائمة معًا (كل عهدة بمبلغها الكامل افتراضيًا، قابل للتعديل/الاستبعاد لكل صف) — لتصفية البواقي الصغيرة (5-10 ريال) المتفرقة على عدة عهد بدون فتح كل واحدة على حدة.
- إضافة خانة "فاتورة ضريبية؟" (نعم/لا) لنماذج المصروفات/المشتريات/الإيرادات: المبلغ المُدخل دائمًا شامل الضريبة، وعند اختيار "نعم" يُحسب المبلغ قبل الضريبة وقيمتها تلقائيًا (15%) عبر `calculateTaxBreakdown()` في `formatters.js` ويُعرض حيًا في النموذج. أُضيف أيضًا عمود "فاتورة ضريبية" في صفحات السجل الثلاث، وبطاقة/عمود "إجمالي الضريبة" في صفحات التقارير الثلاث (وضع الفترة الواحدة).
- استخراج ملف `al-ghazeez-farm.zip` داخل مجلد العمل.
- مراجعة بنية التطبيق والتأكد أنه تطبيق عربي RTL مبني بـ HTML/CSS/JavaScript ويستخدم IndexedDB.
- التأكد من وجود الوحدات الأساسية: تسجيل الدخول، لوحة التحكم، القطيع، الولادات، المصروفات، الإيرادات، الموظفون، العهد، الإشعارات، والتقارير.
- إنشاء صفحة الإعدادات `settings/settings.html`.
- إنشاء منطق صفحة الإعدادات `js/pages/settings-page.js` لحفظ بيانات المزرعة، تصدير نسخة احتياطية، استيراد نسخة احتياطية، وإعادة تهيئة البيانات.
- إنشاء صفحة تقارير المصروفات `reports/expense-reports.html`.
- إنشاء منطق تقارير المصروفات `js/pages/expense-reports-page.js`.

## قيد التنفيذ الآن

- فحص الروابط وملفات JavaScript/CSS المطلوبة للتأكد من عدم وجود مسارات مكسورة.
- تشغيل التطبيق محليًا لاختبار تسجيل الدخول والصفحات الرئيسية.

## المتبقي

- مراجعة أخطاء المتصفح إن وجدت بعد التشغيل.
- اختبار إضافة/تعديل/حذف سجلات القطيع والمصروفات والإيرادات.
- اختبار صفحات الموظفين والعهد والإشعارات والتقارير عمليًا.
- تحسين تجربة الرسوم البيانية في حال عدم توفر الإنترنت لتحميل Chart.js من CDN.

## ملاحظات للإكمال لاحقًا

- حسابا الدخول الافتراضيان يُنشآن تلقائيًا عند أول تشغيل من `js/db/seed.js` — راجع الملف لمعرفة الاسم/كلمة المرور، وغيّرهما من صفحة الإعدادات فور أول دخول.
- البيانات مخزنة في متصفح المستخدم داخل IndexedDB باسم `AlGhazeezFarmDB`.
- إذا احتجت إعادة التهيئة من الصفر، امسح IndexedDB/localStorage للموقع ثم افتح صفحة الدخول.
- التطبيق يعتمد حاليًا على Google Fonts وChart.js من CDN، لذلك قد تتأثر الخطوط والرسوم عند انقطاع الإنترنت.
