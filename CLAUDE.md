# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## كيفية استخدام هذا الملف
اقرأ هذا الملف أولاً في أي محادثة جديدة حول هذا المشروع — يكفي عادة لفهم البنية الكاملة دون قراءة كل الملفات من جديد. ارجع لقراءة ملف معيّن فقط عند الحاجة لتفاصيله الدقيقة قبل تعديله مباشرة.

قسم **"ملاحظات المستخدم"** في نهاية هذا الملف مخصص لصاحب المشروع للتعديل اليدوي مباشرة، بمعزل عن أي اقتراحات. **أي تحديث لاحق لهذا الملف (من Claude) يجب أن يحافظ على ذلك القسم كما هو، ولا يعيد كتابته أو يحذفه.**

## نظرة عامة على المشروع
**مزرعة الغزيز (Al-Ghazeez Farm)** — تطبيق ويب لإدارة مزرعة أغنام وماعز: القطيع، الولادات، المصروفات، الإيرادات، الموظفون، إدارة العهد (نقدية وعينية)، مركز التنبيهات، التقارير، الإعدادات.

- عربي بالكامل، RTL (`dir="rtl"` في كل صفحة)، خط Cairo من Google Fonts.
- تطبيق **من جهة العميل فقط (client-side)** بالكامل — لا يوجد أي خادم أو باك-إند أو API.
- لا يوجد `package.json`، لا أدوات بناء (build)، لا bundler، لا linter، لا اختبارات تلقائية. الكود ملفات HTML/CSS/JS ثابتة تُحمَّل عبر وسوم `<script>` عادية (دوال/ثوابت عامة على `window`، بدون ES modules).
- سجل التقدم الحالي للمشروع (بالعربي، يُحدَّث يدويًا) موجود في `PROJECT_PROGRESS.md` — راجعه لمعرفة آخر حالة/خطوة بدل الافتراض من هذا الملف.

## التشغيل
لا يوجد أي أمر بناء أو تشغيل عبر npm/أدوات مشابهة:
- افتح `index.html` مباشرة في المتصفح، أو (أفضل) قدّمه عبر خادم استضافة ثابت محلي بسيط (مثل `npx serve .` أو `python -m http.server`) بدل فتحه كـ `file://` مباشرة، تفاديًا لأي تضارب محتمل في IndexedDB أو تحميل خط Cairo/Chart.js من CDN بدون اتصال.
- حسابان افتراضيان (role: `systemAdmin` و role: `farmManager`) يُنشآن تلقائيًا عند أول تشغيل عبر `js/db/seed.js` — الاسم وكلمة المرور مكتوبان في ذلك الملف. لا تنسخ قيمهما إلى هذا الملف أو أي توثيق آخر (المستودع قد يُشارك). `seed.js` **لا يدرج أي بيانات تشغيلية تجريبية** (قطيع/مصروفات/إيرادات) — قاعدة البيانات تبدأ نظيفة عدا الحسابين وشجرة الحسابات الافتراضية (`_NEW_CHART_ACCOUNTS`، هرمية كاملة: أب/فرعي + قائمة مالية + قابل للترحيل). لقواعد البيانات الموجودة مسبقًا (بشجرة مسطحة قديمة): `_migrateChartOfAccountsIfNeeded()` **يستبدل** الشجرة القديمة بالجديدة لمرة واحدة — يُضيف كل حسابات الشجرة الجديدة الناقصة، يعيد توجيه سطور القيود اليومية وروابط `categoryAccountId` من كل حساب قديم إلى الحساب الجديد المقابل له (`ACCOUNT_CODE_MIGRATIONS`)، يحذف الحسابات القديمة ناعمًا، ويرحّل أكواد الصندوق/البنك النصية (`'1000'/'1010'` → `'11101'/'11102'`) في `CustodyItems`/`Adjustments`/`CashCounts` — الحسابات المخصصة للمستخدم (بكود خارج الخريطة) تبقى كما هي كجذور مستقلة.
- لإعادة ضبط البيانات: زر "حذف البيانات وإعادة البداية" في صفحة الإعدادات، أو امسح يدويًا قاعدة IndexedDB باسم `AlGhazeezFarmDB` من أدوات المطوّر بالمتصفح.

## البنية المعمارية

### طبقة البيانات — كل شيء يمر من `js/db/db.js`
قاعدة IndexedDB واحدة (`AlGhazeezFarmDB`, `DB_VERSION = 27` حاليًا) تحوي 46 مخزن معرّفة في مصفوفة `OBJECT_STORES`: `Users, Animals, BirthRecords, DeathRecords, HealthRecords, Expenses, Revenues, Purchases, Employees, CustodyItems, Notifications, Categories, Attachments, BulkPurchases, BulkSales, BulkExpenses, BulkBatches, BulkPurchaseBatches, BulkSaleBatches, Parties, InventoryItems, InventoryMovements, ChartOfAccounts, JournalEntries, StockCounts, Locations, WeightRecords, Vaccinations, Transfers, CustodySettlements, PartyTransactions, MatingRecords, PregnancyRecords, FixedAssets, FixedAssetTransactions, FixedAssetCounts, AccountingPeriods, Adjustments, CashCounts, BankStatementLines, AccountMappings, AnimalCounts, AuditLog, WeeklyBatches, FatteningBatches, FiscalYears`. كل مخزن `keyPath: 'id'` مع `autoIncrement`.

⚠️ **حالة "مسودة/مُرحَّل" للقيود اليومية (`JournalEntries.status`)**: كل ترحيل تلقائي (كل `sync*JournalEntry` في خدمات المصادر) يُنشئ القيد بلا `status` صريح فيتحفّظ `'draft'` (القيمة الافتراضية في `createJournalEntry()`، `accounting-service.js`) — **لا يؤثر على أي رصيد/تقرير/تنبيه حتى يُرحَّل** (`getPostedJournalEntries()` هي المصدر الوحيد الصحيح لأي حساب، تستبعد `status === 'draft'`). القيد اليدوي المباشر (`journal-entry-form-page.js`) يرسل `status: 'posted'` صراحة فيترحّل فورًا كما كان دومًا؛ القيود القديمة بلا حقل `status` تُعامَل مُرحَّلة ضمنيًا (توافق خلفي بلا migration). `postJournalEntry(id)`/`revertJournalEntryToDraft(id)` في `accounting-service.js` تبدّلان الحالة يدويًا؛ `journal-entries.html` (`journal-entry-list-page.js`) بها فلتر حالة + زر "ترحيل" لكل قيد مسودة + زر "ترحيل كل المسودات" (يُطبَّق على القيود المعروضة بعد الفلاتر، ويتخطى بصمت أي قيد بتاريخ ضمن فترة مغلقة مع رسالة تحذير واحدة مجمّعة).

⚠️ **`BulkPurchases`/`BulkSales`/`BulkExpenses`/`BulkBatches` قديمة/غير مستخدمة لبيانات جديدة**: ثلاثة أجيال متعاقبة من نماذج "الشراء والبيع الجماعي" بقيت أسماؤها في `OBJECT_STORES` كأرشيف تاريخي فقط، بلا أي صفحة/خدمة حالية تكتب فيها بيانات جديدة. الجيل الأول `BulkPurchases`/`BulkSales`/`BulkExpenses` (سجل مستقل لكل معاملة، بلا محاسبة مزدوجة) استُبدل بالجيل الثاني `BulkBatches` (سجل "دفعة" واحد يخلط شراء+بيع+مصاريف بقيد محاسبي واحد مُعاد الصياغة، يُحمِّل تكلفة الشراء كمصروف فوري بصرف النظر عن البيع)، ثم استُبدل هذا بدوره بالجيل الثالث والحالي `BulkPurchaseBatches`/`BulkSaleBatches` (شراء وبيع مستقلّان تمامًا، كل عملية بقيدها الخاص، ومتوسط تكلفة متحرك — انظر صفَّيهما في جدول "نموذج البيانات" أدناه). `_migrateLegacyBulkDataIfNeeded()` ثم `_migrateBulkBatchesToPurchaseSaleIfNeeded()` في `seed.js` تُرحِّلان تلقائيًا ولمرة واحدة كل الأجيال القديمة إلى الجيل الحالي عند أول تشغيل بعد كل تحديث؛ النسخ الاحتياطي في `settings-page.js` يستمر بتكرار `OBJECT_STORES` بالكامل فتبقى الأجيال القديمة محفوظة أرشيفيًا داخله.

كل الوصول لقاعدة البيانات يمر عبر دوال `db.js` العامة:
- `dbAdd(storeName, record)` — يضيف `createdAt` تلقائيًا إن لم يكن موجودًا.
- `dbGet(storeName, id)` / `dbGetAll(storeName)` / `dbQuery(storeName, filterFn)`
- `dbUpdate(storeName, id, fields)` — يدمج الحقول ويضيف `updatedAt` تلقائيًا.
- `dbSoftDelete(storeName, id)` — يضبط `status: 'deleted'` و`isDeleted: true` معًا.
- `dbHardDelete(storeName, id)` — حذف فعلي. مستخدم حاليًا فقط في `deleteAttachment()` (`attachment-service.js`) لحذف مرفقات الفواتير نهائيًا؛ لا يُستخدم لأي مخزن آخر (البقية تعتمد الحذف الناعم `dbSoftDelete`).

⚠️ **سجل تدقيق شامل تلقائي (`AuditLog`)**: `dbAdd`/`dbUpdate`/`dbHardDelete` الثلاثة تستدعي داخليًا `_logAudit()` (كلها في `db.js` نفسه، بلا أي نقطة كتابة أخرى في التطبيق) فتُسجِّل تلقائيًا كل عملية على **أي** مخزن (باستثناء `AuditLog` نفسه، تفاديًا للتكرار الذاتي): `create` (لقطة كاملة للسجل الجديد `snapshot`)، `update` (فرق حقل-بحقل `changes: [{field, oldValue, newValue}]` بين النسخة القديمة والجديدة، متجاهلاً `id`/`createdAt`/`updatedAt`؛ لا تُسجَّل حركة أصلاً لو الحفظ لم يغيّر أي قيمة فعليًا)، `delete` (نفس آلية `update` لكن عند انتقال `status` إلى `'deleted'` تحديدًا — أي أن `dbSoftDelete` يُسجَّل كـ`delete` لا `update` تلقائيًا بلا حاجة لتمييز صريح من المستدعي)، و`hardDelete` (لقطة السجل قبل حذفه، تُجلب خصيصًا داخل `dbHardDelete` نفسها لأنها لا تمرّ بـ`dbGet` مسبقًا كباقي الدوال). حقلا `passwordHash` (`Users`) و`blob` (`Attachments`) **محجوبان دومًا** بعلامة `'(محجوب)'` بدل القيمة الحقيقية (سواء في `changes` أو `snapshot`) — يُسجَّل أن الحقل تغيّر بلا كشف الهاش الفعلي أو تخزين الملف الثنائي في سجل التدقيق. كل عملية تدقيق **ثانوية دومًا**: أي خطأ فيها (مُغلَّف بـ`try/catch` صامت + تحذير `console.warn`) لا يوقف العملية الأساسية ولا يرفض الـ`Promise` المُرجَع من `dbAdd`/`dbUpdate`/`dbHardDelete`. لا حاجة لأي تعديل في أي service حالي — الآلية شفافة تمامًا فوق طبقة `db.js` الموجودة. القراءة/العرض حصرًا من `js/services/audit-service.js` (`getAllAuditLogs()`, `getAuditStoreLabel()`, `getDistinctAuditFacets()`) عبر `settings/audit-log.html` (`audit-log-page.js`) — **مقصورة على `resolveRoleKey(role) === 'systemAdmin'`** (نفس مبدأ `disposeFixedAsset`/`period-lock.html`) لأن السجل يكشف تفاصيل حسّاسة عبر كل الوحدات (رواتب، صلاحيات مستخدمين، ...)؛ فلاتر نوع السجل/الإجراء/المستخدم/الفترة، وزر "عرض" لكل حركة يفتح تفاصيل الحقول المتغيّرة (أو اللقطة الكاملة لـ`create`/`hardDelete`) في نافذة منبثقة.

**لإضافة مخزن جديد**: أضف اسمه إلى `OBJECT_STORES` وارفع `DB_VERSION` — حلقة `onupgradeneeded` تُنشئ أي مخزن جديد تلقائيًا، لا حاجة لأي تعديل آخر في `db.js`. ملاحظة: رفع `DB_VERSION` وحده لا يملأ المخزن الجديد بأي بيانات لقواعد المستخدمين الموجودة مسبقًا — إن احتجت تهيئة بيانات افتراضية للمخزن الجديد فأضف دالة تهيئة منفصلة في `seed.js` مفحوصة بمعزل عن شرط "هل Users فارغ؟" (مثال: `_migrateChartOfAccountsIfNeeded`، المُستدعاة من `seedDatabaseIfEmpty()` وتفحص شرطها الخاص)، وإلا ستبقى قواعد المستخدمين القدامى بمخزن فارغ.

⚠️ **تناقض قائم في الكود**: بعض الخدمات تفلتر السجلات المحذوفة بـ `status !== 'deleted'` (Animals, Expenses, Revenues, Employees, CustodyItems) وبعضها بـ `!isDeleted` (BirthRecords). كلا الحقلين يُضبطان معًا داخل `dbSoftDelete` فلا يوجد خلل فعلي حاليًا، لكن أي كود جديد يفلتر سجلات يجب أن يتأكد أي الحقلين تستخدمه الخدمة المقابلة في `js/services/`.

⚠️ **لا يوجد encapsulation حقيقي**: بما أن الملفات مجرد سكريبتات عامة (globals)، حتى المتغير `_dbInstance` في `db.js` (المفترض أنه خاص بحكم الشرطة السفلية) يمكن لأي سكريبت آخر الوصول له وتعديله مباشرة. `settings-page.js` يفعل ذلك فعليًا عند "حذف البيانات وإعادة البداية" (`_dbInstance.close(); _dbInstance = null;`) وأيضًا يفتح transactions مباشرة (`_clearStore`) بدل استخدام دوال `db.js` العامة — وهو الاستثناء الوحيد لقاعدة "كل الوصول عبر db.js".

### تحميل الملفات: لا ES modules، الترتيب يهم
كل صفحة HTML تحمّل مجموعة من ملفات JS بترتيب شبه ثابت:
1. Chart.js من CDN — فقط في الصفحات التي بها رسوم بيانية (`dashboard.html` وأغلب صفحات `reports/*.html`).
2. `js/db/db.js`
3. `js/services/auth-service.js` (و`seed.js` أيضًا — فقط في `index.html`).
4. باقي ملفات `js/services/*` المطلوبة لتلك الصفحة.
5. `js/utils/validators.js` — فقط في الصفحات التي بها نماذج تحقق (بما فيها Modals مثل الموظفين/العهد) — و`js/utils/formatters.js`.
6. مكوّنات `js/components/*` المطلوبة (`sidebar.js`, `header.js`, `modal.js`, `toast.js`, وأحيانًا `data-table.js`/`chart-renderer.js`).
7. سكريبت الصفحة نفسها من `js/pages/*` — دائمًا آخر عنصر، لأنه يعتمد على كل ما سبق.

عند إضافة صفحة/خدمة جديدة، انسخ ترتيب صفحة مشابهة موجودة بالضبط.

### بنية الصفحات (Multi-Page App) والمسارات النسبية
- الجذر يحوي `index.html` (تسجيل الدخول) و`dashboard.html` فقط.
- كل وحدة وظيفية لها مجلدها الخاص لصفحات HTML: `herd/`, `expenses/`, `revenues/`, `purchases/`, `bulk/`, `parties/`, `inventory/`, `fixed-assets/`, `employees/`, `custody/`, `notifications/`, `reports/`, `settings/`, `accounting/` — وكلها تشير لملفات `../css/` و`../js/` (مستوى واحد للأعلى). ⚠️ `fixed-assets/` وليس `assets/` — الأخير مجلد ثابت (static) موجود مسبقًا يحوي فقط `assets/logo.png` المستخدم في شعار الشريط الجانبي، لا علاقة له بوحدة الأصول الثابتة رغم تشابه الاسم.
- **نفس قائمة أسماء المجلدات الثلاثة عشر مكرّرة يدويًا في مكانين منفصلين**: `sidebar.js` (`knownFolders` داخل `getRootPath`) و`auth-service.js` (ثابت واحد مشترك `KNOWN_MODULE_FOLDERS` يغذّي كلاً من `logout()` و`requireAuth()`) — `header.js` لا يكرّر القائمة بذاته، بل يستدعي `getRootPath()` نفسها من `sidebar.js` لضبط رابط جرس التنبيهات. أي مجلد وحدة جديد **يجب** إضافته في المكانين معًا وإلا ستنكسر الروابط النسبية بعد التنقل أو تسجيل الخروج.

### المصادقة (بسيطة، محلية فقط) وصلاحيات الوحدات (module-level)
- `js/services/auth-service.js`: هاش كلمة المرور بـ SHA-256 عبر `crypto.subtle` (بدون salt)، تخزين الجلسة في `localStorage['currentUser']` كـ JSON عادي بدون توكن/تشفير — كافٍ لتطبيق محلي أحادي المستخدم، غير آمن لأي سيناريو حقيقي متعدد المستخدمين.
- كل صفحة محمية تستدعي `requireAuth(moduleKey)` أول شيء داخل `DOMContentLoaded` — `moduleKey` اختياري (مثل `'herd'`, `'expenses'`, ...، ويطابق دومًا مفتاح **وحدة** لا رابط شريط جانبي بعينه؛ الآن بعد تفريع `SIDEBAR_LINKS` لعدة روابط لكل وحدة، كل صفحة فرعية داخل نفس الوحدة تمرّر نفس moduleKey — مثلاً كل صفحات القطيع التسع تستدعي `requireAuth('herd')` بصرف النظر عن مفتاحها الفريد في `SIDEBAR_LINKS`)؛ `dashboard-page.js` وحدها تستدعيها بلا وسيط فتبقى متاحة دومًا لأي مستخدم مسجّل دخوله. ⚠️ **مجموعتا "الجرد" و"التسويات"** في `SIDEBAR_GROUPS` (`counts` و`settlements`) استثناءان مقصودان من قاعدة "المجموعة = وحدة": "الجرد" تجمع صفحات الجرد الأربع (جرد الصندوق/جرد المخازن/الجرد الحيواني/جرد الأصول الثابتة) رغم تبعية كل صفحة لوحدتها الأصلية، و"التسويات" تجمع التسويات المحاسبية وتسوية البنوك (من وحدة المحاسبة) — كل رابط يحتفظ بـ`module` وحدته الأصلية (accounting/inventory/herd/assets) فالصلاحية تُفحص على الوحدة الأصلية لا على مجموعة العرض، ولا يوجد مفتاح وحدة `counts`/`settlements` في نظام الصلاحيات.
- `hasModuleAccess(moduleKey)` (في `auth-service.js`) هي مصدر الحقيقة الوحيد للتحقق من **الوصول لوحدة كاملة**: `resolveRoleKey(role) === 'systemAdmin'` يمنح وصولاً كاملاً دومًا؛ غير ذلك تُفحص `Users.permissions` (انظر جدول "نموذج البيانات" أعلاه). تُستخدم في مكانين: داخل `requireAuth()` لمنع الدخول المباشر عبر الرابط، وداخل `sidebar.js` (`renderSidebar`) لإخفاء روابط الوحدات غير المسموحة من القائمة الجانبية (بمقارنة `link.module || link.key`، لأن روابط الوحدة الواحدة المتعددة تتشارك صلاحية واحدة).
- ⚠️ **صلاحيات دقيقة لكل زر داخل الوحدة (`hasActionPermission(moduleKey, action)`)**: طبقة ثانية فوق `hasModuleAccess` (تفترض ضمنيًا أنها صحيحة أصلاً، وتُعيد `false` مباشرة إن لم تكن كذلك). `action` من `ACTION_KEYS` الثابتة في `auth-service.js`: `'add'|'edit'|'delete'|'approve'|'print'|'export'` (تسمياتها العربية في `ACTION_LABELS`). `Users.permissions` صار الآن **نوعًا اتحاديًا (union)** بلا سكريبت ترحيل — التوافق الخلفي مضمّن بالكامل في القراءة: غائب (`undefined`) = وصول كامل لكل شيء (كما كان دومًا)؛ مصفوفة نصوص قديمة = وصول كامل لكل الأزرار الستة في كل وحدة مذكورة؛ كائن جديد `{[module]: true | {add,edit,delete,approve,print,export}}` (`true` يُستخدم لـ`systemAdmin` فقط، أي كائن فرعي يمثّل صلاحيات دقيقة، مفتاح غائب فيه = `false`). تُبنى/تُحرَّر هذه المصفوفة من قسم "إدارة المستخدمين والصلاحيات" في `settings/settings.html` (`js/pages/settings-page.js`: جدول `.permissions-matrix`، وحدة × 6 أزرار، عمود checkbox رئيسي للوحدة يُفعّل/يعطّل الستة التابعة له). **ملاحظة مهمة لإصلاح لازم أُجري بالتزامن مع هذه الميزة**: `login()` كانت تحوّل أي شكل غير-مصفوفة لـ`Users.permissions` (بما فيها الكائن الجديد) إلى `null` داخل جلسة `localStorage['currentUser']`، و`null` يعني "وصول كامل" — أي فجوة أمنية فورية لأي مستخدم صلاحياته بالشكل الكائني الجديد؛ صُحِّحت لتحافظ على الشكل كما هو. ⚠️ **التطبيق الفعلي (إخفاء/تعطيل الأزرار المقابلة داخل الصفحات) مُعمَّم الآن على معظم وحدات CRUD** (كان مقصورًا على المصروفات فقط كنموذج مرجعي، ثم عُمِّم بنفس النمط بالضبط إلى: الإيرادات، المشتريات، الموظفون، العملاء/الموردون/الشركاء، المخزون (الأصناف والحركات)، الأصول الثابتة (القائمة/النموذج/البطاقة)، الشراء والبيع بالجملة، القطيع (القائمة/النموذج)، وإدارة العهد (إصدار/تصفية/تسوية/تصدير) — إخفاء "+ تكويد"/أزرار التصدير بلا `add`/`export`، منع دخول نموذج الإنشاء/التعديل مباشرة عبر الرابط بلا صلاحية `add`/`edit` مع Toast وإعادة توجيه لقائمة الوحدة، إخفاء زر الحذف بلا `delete`، وفي المصروفات/المشتريات تحديدًا تعطيل حقل الحالة `approved`/`pending` بلا `approve` (نفس منطق دورة الاعتماد أدناه) — تصدير PDF يتطلب `export` **و** `print` معًا في كل هذه الوحدات — قرار صلاحيات قائم أُبقي عليه عمدًا رغم أن التصدير صار تنزيلاً مباشرًا لملف PDF حقيقي بدل `window.print()` فعليًا (انظر ملاحظة `export-utils.js` أعلاه). **لم يُعمَّم بعد** على: وحدة المحاسبة (قيود يومية/تسويات/تسوية بنوك، لها آليات ترحيل/اعتماد خاصة بها)، وصفحات القطيع الفرعية (مواليد/نفوق/صحي/تحصينات/تلقيح/حمل/أوزان/جرد حيواني — كل صفحة نمط "نموذج أعلى + جدول" مستقل يحتاج تطبيقًا منفصلاً)، وهذه ما زالت تعمل بمنطق "وصول للوحدة = وصول كامل لكل شيء داخلها" لحين تعميم نفس النمط عليها لاحقًا.

⚠️ **دورة الاعتماد (قفل السجلات المعتمدة)**: صلاحية `approve` نفسها تُعاد استخدامها أيضًا كشرط لتعديل/حذف سجل حالته `status: 'approved'` (`Expenses`/`Purchases` فقط — المخزنان الوحيدان بحقل `status` من نوع اعتماد حاليًا؛ `Revenues` لا يملك هذا المفهوم بعد). التطبيق في `expense-form-page.js`/`purchase-form-page.js`: عند فتح سجل معتمد للتعديل بلا صلاحية `approve` → `showToast` وإعادة توجيه فوري لصفحة القائمة (نفس نمط فحص `add`/`edit` أعلى `DOMContentLoaded`)، وزر الحذف يبقى مخفيًا كذلك. لا حقل/صلاحية جديدة مستقلة — من يملك `approve` هو من يملك التراجع عنه أصلاً.
- **أدوار النظام** (`SYSTEM_ROLES` في `auth-service.js`، ثلاثة فقط حاليًا بقرار عمل صريح — بقية الأدوار المطروحة سابقًا كمقترح مستقبلي لم تُنفَّذ بعد): `systemAdmin` ("مدير النظام") وصول كامل دومًا بصرف النظر عن `Users.permissions` (`fullAccess: true`)؛ `farmManager` ("مدير المزرعة") و`custom` ("مستخدم مخصص") كلاهما مقيَّدان فعليًا بـ`Users.permissions` المحفوظة له بلا أي تمييز في الكود بينهما غير `defaultPermissions` (تعبئة تلقائية قابلة للتعديل الكامل عند اختيار الدور في نموذج المستخدم بصفحة الإعدادات — `farmManager` معبَّأ بمعظم الوحدات، `custom` يبدأ فارغًا). ⚠️ **توافق خلفي**: القيم القديمة `role: 'manager'`/`role: 'owner'` (قبل هذا النظام الثلاثي — `manager` كان يمنح وصولاً كاملاً دومًا، `owner` كان مقيَّدًا) تُرحَّل تلقائيًا لمرة واحدة إلى `systemAdmin`/`custom` عبر `_migrateLegacyUserRolesIfNeeded()` في `seed.js`؛ ولأي جلسة لم تُرحَّل بعد وقت هذا التغيير، `resolveRoleKey(role)` في `auth-service.js` طبقة توافق إضافية تُترجم القيم القديمة أينما استُخدم `role` فعليًا في الكود (وليس فقط `hasModuleAccess`) — راجعها قبل إضافة أي فحص جديد على `user.role` مباشرة بدل المرور عبر `resolveRoleKey()`. إدارة المستخدمين وصلاحياتهم تتم حصرًا من قسم "إدارة المستخدمين والصلاحيات" في `settings/settings.html` (يظهر لـ `resolveRoleKey(role) === 'systemAdmin'` فقط).

### نمطان مختلفان للـ CRUD — اختر المناسب عند إضافة ميزة جديدة
1. **صفحة قائمة + صفحة نموذج منفصلة** (القطيع، المصروفات، الإيرادات): `xxx-list.html` يعرض `renderDataTable` مع `onRowClick` يوجّه إلى `xxx-form.html?id=<id>`؛ صفحة النموذج تفرّق بين إنشاء/تعديل حسب وجود `?id=` في `URLSearchParams`.
2. **صفحة قائمة + Modal للإنشاء/التعديل** (الموظفون): زر "+ تكويد..." و`onRowClick` كلاهما يفتحان نفس `openModal(...)`، بدون صفحة `form` منفصلة، وبدون service منفصل — تُستخدم دوال `db.js` مباشرة (`dbGetAll/dbAdd/dbUpdate`) من داخل سكريبت الصفحة.
3. **مدخل (hub) + صفحات عملية منفصلة لكل إجراء** (إدارة العهد وحدها حاليًا): `custody-hub.html` صفحة مدخل بلا CRUD خاص بها (بطاقات `kpiGrid` من `kpi-bar.js` تؤدي لصفحات منفصلة: إصدار/تسوية/سجل/كشف حساب/تقارير/محفظة موظف)، كل عملية إنشاء/تسوية لها **service حقيقي** (`custody-service.js`) بدل `dbAdd/dbUpdate` مباشرة من سكريبت الصفحة، لأن كل عملية تُشغِّل أتمتة محاسبية/مخزنية حقيقية (انظر صف `CustodyItems` في "نموذج البيانات" وقسم "خريطة الوحدات" أدناه).

### المكوّنات المشتركة (`js/components/`)
- `data-table.js` — `renderDataTable(containerId, columns, rows, options)`: بحث + فرز + ترقيم صفحات، تُستخدم في كل جدول تقريبًا. لتحديث الصفوف بعد تعديل بدون إعادة تعريف الأعمدة: `updateDataTableRows(containerId, newRows, newFooterRow?)`. `options.footerRow` (اختياري): كائن بنفس مفاتيح `columns` يُعرض كصف إجمالي ثابت في `<tfoot>` أسفل الجدول — لا يتأثر ببحث/فرز/ترقيم الجدول الداخلي لأنه يُحسب من الطرف المستدعي على كامل مجموعة الصفوف المفلترة (لا صفوف الصفحة الحالية فقط). مستخدم في جداول تقارير المصروفات/الإيرادات/المشتريات/القطيع لعرض صف "الإجمالي".
- `modal.js` — `openModal(html, options)` و`confirmDelete(message, onConfirmed)` (اختصار جاهز لتأكيد الحذف، مستخدم في كل صفحات الحذف). زر التأكيد/الإلغاء دومًا خارج أي `<form>` بمحتوى النافذة (بنية `modal-box__footer` منفصلة عن `modal-box__body`) — لذا `openModal` تعترض بنفسها ضغط **Enter** داخل أي حقل إدخال/اختيار (ما عدا `textarea`) في أي `<form>` بمحتوى النافذة وتُحوّله لنفس ضغط زر التأكيد؛ الاعتماد على حدث `submit` وحده غير كافٍ لأن معيار HTML لا يُطلقه ضمنيًا عند Enter إلا لحقل نصي واحد أو زر submit داخل النموذج نفسه (لا ينطبق على أي نموذج بهذا التطبيق تقريبًا). `onConfirm` أيضًا مُغلَّفة بـ`try/catch` (متزامنة أو `async`) تعرض Toast خطأ عام عند أي رفض غير متوقّع بدل الفشل الصامت.
- `toast.js` — `showToast(message, 'success'|'error'|'warning'|'info')`.
- `chart-renderer.js` — غلاف فوق Chart.js: `renderLineChart` / `renderBarChart` / `renderPieChart(canvasId, ...)`. يحتفظ بمرجع كل رسم في `_chartInstances[canvasId]` ويدمّره قبل أي إعادة رسم — أي canvas جديد يحتاج `id` فريد، ويتطلب تحميل Chart.js من CDN **قبل** هذا الملف في الصفحة. الخيارات تضبط `maintainAspectRatio: false` دائمًا، لذا **كل canvas يجب أن يُلفّ بـ `<div class="chart-box"><canvas id="..."></canvas></div>`** (تعريف `.chart-box` في `components.css`, ارتفاع ثابت 300px / 240px على الشاشات الصغيرة) — بدون هذا اللف يفقد الرسم بُعده وقد ينهار أو يتمدد بلا حدود.
- `sidebar.js` / `header.js` — `renderSidebar(activePageKey)` و`renderHeader(pageTitle)` تُستدعيان أول كل صفحة محمية. `renderHeader` ترسم أيضًا جرس تنبيهات في الهيدر (رابط مباشر إلى `notifications/alerts-center.html` عبر `getRootPath()`) **+ شارة عداد حمراء حيّة** (`#alerts-badge`، بعدد التنبيهات غير المقروءة — انظر `isRead`/`getReadAlertIds` في قسم "التنبيهات" أدناه) تُحدَّث تلقائيًا في نهاية كل `renderHeader()` عبر `refreshAlertsBadge()` (معرَّفة في `header.js` نفسه، مُصدَّرة عامة لتستدعيها صفحات أخرى بعد حدث فوري مثل تسجيل ولادة/نفوق — انظر `births-page.js`/`deaths-page.js`). ⚠️ **`computeAllAlerts()` تعتمد على نحو 15 ملف `service` غير محمَّلة ثابتًا إلا في 3 صفحات (`dashboard.html`/`alerts-center.html`/`settings.html`)** — بدل تكرار وسوم `<script>` هذه في عشرات الصفحات الأخرى، `refreshAlertsBadge()` تحقن ديناميكيًا (`_ensureAlertsDependenciesLoaded`/`_loadScriptOnce` في `header.js`) أي ملف ناقص فقط (بفحص وجود دالة مميّزة منه أولاً لتفادي إعادة تحميل ملف محمَّل ثابتًا بالفعل — إعادة تنفيذ `const` بمستوى ملف مرتين تُسبب خطأ)، بترتيب تسلسلي، Promise واحد مخبّأ لكل تحميل صفحة. بذلك العداد حيّ 100% في كل صفحة بالتطبيق بلا حاجة لتعديل ملفات HTML.
- `attachment-manager.js` — أرشفة مرفقات الفواتير (صور/PDF حتى 10 ميجابايت، مخزَّنة كـ Blob في مخزن `Attachments`). `openAttachmentsModal(recordType, recordId, recordLabel?)` يفتح نافذة `openModal(..., { hideFooter: true })` تعرض قائمة مرفقات السجل (عرض/تنزيل/حذف) وحقل رفع ملف جديد؛ يتطلب تحميل `modal.js`, `toast.js`, و`attachment-service.js` قبل هذا الملف. مستخدم من أزرار "📎 مرفقات" في أعمدة جداول `bulk-purchases.html` (`recordType: 'bulkPurchase'`), `bulk-sales.html` (`recordType: 'bulkSale'`), `custody-list.html` (`recordType: 'custody'`), و`employee-wallet.html` (زر لكل حدث في السجل الزمني: `recordType: 'custody'` لإصدار العهدة نفسها، أو `'custodySettlement'` لكل تسوية على حدة) (زر داخل الصف/الحدث يستدعي `event.stopPropagation()` عند الحاجة لتفادي تفعيل `onRowClick`). **قيد معماري مهم**: النافذة المنبثقة في هذا التطبيق لا تدعم التكديس (`openModal` يستبدل `#modal-root` بالكامل في كل استدعاء) — لذلك حذف مرفق يستخدم `window.confirm()` الأصلي للمتصفح بدل `confirmDelete()` المعتاد، تفاديًا لفتح نافذة تأكيد متداخلة تُغلق نافذة المرفقات نفسها. لا رابط مباشر بين المرفقات وصفحات النماذج (`expense-form.html` وغيرها) — الرفع يتم فقط من صفحة القائمة بعد وجود `id` فعلي للسجل، لتفادي مشكلة إرفاق ملفات بسجل لم يُحفظ بعد. ⚠️ **`expense-list.html`/`revenue-list.html`/`purchase-list.html` لم تعد تستخدم هذا الملف**: زر "📎 مرفقات" (`recordType: 'expense'|'revenue'|'purchase'`) استُبدل بعمود **"رقم الأرشفة"** محسوب حيًا من الأرشفة الأسبوعية للقيود (`journalEntryId` → `JournalEntries.weeklyBatchId` → `WeeklyBatches.batchNumber`، انظر صف `WeeklyBatches` أدناه) — قرار عمل صريح لأن الأرشفة الورقية الفعلية تتم برقم ملف واحد يغطي عدة سجلات معًا لا بمرفق رقمي لكل سجل على حدة، وهذا الرقم موجود أصلاً وتلقائيًا في نظام الأرشفة الأسبوعية بلا حاجة لإدخال يدوي مستقل؛ أي مرفقات قديمة بهذه الأنواع الثلاثة (مُنشأة قبل هذا التغيير) تبقى محفوظة في مخزن `Attachments` لكن بلا أي واجهة حالية لعرضها.
- `export-utils.js` — `exportRowsToExcel(filename, columns, rows)` (يتطلب تحميل مكتبة `xlsx` من CDN **قبل** هذا الملف) و`exportRowsToPdf(title, columns, rows)` (**تنزيل ملف PDF فعلي مباشرة** بضغطة واحدة، بلا أي حوار وسيط). `columns` بنفس شكل أعمدة `renderDataTable` (`{key, label}`)، و`rows` يجب أن تكون نفس الصفوف المعروضة فعليًا في الجدول. كل صفحة تحتفظ بآخر صفوف/أعمدة معروضة في متغيّرات page-level (مثل `_herdExportRows`، أو `_currentExportColumns`/`_currentExportRows` في صفحات التقارير متعددة التبويبات/الأوضاع حيث تُحدَّث هذه المتغيرات عند كل تبديل تبويب — وتُضبط `null` في تبويبات بلا جدول مثل "إحصائيات"/"حسب النوع" في `herd-reports.html`، فيعرض زرّا التصدير تحذيرًا عبر `showToast` بدل التصدير). **`exportSectionsToExcel(filename, sections)`/`exportSectionsToPdf(title, sections)`** (كل `section: {title, columns, rows}`) نسخة متعددة الأقسام لتصدير أكثر من جدول في نفس الملف بضغطة واحدة — Excel: كل قسم ورقة (sheet) منفصلة باسمه؛ PDF: كل قسم بعنوان فرعي خاص به قبل جدوله ضمن نفس المستند المتسلسل. `exportRowsToExcel`/`exportRowsToPdf` أصبحتا الآن مجرد غلاف رقيق يستدعي النسخة متعددة الأقسام بقسم واحد (`title: null` لـ PDF يحافظ على نفس المخرجات الأصلية بالضبط، بلا عنوان فرعي) — **لا تغيير في سلوك أي صفحة موجودة** تستخدم النسختين المفردتين. مستخدم حاليًا في: `herd-list.html`, `births.html`, `deaths.html` (القطيع)، `expense-list.html`, `revenue-list.html` (المصروفات/الإيرادات)، `custody-list.html` و`employee-statement.html` (سجل/كشف حساب العهد)، `bulk-purchases.html`, `bulk-sales.html`، `inventory-items.html`, `inventory-movements.html` (المخزون)، و`herd-reports.html`, `revenue-reports.html`, `expense-reports.html`, `purchase-reports.html`, `party-reports.html`, `overview-report.html`, `custody-reports.html` (التقارير، بما فيها وضع المقارنة بين فترتين)، و`inventory-reports.html` **وحدها تستخدم `exportSectionsToExcel`/`exportSectionsToPdf`** (قسمان: "الرصيد لكل صنف" + "سجل الحركات" في ملف واحد).
  - ⚠️ **آلية `exportSectionsToPdf`/`exportRowsToPdf` (تُنزِّل مباشرة، بلا حوار طباعة)**: كل صفحة `.report-page` (نفس بنية HTML/CSS القديمة لمعاينة الطباعة) تُرسم مؤقتًا **خارج الشاشة داخل نفس مستند الصفحة الحالية** (`position:fixed; left:-100000px`، لا نافذة/تبويب/iframe منفصل — يرث بذلك خط Cairo وكل التنسيق العام لحظيًا بلا أي انتظار تحميل خط إضافي، بعكس نافذة الطباعة المعزولة القديمة التي لم تكن تحمّل Cairo أصلاً) عبر مكتبة `html2canvas` (تصوير الـ HTML المُنسَّق فعليًا كما يظهر بالضبط، بما فيه العربي RTL)، ثم تُلصَق كل لقطة (JPEG) كصفحة مستقلة بأبعادها الفعلية المقاسة في مستند `jsPDF` واحد يُنزَّل عبر `pdf.save()` (تنزيل Blob حقيقي، لا `window.print()`). هذا "تصوير" وليس نصًا حقيقيًا مُضمَّنًا بخط jsPDF — تفاديًا لمشاكل تشكيل/اتجاه الخط العربي في مكتبات PDF النصية (كان هذا سبب اختيار حوار الطباعة أصلاً؛ نفس المشكلة تجاوزناها هنا بالتصوير بدل التضمين النصي)، على حساب حجم ملف أكبر قليلاً (صور JPEG) بدل نص متجهي — مقايضة مقصودة. **يتطلب تحميل `html2canvas@1.4.1` و`jspdf@2.5.2` (UMD، `window.jspdf.jsPDF`) من jsDelivr قبل `export-utils.js` في كل صفحة تحمّله** — أُضيفا فعليًا لكل الصفحات الـ42 التي كانت تحمّل `export-utils.js` وقت هذا التغيير؛ أي صفحة جديدة تستخدم التصدير مستقبلاً يجب أن تحمّلهما بنفس الترتيب. الدالتان الآن `async` (تُعيدان Promise) لكن كل نقاط الاستدعاء الحالية (~40 موضعًا) تناديها بلا `await` من مستمعي أحداث بسطاء — آمن لأن كل الأخطاء (فشل تحميل المكتبتين/فشل الرسم) مُغلَّفة داخليًا بـ`try/catch` وتُعرض كـ`showToast` خطأ بدل رفض Promise غير مُعالَج للمستدعي.
- `quick-action-fab.js` — زرّ دائري عائم أخضر ثابت أسفل يسار الشاشة (`renderQuickActionFab()`، تُستدعى تلقائيًا من آخر `renderHeader()` — أي صفحة تحمّل هذا الملف وتستدعي `renderHeader()` تحصل عليه مجانًا) يفتح قائمة اختصارات (`QUICK_ACTIONS`) تنتقل مباشرة لنموذج تسجيل شائع (مصروف/إيراد/شراء حيوان/عهدة/أصل...) بدل المرور بصفحة القائمة أولاً. كل عنصر يُفلتَر بـ`hasModuleAccess(moduleKey)`؛ عنصر "إضافة مستخدم" الوحيد يحمل أيضًا `systemAdminOnly: true` (يتحقق صراحة من `resolveRoleKey(role) === 'systemAdmin'` في الفلترة، بما أن `moduleKey: 'settings'` وحده لا يكفي — أي مستخدم بصلاحية "الإعدادات العامة" يملك وصول الوحدة لكن قسم المستخدمين نفسه مقصور على مدير النظام) ورابطه يحمل `?openAddUser=1` تقرأها `settings-page.js` لتفتح نافذة "إضافة مستخدم" تلقائيًا. ⚠️ **حساسية CSS مهمة (`.quick-fab` في `components.css`)**: القائمة (`.quick-fab__menu`) تُخفى بـ`opacity:0`+`pointer-events:none` (لا `display:none`، حفاظًا على أنيميشن الفتح) فتبقى محتفظة بارتفاعها الطبيعي داخل عمود `flex-direction:column` حتى وهي مغلقة — **الحاوية الخارجية `.quick-fab` نفسها يجب أن تحمل `pointer-events:none`** (والزر المستدير `.quick-fab__btn` وحده `pointer-events:auto` دومًا، وعناصر القائمة `pointer-events:auto` فقط تحت `.quick-fab--open`)، وإلا فإن الحاوية بارتفاعها الوهمي الممتد لأعلى الشاشة (يكبر كل ما زاد عدد عناصر `QUICK_ACTIONS`) تعترض بصمت أي ضغطة تقع فوقها من عناصر صفحة أخرى تحتها تمامًا بلا أي أثر بصري — كان هذا يمنع فعليًا الضغط على أزرار "+" في عدة صفحات (تكويد موظف، إضافة مستخدم، ...) بلا أي خطأ ظاهر في الـ Console، وسبب ارتباك حقيقي قبل اكتشافه.

## نموذج البيانات (شكل كل مخزن)
> IndexedDB بلا مخطط ثابت (schemaless) — هذه الحقول اتفاقية استخدام فعلي مُستنتجة من `seed.js`/الخدمات/صفحات النماذج، وليست قيدًا مفروضًا من الكود.

| المخزن | الحقول |
|---|---|
| `Users` | `username, passwordHash, role ('systemAdmin'\|'farmManager'\|'custom' — انظر SYSTEM_ROLES في auth-service.js)، fullName, phone, permissions?: string[] \| {[module]: true \| {add,edit,delete,approve,print,export}}, status?: 'active'\|'disabled'\|'deleted', twoFactorEnabled?: boolean, twoFactorSecret?: string, twoFactorRecoveryCodeHashes?: string[]` — `twoFactorRecoveryCodeHashes` (اختياري، مصفوفة هاش SHA-256 لكل رمز احتياطي غير مستهلَك — انظر `generateRecoveryCodes`/`hashRecoveryCode`/`matchRecoveryCode` في `totp-service.js`): رموز استخدام واحد (نمط `XXXXX-XXXXX`) تُنشأ لحظة تفعيل/ربط 2FA (أو عند طلب "توليد رموز جديدة" — `_regenerateRecoveryCodes` في `settings-page.js`) وتُعرض نصًا صريحًا **مرة واحدة فقط** في نافذة منفصلة (`_showRecoveryCodesModal`، بزر نسخ) — لا سبيل لعرضها ثانية بعد الإغلاق لأنها تُخزَّن مُجزّأة فقط، تمامًا مثل `passwordHash`. الحل الوحيد لموقف "فقدت الجوال ولا يوجد مدير نظام آخر يقدر يوقف 2FA بدلاً منك" (خصوصًا حساب `systemAdmin` الوحيد في التطبيق) — بلا هذه الرموز يبقى الحساب مقفولاً تمامًا. `completeTwoFactorLogin()` في `auth-service.js` تجرّب الكود كـTOTP عاديًا أولاً، فإن فشل تطابقه مقابل `twoFactorRecoveryCodeHashes` (عبر `matchRecoveryCode`)؛ عند التطابق يُستهلك الرمز فورًا (يُحذف من المصفوفة) فلا يصلح لمرة ثانية. خطوة الكود بصفحة الدخول تقبل الشكلين في نفس الحقل (6 أرقام أو رمز احتياطي) بلا تمييز واجهة، ورابط "فقدت الوصول لتطبيق المصادقة؟" (`twofa-lost-device-link` في `login-page.js`) يذكّر المستخدم بتجربة رمز احتياطي أولاً قبل التواصل مع مدير النظام. `twoFactorEnabled`/`twoFactorSecret` (انظر `js/services/totp-service.js`): تحقق بخطوتين اختياري عبر تطبيق مصادقة على الموبايل (TOTP، RFC 6238 — لا SMS/إيميل، التطبيق بلا خادم أصلاً فلا مجال لإرسال كود فعليًا). `twoFactorSecret` سرّ Base32 يُخزَّن **نصًا صريحًا غير مشفّر** (نفس مستوى أمان `passwordHash` بلا salt المُقرّ به أعلاه)؛ غيابه/`twoFactorEnabled` غير `true` = 2FA معطّل ضمنيًا (توافق خلفي كامل مع كل حسابات ما قبل هذه الميزة). إدارة ذاتية لكل مستخدم لحسابه هو (بطاقة "🔐 التحقق بخطوتين" في `settings/settings.html`، متاحة لأي مستخدم مسجّل دخوله لا لمدير النظام فقط): توليد سرّ جديد + عرضه كـ QR Code (مكتبة `qrcodejs`، نفس مكتبة `animal-card-print.js`) + مفتاح نصي يدوي، تفعيل فعلي فقط بعد إدخال كود صحيح من التطبيق للتأكيد (`_openEnableTwoFactorModal` في `settings-page.js`) — يمنع قفل الحساب بسرّ لم يُمسح فعليًا. الدالة الفعلية عامة (`_openLinkTwoFactorModal(targetUser, onLinked)`) تخدم أيضًا مدير النظام: زر "🔐 ربط بالمصادقة" لكل صف في جدول "إدارة المستخدمين" (`_linkUserTwoFactor` — يظهر فقط لمستخدم لم يربط بعد؛ يتحوّل لزر "إيقاف 2FA" بعد الربط) يفتح نفس نافذة QR/التأكيد لحساب أي مستخدم آخر مباشرة، بما فيها مستخدم أُضيف لتوّه — مفيد لربط الحساب فورًا بموبايل صاحبه لحظة إنشائه بدل تركه له لاحقًا؛ عمود "التحقق بخطوتين" (badge) في نفس الجدول يعرض حالة كل مستخدم لحظيًا. `login()` في `auth-service.js` تتوقف عند كلمة المرور الصحيحة وتُعيد `requiresTwoFactor:true` بلا إنشاء جلسة إن كان `twoFactorEnabled`؛ صفحة الدخول (`index.html`/`login-page.js`) تنتقل لخطوة ثانية (كود 6 أرقام) تستدعي `completeTwoFactorLogin(userId, token)` (تتحقق عبر `verifyTotpCode()` بهامش تسامح ±30 ثانية لفرق الساعة) لإنشاء الجلسة فعليًا. إنقاذ حساب فقد صاحبه موبايله: لا استرجاع ذاتي ممكن (السرّ محفوظ فقط داخل تطبيق المصادقة المفقود) — رابط "فقدت جهازك أو تطبيق المصادقة لا يعمل؟" في خطوة الكود بصفحة الدخول (`twofa-lost-device-link`) يعرض رسالة توجّه المستخدم للتواصل مع مدير النظام، الذي يملك زر "إيقاف 2FA" لكل مستخدم آخر في جدول "إدارة المستخدمين" (`_resetUserTwoFactor`) يعطّله تمامًا بلا استرجاع للسرّ القديم؛ يدخل المستخدم بعدها بكلمة المرور فقط ويربط جهازًا جديدًا من جديد (ذاتيًا من بطاقة "🔐 التحقق بخطوتين"، أو مباشرة من مدير النظام عبر زر "🔐 ربط بالمصادقة" لنفس السطر). نوع اتحادي (union) لـ`permissions`، انظر شرح `hasActionPermission` أعلاه للتفاصيل الكاملة: غائب = وصول كامل؛ مصفوفة نصوص قديمة = وصول كامل لكل الأزرار في كل وحدة مذكورة (كل مصفوفة نصية تمثّل **وحدات** مسموحة، لا مفاتيح `SIDEBAR_LINKS` الفردية؛ مفتاح `module` لكل رابط، أو `key` نفسه إن غاب `module`)؛ كائن جديد = صلاحيات دقيقة لكل زر لكل وحدة. لدور `'systemAdmin'` الحقل غير مؤثر لأن `hasModuleAccess()`/`hasActionPermission()` في `auth-service.js` يمنحانه وصولاً كاملاً دومًا بصرف النظر عن قيمته. `status` (غياب = `'active'` ضمنيًا): `'disabled'` يمنع الدخول برسالة صريحة "هذا الحساب معطّل" دون فقدان أي بيانات/صلاحيات (`login()` في `auth-service.js`)، `'deleted'` حذف ناعم عادي (`dbSoftDelete`، نفس اتفاقية باقي المخازن) يُعامَل كمستخدم غير موجود تمامًا عند تسجيل الدخول ولا يظهر بقائمة المستخدمين. كلاهما يُدار بزرّي "تعطيل/تفعيل" و"حذف" لكل صف في جدول المستخدمين (`settings-page.js`: `_toggleUserStatus`/`_deleteUser`) — مخفيان لصف المستخدم الحالي نفسه (لا يمكن تعطيل/حذف النفس)، ومحكومان بحارس يمنع تعطيل/حذف آخر `systemAdmin` نشط في التطبيق (`_activeSystemAdminCount`) تفاديًا لقفل كامل للنظام. تُدار الإضافة/التعديل من قسم "إدارة المستخدمين والصلاحيات" في `settings/settings.html`، ولا يظهر إلا لمستخدم `resolveRoleKey(role) === 'systemAdmin'` |
| `Animals` | `code ('SH-0001'… للأغنام / 'GO-0001'… للماعز — تلقائي عبر generateNextAnimalCode(type)، البادئة القديمة 'GZ-' للأكواد السابقة تبقى كما هي والعدّاد مستقل لكل بادئة), type ('sheep'\|'goat'), breed, gender ('male'\|'female'), birthDate (تاريخ الميلاد أو الاقتناء — نفس الحقل للحالتين), color, weight, healthStatus ('healthy'\|'sick'\|'underTreatment'\|'quarantine'), source ('born'\|'purchased'), purchasePrice, status ('alive'\|'dead'\|'sold'), notes, motherId?` — لا يوجد حقل عمر مخزَّن؛ `herd-list-page.js` يحسبه ديناميكيًا (`calculateAgeLabel` في `formatters.js`) من `birthDate` حتى اليوم، أو حتى `deathDate`/تاريخ إيراد البيع المرتبط عند `status` = `'dead'`/`'sold'` |
| `MatingRecords` | `animalId (أنثى), date, method ('natural'\|'ai'), maleInfo? (نص حر: كود الفحل أو مصدر التلقيح), notes` — سجل تلقيح مرجعي بسيط عبر `herd/mating.html`/`mating-service.js`. **لا ربط صريح** بـ`PregnancyRecords` (لا `matingRecordId`) ولا يُغذّي أي تنبيه تلقائي بذاته — تخمين بسيط بحكم عدم إثمار كل تلقيح حملاً مؤكدًا |
| `PregnancyRecords` | `animalId (أنثى), confirmedDate, expectedBirthDate, status ('pregnant'\|'delivered'\|'lost', افتراضي 'pregnant' عند الإنشاء), actualBirthDate?, notes` — عبر `herd/pregnancy.html`/`pregnancy-service.js`. `expectedBirthDate` يُقترَح تلقائيًا (+150 يومًا تقريبًا من `confirmedDate`، `PREGNANCY_GESTATION_DAYS`) وقابل للتعديل الكامل. أي سجل `status: 'pregnant'` يقترب `expectedBirthDate` منه خلال 14 يومًا (أو تجاوزه) يظهر في تنبيه "قرب الولادة" (انظر قسم "التنبيهات" أدناه) **و** يُنشئ إشعار `type: 'pregnancy'` عند زيارة `pregnancy.html` (نفس مبدأ `Vaccinations.nextDueDate`). يتحوّل تلقائيًا لـ`'delivered'` عبر `resolvePregnancyOnBirth()` (تُستدعى من `births-page.js` بعد كل `createBirthRecord` ناجح لنفس الأم) حتى لا يبقى تنبيه "قرب الولادة" قائمًا بصمت بعد وقوع الولادة فعليًا — تخمين بأحدث سجل `'pregnant'` لنفس الأم (لا ربط صريح بسجل ولادة بعينه) |
| `BirthRecords` | `motherId, birthDate, offspringCount, offspringDetails: [{gender, weight, animalId?, animalCode?}], veterinarianNotes` — `animalId`/`animalCode` تُملأ فقط عند تفعيل خيار "تكويد كل مولود تلقائيًا" في `births.html`؛ تبقى `null` لأي مولود لم يُسجَّل كحيوان |
| `DeathRecords` | `animalId, deathDate, cause ('disease'\|'accident'\|'old_age'\|'unknown'), details` — تُكتب من `herd/deaths.html` (`deaths-page.js`)، وتُحدّث تلقائيًا `Animals.status` إلى `'dead'` عبر `createDeathRecord` |
| `HealthRecords` | `animalId, date, diagnosis, treatment, veterinarianName, cost, resultingHealthStatus?` — سجل تاريخي لزيارات/حالات صحية لكل حيوان، يُدار من `herd/health.html` عبر `health-service.js`. `resultingHealthStatus` (اختياري، من نفس قيم `Animals.healthStatus` بما فيها `'healthy'` لتوثيق الشفاء) إذا حُدِّد يُحدِّث `Animals.healthStatus` لذلك الحيوان تلقائيًا. لا علاقة مباشرة بين هذا المخزن وحقل `Animals.healthStatus` نفسه (لا مرجع/مفتاح أجنبي حقيقي)، لكن التحديث ليس مقصورًا على الإنشاء: `updateHealthRecord()` يعيد حساب `healthStatus` الحالي للحيوان أيضًا، لكن **فقط** عند تعديل `resultingHealthStatus` **لأحدث سجل صحي لهذا الحيوان تحديدًا** (بالمقارنة بالتاريخ `date`) ولحيوان ما زال `status === 'alive'` — تعديل سجل أقدم من الأحدث، أو تعديل سجل لحيوان نافق/مباع، لا يغيّر `Animals.healthStatus` بصمت. `herd/health.html` نفسه يمنع اختيار حيوان `status !== 'alive'` من قائمة "الحيوان" عند تسجيل زيارة **جديدة** (الحيوان يختفي من القائمة المنسدلة) — تعديل/حذف سجلات قديمة لحيوان نافق/مباع يبقى ممكنًا لأغراض الأرشفة، فقط بلا أثر رجعي على حالته الصحية |
| `FatteningBatches` | `batchNumber ('FTN-0001'…، تلقائي عبر generateNextFatteningBatchCode()), startDate, endDate? (null = دفعة نشطة), notes, locationId? (يشير لسجل `Locations` — "الحظيرة"، اختياري)، allocationMethod ('days'\|'equal'\|'weight'، غياب = 'days' ضمنيًا للدفعات القديمة قبل هذه الميزة), status ('active'\|'closed'), animals: [{animalId, entryDate, entryWeight?, exitDate?, exitWeight?}]` — "سجل التسمين" (`fattening-service.js`, `herd/fattening.html`): دفعة تسمين جماعية (مجموعة حيوانات تتغذى/تُعالَج مع بعض بنفس الفترة، وليس تسجيلاً فرديًا لكل حيوان — قرار عمل صريح)، بتسلسل هرمي **الحظيرة ← الدفعة ← الحيوان**: `addAnimalToBatch()` ينقل تلقائيًا `Animals.locationId` لنفس حظيرة الدفعة (إن كانت محددة) عند إضافة حيوان لها، فيبقى موقع الحيوان متسقًا مع دفعته بلا إدخال يدوي مزدوج. لا تُسجَّل أي تكلفة من صفحة الدفعة نفسها؛ التكلفة الجماعية تتجمّع من مصدرين معًا: (1) أي مصروف عادي (علف، أدوية، فيتامينات، أو أي تكلفة أخرى خاصة بالدفعة كلها) بحقل `Expenses.linkedFatteningBatchId` مضبوطًا على هذه الدفعة، و(2) أي حركة صرف مباشر من مخزون موجود أصلاً (`InventoryMovements.linkedFatteningBatchId`، اتجاه `'out'` فقط — انظر صف `InventoryMovements` أدناه) — الاثنان مستقلان تمامًا ويمكن استخدامهما معًا لنفس الدفعة، بلا تمييز نوع المصروف/سبب الحركة. **كل الحسابات حيّة وليست مخزَّنة** عبر `computeFatteningBatchResults(batch, {expenses, weightRecords, animals, revenues, healthRecords, inventoryMovements, deathRecords})` (`deathRecords?` اختياري — `DeathRecords` كاملة؛ لو الحيوان نفق وهو لسه بالدفعة بلا `exitDate` صريح، "أيام التغذية"/نصيبه من التكلفة الجماعية تتوقف عند `DeathRecords.deathDate` بدل الاستمرار حتى إغلاق الدفعة أو اليوم — انظر `reports/sales-report.html` لعرض خسارة النفوق الكاملة الناتجة): التكلفة المشتركة الإجمالية = مجموع `Expenses.amount` المرتبطة + مجموع (`quantity × unitCost`) لحركات المخزون المرتبطة (`computeBatchLinkedCost()`)، تُوزَّع على كل حيوان حسب `batch.allocationMethod` — **`'days'`** (الافتراضي): نصيب كل حيوان من إجمالي "أيام التغذية" لكل حيوانات الدفعة (حيوان دخل متأخرًا أو خرج مبكرًا يأخذ نصيبًا أقل)؛ **`'equal'`**: بالتساوي على كل حيوان بصرف النظر عن مدة تواجده؛ **`'weight'`**: حسب نصيب كل حيوان من إجمالي متوسط وزنه (متوسط وزن الدخول/الخروج — حيوان بلا أي بيانات وزن basis=0 فلا يأخذ نصيبًا من التكلفة الجماعية، يبقى فقط بتكلفته الصحية الفردية إن وُجدت). وزن الدخول/الخروج لكل حيوان يُؤخذ من `entryWeight`/`exitWeight` المُدخَلين يدويًا عند الإضافة/الإخراج، وإلا من أقرب سجل `WeightRecords` سابق لتاريخه، وإلا من `Animals.weight` الحالي؛ **بالإضافة** لتكلفة فردية مباشرة منفصلة (`individualCost` — لا مشتركة بين الدفعة ولا تتأثر بطريقة التوزيع، ضمن فترة تواجده بالدفعة فقط) من مصدرين معًا: `HealthRecords.cost` (`healthCost`، علاج/تحصين خاص بحيوان بعينه) + `Expenses.linkedAnimalId` (`directExpenseCost`، أي مصروف آخر رُبط مباشرة بهذا الحيوان — انظر `getAnimalLinkedExpensesTotal()` في صف `Expenses` أعلاه)؛ سعر البيع من `Revenues.animalId` المطابق (إن بيع الحيوان فعليًا) لحساب الربح الصافي وتكلفة الكيلو الواحد المُكتسَب (على إجمالي التكلفة المشتركة الموزَّعة + الفردية معًا). `updateFatteningBatchDetails()` يعدّل الحظيرة/الملاحظات/طريقة التوزيع لاحقًا بلا مساس بقائمة الحيوانات أو الحالة. `closeFatteningBatch()` يضبط `status:'closed'`+`endDate` ويُخرج تلقائيًا أي حيوان متبقٍ بلا `exitDate` بنفس التاريخ (بلا وزن خروج — يُحسب عند العرض من آخر وزن معروف)؛ `reopenFatteningBatch()` يعكسها. لا أثر محاسبي مباشر لهذا المخزن نفسه (التكلفة محاسَبة أصلاً عبر `Expenses`/`InventoryMovements`/`HealthRecords` العاديين) — دور الدفعة تحليلي/إداري بحت لتوزيع تكلفة موجودة أصلاً، وليس مصدر ترحيل جديد |
| `Expenses` | `category (نص = اسم حساب البند وقت الحفظ), categoryAccountId? (يشير لسجل `ChartOfAccounts` — انظر ملاحظة "البند = حساب" أدناه), amount, date, hasTaxInvoice (boolean), amountBeforeTax, taxAmount, paymentMethod ('cash'\|'transfer'\|'cheque'), vendor, status ('approved'\|'pending'), notes, linkedInventoryItemId?, linkedInventoryDirection? ('in'\|'out'، غياب = 'in' ضمنيًا للسجلات قبل هذه الميزة), linkedQuantity?, linkedFatteningBatchId?, linkedAnimalId?` — `linkedInventoryItemId?`/`linkedInventoryDirection?`/`linkedQuantity?` (اختيارية، من قسم "ربط بصنف مخزون" في `expense-form.html`) تُنشئ تلقائيًا حركة مخزون مطابقة في `InventoryMovements` عند الحفظ (`_syncExpenseInventoryLink()` في `expense-form-page.js`، نفس مبدأ ربط `animalId` بالإيراد في `revenue-form-page.js`): `'in'` (الافتراضي، `reasonType:'purchase'`) لمصروف شراء يزيد رصيد المخزون (مثل شراء أعلاف/أدوية بمصروف نقدي مباشر بدل المرور بوحدة المشتريات)، أو `'out'` (`reasonType:'consumption'`) لتسجيل **استهلاك** كمية من رصيد موجود بالفعل كمصروف (مثل "تكلفة أعلاف مستهلكة هذا الشهر") — في الحالتين القيد المحاسبي للمصروف نفسه لا يتغيّر (مدين حساب البند / دائن نقد-بنك-ذمم حسب `paymentMethod` كالمعتاد)؛ حركة المخزون إضافة كمية فقط، لا أثر محاسبي إضافي لها بذاتها. اختيار `'out'` بكمية أكبر من الرصيد الحالي **لا يُمنع** لكنه يعرض `window.confirm` تحذيريًا قبل الحفظ (نفس فلسفة "بلا تحقق آلي مانع" المتّبعة في بيع الجملة الحر). تكلفة الوحدة `unitCost` في الحركة الناتجة إعلامية فقط، محسوبة من `amount / linkedQuantity`. تعديل/حذف المصروف يزامن (يحدّث/يحذف) الحركة المرتبطة تلقائيًا عبر `getInventoryMovementByExpenseId()`. `linkedFatteningBatchId?` (اختياري، مستقل تمامًا عن `linkedInventoryItemId?` ويمكن استخدام الاثنين معًا لنفس المصروف — قسم "ربط بدفعة تسمين" في `expense-form.html`، لأي مصروف خاص بالدفعة كلها: علف/أدوية/فيتامينات/غيرها) مجرد "وسم" بلا أي أثر تلقائي آخر (لا حركة مخزون/قيد إضافي): يُستخدم فقط لتجميع التكلفة المشتركة لكل `FatteningBatches` عبر `computeBatchLinkedCost()`/`computeFatteningBatchResults()` في `fattening-service.js` — انظر صف `FatteningBatches` أعلاه. `linkedAnimalId?` (اختياري، مستقل تمامًا عن `linkedFatteningBatchId?`/`linkedInventoryItemId?` — قسم "ربط بحيوان" في `expense-form.html`، قائمة اختيار من الحيوانات الحية فقط عند إنشاء سجل جديد، مثل حقل "الحيوان" في `health.html`) وسم "تكلفة فردية مباشرة" على حيوان بعينه بلا أي أثر تلقائي آخر (لا حركة مخزون/قيد إضافي — نفس مبدأ `linkedFatteningBatchId?` تمامًا لكن على مستوى الحيوان الواحد بدل الدفعة كلها؛ **لا يُوزَّع أبدًا على أي حيوان آخر**): يُقرأ عبر `getAnimalLinkedExpensesTotal(animalId, expenses, {from?, to?})` في `expense-service.js` — تُجمع مع `HealthRecords.cost` لتكوين "التكلفة الفردية المباشرة" الكاملة للحيوان في `computeFatteningBatchResults()` (`fattening-service.js`، ضمن فترة تواجده بالدفعة إن كان ضمن دفعة تسمين) و`sales-report-page.js` (قسم "تكلفة الرأس"، بلا حصر بفترة — انظر صفوف `FatteningBatches` و`reports/sales-report.html` أدناه). ⚠️ **لا حقل `archiveNumber` مُخزَّن على السجل نفسه** — عمود "رقم الأرشفة" في `expense-list.html` (وكذلك `revenue-list.html`/`purchase-list.html`) محسوب حيًا بالكامل: `Expenses.journalEntryId` → `JournalEntries.weeklyBatchId` (إن كان القيد ضمن أرشفة أسبوعية سابقة) → `WeeklyBatches.batchNumber`؛ '-' لسجل قيده غير مُرحَّل أو غير مُؤرشف أسبوعيًا بعد (انظر صف `WeeklyBatches` أدناه). لا واجهة لإدخاله يدويًا؛ استبدل زر "📎 مرفقات" الرقمي القديم لهذا المخزن تحديدًا (انظر ملاحظة `attachment-manager.js` أعلاه) |
| `Revenues` | `category (نص = اسم حساب البند وقت الحفظ), categoryAccountId?, amount, date, hasTaxInvoice (boolean), amountBeforeTax, taxAmount, receiveMethod ('cash'\|'transfer'\|'credit'), client, notes, animalId?, partyId?` — اختيار أي حساب من حسابات "إيرادات بيع الحيوانات" (الأوراق تحت فرعي `411`/`412`، الفحص عبر `isAnimalSaleAccount()` في `accounting-service.js` — بعد الشجرة الهرمية حل محل الفحص القديم بالكود الثابت `4000`) في `revenue-form.html` يُظهر حقل اختيار `animalId` ويضبط تلقائيًا `Animals.status = 'sold'` لذلك الحيوان عند الحفظ (ويُعاد إلى `'alive'` عند حذف الإيراد أو تغيير الحيوان المختار). قراءات لاحقة (`herd-list-page.js`, `sales-report-page.js`) تكتفي بفحص `animalId` (موجود حصرًا على إيرادات بيع حيوان فردي) بدل مطابقة أي نص/حساب. عمود "رقم الأرشفة" في `revenue-list.html` محسوب حيًا بنفس آلية `Expenses` أعلاه (`Revenues.journalEntryId` → `JournalEntries.weeklyBatchId` → `WeeklyBatches.batchNumber`) |
| `Purchases` | `category (نص = اسم حساب البند وقت الحفظ), categoryAccountId?, amount, date, hasTaxInvoice (boolean), amountBeforeTax, taxAmount, paymentMethod ('cash'\|'transfer'\|'cheque'\|'credit'), vendor, status ('approved'\|'pending'), notes, partyId?, linkedInventoryItemId?, linkedQuantity?` — نفس شكل `Expenses` تمامًا هيكليًا، لكن مصدر بند مختلف تمامًا (⚠️ قرار عمل بتاريخ لاحق، جُرِّب بترتيب: أولاً فرع "13 الأصول الثابتة" فقط، ثم **كل** حسابات `type:'asset'` بلا تقييد، ثم استقر نهائيًا على ما يلي): بند `Expenses` حساب من النوع `expense`، بينما بند `Purchases` مقصور على أوراق فرع **"113 المخزون"** فقط تحديدًا (`getPurchaseCategoryAccounts()` في `accounting-service.js` — مخزون الأعلاف 11301/الأدوية والمستلزمات البيطرية 11302/مواد ومستلزمات المزرعة 11303 وأوراقها) — لا صندوق/بنك/ذمم/مخزون حيواني/أصول ثابتة. أي مشترى يُرسمَل مباشرة مدينًا لحساب مخزون مباشرة (رسملة كأصل مخزون) بدل حساب مصروف. `linkedInventoryItemId?`/`linkedQuantity?` (اختياريان، من قسم "ربط بصنف مخزون" في `purchase-form.html` — أُضيفا لاحقًا ليربطا هذا الترسيم المحاسبي بوحدة "المخزون والمستلزمات" التشغيلية فعليًا، انظر أدناه) يُنشئان تلقائيًا حركة "وارد" (`reasonType:'purchase'`) مطابقة في `InventoryMovements` عند الحفظ عبر `_syncPurchaseInventoryLink()` في `purchase-form-page.js` — نسخة طبق الأصل من `_syncExpenseInventoryLink()` (`relatedPurchaseId` بدل `relatedExpenseId`، اتجاه `'in'` ثابت دومًا لأن المشترى شراء لا استهلاك). تعديل/حذف المشترى يزامن (يحدّث/يحذف) الحركة المرتبطة تلقائيًا عبر `getInventoryMovementByPurchaseId()`. سجلات قديمة قبل هذا التغيير (بند = حساب مصروف، أو بلا ربط مخزون) تبقى تعمل بلا مشاكل (`syncPurchaseJournalEntry` يعتمد `categoryAccountId` المحفوظ وقت الحفظ بصرف النظر عن نوعه الحالي؛ خط الدفاع الثاني بمطابقة الاسم يبقى مقصورًا على `type:'expense'` لتوافق تلك السجلات القديمة تحديدًا). ⚠️ **أثر على "صافي الربح" (مُصحَّح بالتزامن مع هذا التغيير، بقرار صريح من صاحب المشروع)**: الصيغة القديمة (`صافي الربح = إيرادات + مبيعات جملة − مصروفات − مشتريات − تكلفة البضاعة المباعة (جملة)`) كانت تطرح `Purchases.amount` بالكامل من صافي الربح بافتراض أنه دومًا تكلفة تشغيلية — لم يعد هذا الافتراض صحيحًا بعد أن صارت المشتريات تُرسمَل محاسبيًا كأصول (رسملة، لا تحميل فوري على قائمة الدخل)، تمامًا مثل مشتريات الجملة (`BulkPurchaseBatches`) الموضحة أدناه. الصيغة الحالية في `dashboard-page.js`/`overview-report-page.js` صارت: `صافي الربح = إيرادات + مبيعات جملة − مصروفات − تكلفة البضاعة المباعة (جملة)` — `Purchases`/`BulkPurchaseBatches` كلاهما مستبعدان الآن (كلاهما رسملة أصل)، ويبقى إجمالي المشتريات معروضًا كبطاقة/عمود **معلوماتي فقط** (KPI "إجمالي المشتريات") بلا تأثير على صافي الربح. ⚠️ **أثر على "ربط العمليات بالحسابات"**: تكويد `catInventory/catPayCash/catPayBank/catPayCredit:<categoryAccount.id>` التفصيلي لكل بند في `account-mappings.html` (انظر صف `AccountMappings` أدناه) مبني على قائمة `getCategoryAccounts('expense')` فقط — لم يعد يغطي بنود المشتريات الجديدة (حسابات مخزون فرع 113)، فتتدهور بأمان لمفاتيح `purchasePayCash/Bank/Credit` العامة (تخصيص عام لكل المشتريات بصرف النظر عن البند، لا لكل بند على حدة). عمود "رقم الأرشفة" في `purchase-list.html` محسوب حيًا بنفس آلية `Expenses` أعلاه |

**"البند = حساب" (`category`/`categoryAccountId`, مشتركة بين `Expenses`/`Revenues`/`Purchases`)**: لا يوجد مخزن "تكويدات" منفصل — كل بند هو حساب فعلي من `ChartOfAccounts` مباشرة (`type: 'expense'` لبنود المصروفات **و** المشتريات معًا — نفس مجمّع الحسابات لكليهما، `type: 'revenue'` لبنود الإيرادات). **الشجرة الآن هرمية (أب/فرعي)**: كل حساب يحمل `parentCode?` (كود الأب، null للجذور)، `financialStatement` (`'balanceSheet'` = المركز المالي / `'incomeStatement'` = قائمة الدخل)، و`isPostable` (قابل للترحيل: `false` على "الرؤوس" التجميعية، `true` على الأوراق) — الشجرة الكاملة معرّفة في `_NEW_CHART_ACCOUNTS` داخل `seed.js`. فقط حساب `isCategoryAccount !== false && isPostable !== false` (أي **ورقة** مصروف/إيراد غير تحكّم) يظهر في قائمة الاختيار بنماذج المصروفات/الإيرادات/المشتريات (`getCategoryAccounts(accountType)`/`getCategoryAccountNames(accountType)` في `accounting-service.js`؛ الأولى تُعيد كائنات الحساب كاملة للنماذج التي تحتاج `id`، والثانية أسماء فقط لفلاتر القوائم/التقارير). حسابات "تحكّم" (`isCategoryAccount: false`) — `49101`-`49103`/`49201` (مردودات وخصومات المبيعات)، `82101` (أرباح بيع أصل)، `51101`-`51104`/`52101`-`52104` (تكلفة المبيعات)، `75101`-`75104` (الإهلاك)، `81101`/`81104` (خسائر بيع أصل/فروقات جرد) — تُستخدم حصرًا من ترحيل تلقائي في وحدات أخرى (الجملة/مرتجعات الذمم/إعدام عهد/إهلاك الأصول الثابتة، انظر صفوف `BulkPurchaseBatches`/`BulkSaleBatches`/`PartyTransactions`/`CustodySettlements`/`FixedAssetTransactions`) ولا تظهر أبدًا في النماذج اليدوية؛ تُدار الخانة من `accounting/chart-of-accounts.html` (عرض شجري مُزاح حسب العمق + فلاتر نوع/قائمة مالية + نافذة إضافة/تعديل بحقول الكود/الاسم/النوع/**الحساب الأب** (رؤوس فقط، مع استبعاد الحساب نفسه وأحفاده عند التعديل)/**القائمة المالية**/**قابل للترحيل**/**الطبيعة**/يظهر كبند/ملاحظات — الحذف ممنوع لأي حساب مستخدم في قيود أو له أبناء أو مرتبط كـ`categoryAccountId`). قائمة الحساب في نموذج القيد اليدوي (`journal-entry-form-page.js`) مقصورة على `isPostable !== false` عبر `getPostableAccounts()` — لا يُرحَّل يدويًا على رأس تجميعي (الأتمتة وحدها ترحِّل على رؤوس بعينها عند الحاجة مثل `112`/`211`/`49`/`81`/`11301`-`11303`). استثناءات الطبيعة عن افتراضي النوع: مجمع الإهلاك `136*` **دائنة** رغم نوعه أصل، ومردودات وخصومات المبيعات `49*` ومسحوبات المالك `31103` **مدينة**. الحساب `3020 أرصدة افتتاحية (عملاء وموردون)` إضافة فوق شجرة المستخدم المقترحة (حساب موازنة قيد الرصيد الافتتاحي للأطراف، ابن الجذر `3`).. `categoryAccountId` يشير لسجل `ChartOfAccounts` وقت الحفظ؛ `category` نسخة نصية من اسم الحساب في تلك اللحظة (لعرض/تجميع سريع في الجداول والتقارير بلا استعلام إضافي — قد لا تعود تطابق اسم الحساب الحالي إن أُعيد تسميته لاحقًا، وهذا متوقَّع). ⚠️ **مخزن `Categories` القديم أُلغي بالكامل** (كان "بند" منفصل بلا حساب، يُربط اختياريًا بحساب عبر `linkedAccountId`؛ استُبدل بربط إجباري مباشر بالحساب نفسه — يزيل ثغرة "بند بلا ربط يقع صامتًا على حساب عام") — بقي اسمه فقط في `OBJECT_STORES`/النسخ الاحتياطي الشامل تفاديًا لكسر قواعد بيانات قديمة، بلا أي كود حالي يكتب/يقرأ منه (نفس مبدأ `Notifications` أدناه). `_migrateCategoryToAccountIdIfNeeded()` في `seed.js` تملأ `categoryAccountId` لمرة واحدة على كل سجل قديم بالاعتماد على بيانات `Categories` المحفوظة سابقًا إن وُجدت، وإلا بمطابقة اسم البند النصي القديم باسم حساب من نفس النوع؛ `syncExpenseJournalEntry`/`syncRevenueJournalEntry`/`syncPurchaseJournalEntry` (في `js/services/`) تعتمد `categoryAccountId` أولاً ثم نفس مطابقة الاسم كخط دفاع ثانٍ لسجل لم يُرحَّل بعد.

⚠️ **إعادة هيكلة شجرة الحسابات بتاريخ 2026-08-27 (بطلب صريح من صاحب المشروع)**: أُزيلت من `_NEW_CHART_ACCOUNTS` ثلاث مجموعات كاملة بأوراقها جميعًا — **"6 تكاليف الإنتاج والتربية"، "7 مصروفات التشغيل والإدارة" (شاملة فرع الإهلاك 75*)، "8 إيرادات ومصروفات أخرى"**. وأُضيف حساب **`3010 جاري الشريك`** (حقوق ملكية، ورقة قابلة للترحيل مباشرة تحت الجذر `3`). انعكاسات عملية يجب مراعاتها لأي تعديل لاحق:
- لم تبقَ بعد الإزالة أي ورقة `expense` تظهر بنموذج يدوي (`getCategoryAccounts('expense')` فارغة) — **قوائم "البند" في نموذجي المصروفات والمشتريات فارغة حتى يضيف صاحب المشروع بنوده بنفسه من شاشة شجرة الحسابات**؛ كل الأوراق اليدوية المتبقية `revenue` (41x/42x/43x) وورقتا "البند = حساب" الأصليتان لعهدة عينية والحسابات المخصصة إن أُنشئت.
- مفاتيح "ربط العمليات بالحسابات" كانت تشير افتراضيًا لحسابات أزيلت وباتت بلا هدف (كلها تظل strings فقط): `depExpense*` (كانت 75101-75104)، `assetGain`/`assetLoss` (82101/81101)، `cashShortage`/`cashOverage` (81104/82103)، `custodyWriteOff`/`custodyAdminWaiver` (81105)، `expenseFallback`/`purchaseFallback`/`custodyExpenseDefault` (74107)، `inventoryExpense*` (61101/62101/74107)، `purchaseReturn` (81). الخدمات المتأثرة تتدهور بأمان: ترحيل الإهلاك يتخطى الأصول (ويرمي رسالة إن لم يبقى شيء مستحق)، بيع/استبعاد الأصل وفروقات الصندوق وإعدام العهد ترمي رسائل خطأ عربية واضحة، مرتجعات/خصومات الموردين تُسجل السند بلا قيد (نفس الدفاعية `_postPartyJournal`)، وفروقات الجرد تتخطى الأصناف المتأثرة — حتى يختار المستخدم بدائل من شاشة الربط بعد إنشاء الحسابات.
- للقواعد الموجودة مسبقًا: `_removeRetiredSystemAccountsIfNeeded()` في `seed.js` (مستدعاة من `seedDatabaseIfEmpty()`) تحذف ناعمًا لمرة واحدة كل حساب بكود من `_RETIRED_SYSTEM_ACCOUNT_CODES` **غير مستخدم في أي قيد نشطة أو رابط `categoryAccountId`**؛ المستخدم منها فعليًا يبقى نشطًا كجذر مستقل (نفس اتفاقية الحسابات المخصصة) حفاظًا على سلامة أرصدة التقارير القديمة. إضافة `3010` نفسها تجعل شرط `_migrateChartOfAccountsIfNeeded` صحيحًا على القواعد القائمة فيُضاف تلقائيًا عبر `_normalizeNewChartAccounts()`.
- خريطة الترحيل القديمة `ACCOUNT_CODE_MIGRATIONS` وما دونها من خرائط دفاعية (`_LEGACY_CATEGORY_NAME_TO_ACCOUNT_CODE`) ما زالت تحمل أهدافًا من المجموعات المحذوفة (مثل '5000'→'61101') — لقواعد قديمة جدًا ستُتخطى هذه الصفوف بصمت (`if (!target …) continue`) ويبقى الحساب القديم جذرًا مستقلًا، وهذا سلوك مقصود ومقبول.

⚠️ **`paymentMethod`/`receiveMethod` قد تكون `'credit'` (آجل) وهذا يغيّر معنى القيد المحاسبي**: راجع صف `PartyTransactions` أدناه — عند `'credit'` يدين/يدائن القيد حساب ذمم (`112` للإيرادات، `211` للمشتريات) بدل الصندوق/البنك مباشرة، ويتطلب `partyId` إلزاميًا (يُفرَض في `revenue-form-page.js`/`purchase-form-page.js`). ⚠️ **`amount` قد يكون سالبًا**: سجلات المرتجعات/الخصومات (تُنشأ حصرًا من `createReturnOrDiscount()` في `party-service.js`، وليس من `revenue-form.html`/`purchase-form.html` اليدويتين) تُخزَّن بمبلغ سالب عمدًا حتى تُخصم تلقائيًا من `SUM(amount)` في كل تقارير/داشبورد المبيعات والمشتريات دون تعديلها؛ أي كود جديد يقرأ `Revenues`/`Purchases` ويفترض `amount > 0` دومًا سيُخطئ بوجود هذه السجلات (`category` تكون حينها `'مرتجعات وخصومات المبيعات'`/`'مرتجعات وخصومات المشتريات'` الثابتة).

**فاتورة ضريبية (`hasTaxInvoice`/`amountBeforeTax`/`taxAmount`)**: مشتركة بين `Expenses`/`Revenues`/`Purchases`. الحقل `amount` المُدخل في النموذج هو **دائمًا المبلغ شامل الضريبة** (لم يتغيّر معناه ولا استخدامه في أي مكان آخر بالتطبيق — التقارير/لوحة التحكم/صافي الربح كلها تستمر بالجمع على `amount` كما كانت). عند اختيار "نعم" لفاتورة ضريبية، تُحسب `amountBeforeTax`/`taxAmount` تلقائيًا بمعدل ثابت 15% عبر `calculateTaxBreakdown()` في `formatters.js` وتُخزَّن وقت الحفظ؛ عند "لا" يبقى الحقلان `null`. سجلات موجودة قبل هذه الميزة ستُعامَل ضمنيًا كـ "لا" (`hasTaxInvoice` غير موجود = falsy) في كل الأماكن التي تعرض/تجمع هذا الحقل.
| `BulkPurchaseBatches` | `code ('DFP-0001'…، تلقائي عبر generateNextBulkPurchaseCode()), date, lines: [{partyId?, type, gender, breed, count, unitPrice}], notes, source ('manual'\|'transfer'), journalEntryId?` — عملية شراء جملة مستقلة (بند واحد أو أكثر، تاريخ واحد للعملية كلها) **بدون** تسجيل الحيوانات فرديًا في `Animals`. تُرسمَل محاسبيًا كأصل: مدين مخزون كل بند على ورقة نوعه/جنسه (`1140101`-`1140104`/`1140201`-`1140204`، تقريب موثّق لأن بند الجملة بلا تصنيف عمر: إناث على "أمهات" وذكور على "كباش/تيوس") / دائن `11101 الصندوق الرئيسي` بالإجمالي، عبر `syncBulkPurchaseJournalEntry()` في `bulk-batch-service.js` (نفس نمط "قيد واحد مُعاد الصياغة لكل سجل" في `syncExpenseJournalEntry`). `source: 'transfer'` (يُنشأ فقط من `transferAnimalToTrade()` في `transfer-service.js` عند تحويل حيوان من القطيع لمخزون التجارة) **يُستثنى بالكامل من القيد** (بلا أثر نقدي حقيقي) لكنه يدخل حساب متوسط التكلفة بسعر الوحدة المُدخَل (القيمة التقديرية إن وُجدت، وإلا صفر) |
| `BulkSaleBatches` | `code ('DFS-0001'…، تلقائي عبر generateNextBulkSaleCode()), date, lines: [{partyId?, type, gender, breed, count, unitPrice, avgCostAtSale, cogsAmount}], notes, source ('manual'\|'transfer'), journalEntryId?` — عملية بيع جملة مستقلة، بنفس بنية الشراء. **البيع حرّ تمامًا**: لا يُشترط تطابق بنوده مع أي عملية شراء بعينها ولا مع الرصيد المتبقي (لا تحقق آلي مانع، يُسمح بالبيع بالسالب). `avgCostAtSale`/`cogsAmount` لكل بند **تُحسب وتُحفظ (snapshot) وقت الحفظ فقط** عبر `computeSaleLinesCosts()` (متوسط تكلفة متحرك: إجمالي تكلفة كل عمليات الشراء لنفس تركيبة نوع/جنس/سلالة ÷ إجمالي عددها، محسوبًا "كما في" تاريخ عملية البيع — لا تتأثر بعمليات بيع أخرى، فقط بالشراء) ولا تُعاد حسابها تلقائيًا بأثر رجعي إن عُدِّلت عملية شراء قديمة لاحقًا (نفس فلسفة عدم إعادة الحساب بأثر رجعي المتّبعة في باقي أتمتة المحاسبة بالمشروع). تُرسمَل محاسبيًا بقيد مركّب: (مدين `1000`/دائن `4010 إيرادات الشراء والبيع الجماعي`) بكامل الإيراد + (مدين `5060 تكلفة البضاعة المباعة - الشراء والبيع الجماعي`/دائن `1035`) بإجمالي `cogsAmount` — فيظهر الربح الحقيقي (إيراد − تكلفة بضاعة مباعة) فقط عند البيع الفعلي، بعكس الجيل السابق (`BulkBatches`) الذي كان يُحمِّل تكلفة الشراء كمصروف فوري. `source: 'transfer'` (من `transferTradeToAnimal()`) يُستثنى من القيد بنفس مبدأ الشراء أعلاه. كل الأرصدة (عدد/تكلفة الشراء، متوسط التكلفة، عدد/إيراد البيع، تكلفة البضاعة المباعة، الرصيد المتبقي، إجمالي الربح) **محسوبة حيًا وليست مخزَّنة** لكل تركيبة نوع/جنس/سلالة عبر `computeBulkGroupBalances(purchases, sales, asOfDate?)` في `bulk-batch-service.js` (تدعم "كما في تاريخ" بنفس مبدأ `computeAccountBalance`/`computeItemStockLevel`) — تُستخدم في صفحة "ملخص الأرصدة"، لوحة التحكم، والتقرير الشامل. لا حقول فاتورة ضريبية لكلا المخزنين (لم تُطلب لهذه الوحدة) |
| `Employees` | `fullName, employeeCode, phone, jobTitle (من JOB_TITLES), photoBase64? (صورة مُصغَّرة ≤200×200 عبر canvas قبل الحفظ، `employee-list-page.js` → `_resizeImageFileToBase64`), hireDate, salary, status ('active'\|'leave'\|'terminated')` |
| `CustodyItems` | `custodyNumber ('CUS-0001'…، تلقائي), date, employeeId, custodyType ('cash'\|'equipment'\|'devices'\|'vehicle'\|'tools'\|'supplies'\|'other'), purpose?, notes?, expectedReturnDate?, amount, settledAmount, settledQuantity, status ('open'\|'partially_settled'\|'settled'\|'cancelled'), createdByUserId, createdByUserName` + حقول خاصة بالنقدية فقط (`cashAccountCode: '11101'\|'11102'` — قيم قديمة `'1000'/'1010'` تُرحَّل تلقائيًا عبر `_migrateCashAccountCodeStrings` في `seed.js`) أو بالعينية فقط (`inventoryItemId, quantity, unitValue, serialNumber?`), و`journalEntryId?`/`inventoryMovementId?` لقيد/حركة الإصدار — كلها تُدار حصرًا عبر `custody-service.js` (`createCustodyIssue`/`settleCustody`/`cancelCustodyItem`/`transferCustodyBalance`)، **ليس** بـ`dbAdd`/`dbUpdate` مباشرة من صفحات العهد. `amount`/`settledAmount` دائمًا "قيمة نقدية موحّدة" لكلا النوعين (للعينية = `quantity × unitValue`) حتى تعمل التقارير/كشوف الحساب بمنطق واحد بصرف النظر عن `custodyType`؛ `quantity`/`settledQuantity` إضافيان للعينية فقط لتتبّع الأعداد الفعلية. **إصدار عهدة نقدية** ينشئ قيدًا (مدين `12105 عهد الموظفين` / دائن `11101` أو `11102`)؛ **عهدة عينية** تُنشئ حركة مخزون `out` (`reasonType: 'custody'`) + قيدًا (مدين `12105` / دائن `11301`/`11302`/`11303` حسب تصنيف الصنف — على رأس التصنيف لأن الصنف غير مربوط بورقة بعينه). سجلات `CustodyItems` القديمة (قبل هذه الميزة، بلا `custodyType`) تُعامَل كنقدية ضمنيًا عبر `isCashCustody()`، بلا `journalEntryId` (خارج نطاق المحاسبة المزدوجة — نفس مبدأ التوافق الخلفي لسجلات الفاتورة الضريبية أعلاه). حالة `'overdue'` **لا تُخزَّن أبدًا** — محسوبة حيًا للعرض فقط عبر `getEffectiveCustodyStatus()` (`status` مفتوحة/جزئية + `expectedReturnDate` تجاوز اليوم). ⚠️ `source?: 'transfer'` + `transferredFromCustodyId?`/`transferredFromVoucherNumber?` (اختيارية، غائبة = عهدة عادية `'issue'` ضمنيًا): تُضبط فقط على العهدة **الجديدة** التي تُنشئها `transferCustodyBalance()` (انظر `CustodySettlements` أدناه) — عهدة نقدية "منقولة" من عهدة أخرى، **بلا** `journalEntryId` (لا حركة نقدية حقيقية، نفس مبدأ `source:'transfer'` في `BulkPurchaseBatches`/`BulkSaleBatches`) |
| `CustodySettlements` | `custodyItemId, date, settlementType ('cashReturn'\|'itemsReturn'\|'invoiceSettlement'\|'salaryDeduction'\|'convertToExpense'\|'writeOff'\|'chargeToEmployee'\|'adminWaiver'\|'transferOut'), amount, quantity?, expenseCategory?, notes?, journalEntryId?, linkedExpenseId?, inventoryMovementId?, voucherNumber?, createdByUserId, createdByUserName` — سجل تسوية واحد لكل عملية تسوية على عهدة (append فعليًا؛ يبني "السجل الزمني" في `employee-wallet.html` وكشف الحساب في `employee-statement.html`)، يُنشأ حصرًا عبر `settleCustody()`/`transferCustodyBalance()` في `custody-service.js`. ⚠️ **الأنواع الثلاثة الأولى (`cashReturn`/`itemsReturn`/`invoiceSettlement`) تمثّل "تصفية" عهدة (الإغلاق الطبيعي المتوقّع) وتُنشأ حصرًا من `custody-liquidate.html`**؛ الباقي يمثّل "تسوية" (معالجة استثناء/فرق) وتُنشأ من `custody-settle.html`. `custody-liquidate.html` يسمح بتصفية عهدة نقدية بسطرين معًا (فواتير معتمدة + نقد مرتجع) يشتركان بنفس `voucherNumber`، مطابقةً لمثال كشف الحساب البنكي. لكل نوع قيد مختلف: `cashReturn` (مدين صندوق/بنك / دائن 12105)، `itemsReturn` (حركة مخزون `in` + مدين 11301/11302/11303 / دائن 12105)، `invoiceSettlement`/`convertToExpense` (ينشئ سجل `Expenses` فعليًا مع `linkedCustodyItemId`، وقيد مخصّص مدين حساب البند/دائن 12105 — **لا** يمر عبر `syncExpenseJournalEntry` العادية لأن النقدية خرجت فعليًا عند الإصدار؛ ⚠️ تعديل هذا المصروف لاحقًا من `expense-form.html` يستدعي `syncExpenseJournalEntry` العادية ويكسر هذا الربط بصمت)، `salaryDeduction` (مدين `12106 سلف الموظفين` / دائن 12105)، `chargeToEmployee` (مدين `12107 ذمم مدينة أخرى` / دائن 12105 — مديونية مفتوحة على الموظف بلا آلية استرداد مبرمجة بعد، أي استرجاع لاحق يتطلب قيدًا يدويًا من `accounting/journal-entries.html`)، `writeOff`/`adminWaiver` (مدين `81105 خسائر أخرى` / دائن 12105 — **كلاهما مقصوران على `resolveRoleKey(role) === 'systemAdmin'`**؛ بعد الشجرة الهرمية دُمجا على ورقة واحدة `81105` بدل `5091`/`5092` المنفصلتين، تمييزًا في التقارير رغم تطابق الآلية). `transferOut` (بلا `journalEntryId`، انظر شرح `transferCustodyBalance()` وحقل `CustodyItems.source` أعلاه — يُغطّي معًا "نقل الرصيد" و"تسوية عهدة بأخرى": إغلاق/تخفيض `settledAmount` على العهدة المصدر + إنشاء عهدة جديدة فعليًا للمستفيد، لأن Netting مباشرًا بين عهدتين على نفس اتجاه حساب `12105` لا يتوازن محاسبيًا، وزيادة `amount` عهدة موجودة تخالف مبدأ عدم تعديل العهدة الأصلية). الحساب `12105 عهد الموظفين` موجود ضمن الشجرة الهرمية الجديدة في `seed.js` (الفرع `12 المصروفات المقدمة والذمم الأخرى`). `voucherNumber?` (اختياري، غائب = سجلات قديمة قبل هذه الميزة بلا مرجع) مرجع تلقائي لكل حركة عبر `generateNextCustodyVoucherNumber(prefix)` — بادئة `TSF` لأنواع التصفية، `TSW` لأنواع التسوية، `TRF` لـ`transferOut`؛ يُعرض كعمود "المرجع" في `employee-statement.html` وضمن تفاصيل كل حدث في `employee-wallet.html` |
| `Notifications` | `type ('health'\|'custody'\|'finance'\|'death'\|'summary'\|'inventory'\|'vaccination'\|'pregnancy'), title, message, relatedEntityId, isRead` — ⚠️ **مخزن قديم متوقف الاستخدام بالكامل**: كان يُغذّى عبر `createNotification()` (`notification-service.js`) من 10 نقاط مختلفة في الكود وتُعرض عبر `notifications/notifications.html`. أُزيلت الصفحة والخدمة وكل نقاط الكتابة/القراءة لصالح الاعتماد الحصري على مركز التنبيهات الحي (`alerts-service.js`، انظر قسم "التنبيهات" أدناه). لا كود حالي يكتب أو يقرأ من هذا المخزن؛ بقي فقط اسمه في `OBJECT_STORES`/النسخ الاحتياطي الشامل تفاديًا لكسر قواعد بيانات المستخدمين الحاليين، وأي سجلات قديمة فيه من قبل هذا التغيير تبقى محفوظة لكن غير قابلة للعرض من أي واجهة |
| `Parties` | `type ('client'\|'supplier'\|'partner'), name, partyCode ('CL-0001'…/'SUP-0001'…/'PTR-0001'…، تلقائي عبر generateNextPartyCode()), category?, phone?, address?, locationUrl?, bankAccount? (مورّد غالبًا، لكن الحقل عام), creditLimit? (عميل غالبًا), paymentTermDays? (مهلة السداد بالأيام، تُستخدم لتنبيهات التأخر — لا معنى له لشريك، انظر أدناه), accountStatus ('active'\|'suspended', افتراضي 'active'), openingBalance (رقم، افتراضي 0), openingBalanceType ('debit'\|'credit', افتراضي 'debit'), openingBalanceJournalEntryId?, status?, notes?` — "تكويدات" عملاء/موردين/شركاء ممتدة لنظام ذمم مدينة/دائنة مبسّط، تُدار من `parties/parties.html` (CRUD بـ Modal) عبر `party-service.js`. ⚠️ **`accountStatus` ≠ `status`**: `status` محجوز حصرًا للحذف الناعم (`dbSoftDelete`)؛ `accountStatus` هو حقل "نشط/موقوف" التجاري الجديد — تعمّد استخدام اسم مختلف تفاديًا للتصادم. `openingBalance`/`openingBalanceType` يُرحَّلان محاسبيًا فور الحفظ عبر `_postPartyOpeningBalanceJournal()` (قيد بين حساب الطرف — `112`/`211`، أو `partyAccount:<id>`/`partnerControl` (افتراضيًا `3010`) لشريك — وحساب موازنة `3020`، يُعاد ترحيله بالكامل عند أي تعديل لاحق على المبلغ/الاتجاه). `Revenues`/`Purchases`/بنود `BulkSaleBatches.lines`/`BulkPurchaseBatches.lines` تحمل `partyId?` اختياريًا يشير لسجل هنا. **رصيد الطرف الحيّ** (غير مخزَّن) عبر `computePartyBalance(party, revenues, purchases, partyTransactions, fixedAssets?)`: موجب = مدين (الطرف مدين لنا)، سالب = دائن (نحن مدينون له) — يتغذّى من `openingBalance` + فواتير آجلة (`receiveMethod`/`paymentMethod === 'credit'` فقط؛ فواتير نقدي/تحويل لا تدخل الحساب إطلاقًا) ∓ سندات `PartyTransactions`. كشف الحساب الزمني الكامل (مع رصيد تراكمي) عبر `getPartyLedgerEntries()` في `party-statement.html` — كل سطر يحمل `journalEntryId` (من السجل المصدر: `Revenues`/`Purchases`/`PartyTransactions`/`FixedAssets`) يُعرض كعمود "رقم القيد" (مُقارَن بـ`entryNumber` عبر خريطة تُبنى من `getAllJournalEntries()` وقت تحميل الصفحة) وينقل النقر على الصف لصفحة القيد نفسه (`accounting/journal-entry-form.html?id=`، نفس نمط `cash-ledger-page.js`) — سطر بلا قيد مرحَّل (نادر) يعرض '-' بلا استجابة للنقر. تقرير أقدم منفصل بلا مفهوم ذمم: `reports/party-reports.html` (`party-reports-page.js`) — كشف معاملات تفصيلي (كل معاملاته + إجمالي، بلا فلترة نقدي/آجل) وترتيب "أعلى العملاء/الموردين" خلال فترة، لا يزال يعمل كما هو دون تغيير، ومقصور على عميل/مورّد فقط (`getAllParties('client')`/`getAllParties('supplier')` صراحة) — الشريك لا يظهر فيه بتصميم واعٍ. ⚠️ **نوع `partner` (شريك)**: يتعامل بحساب واحد موحّد **"جاري الشريك" (`3010`، حساب حقوق ملكية جاهز في الشجرة)** بدل `112`/`211` — كل الشركاء يشتركون بنفس الحساب الرأسي فتُفرَّق أرصدتهم الفردية حسابيًا (بفلترة `partyId`) لا دفتريًا، تمامًا كآلية عميل/مورّد الحالية. `createParty()`/`updateParty()` يضبطان تلقائيًا مفتاح الربط الديناميكي `partyAccount:<partyId>` (في `AccountMappings`، انظر `account-mapping-service.js`) على `3010` عند إنشاء/تحويل طرف لشريك — بهذا الربط وحده يبيع/يشتري الشريك بنفس نموذجَي `revenue-form.html`/`purchase-form.html` (بند "شركاء" منفصل في قائمة الطرف) **بلا أي تعديل** في `syncRevenueJournalEntry`/`syncPurchaseJournalEntry` (المفتاح `partyAccount:<id>` له أولوية أعلى من `revenueReceiveCredit`/`purchasePayCredit` العامّين أصلاً). `computePartyBalance`/`getPartyLedgerEntries` لهما فرع مخصص للشريك يجمع: مبيعات آجلة له (+) − مشتريات آجلة منه (−) − `capitalIn` (ضخ فلوس) + `capitalOut` (سحب فلوس) − مساهمات أصول (`FixedAssets.paymentMethod === 'partnerContribution'`، انظر صف `FixedAssets` أدناه). `isPartyOverdue()` تُعيد `false` دومًا لشريك (لا مفهوم "متأخر" لحساب جاري)، و`createReturnOrDiscount()` ترفض صراحة أي شريك (مرتجعات/خصومات غير مدعومة لجاري الشريك حاليًا). شاشة `accounting/account-mappings.html` (قسم "حسابات الأطراف") تتيح تغيير ربط كل شريك يدويًا (`partyCodesByType.partner = ['3010']`) لو احتاج المستخدم حسابًا مختلفًا لاحقًا. |
| `PartyTransactions` | `type ('collection'\|'payment'\|'return'\|'discount'\|'capitalIn'\|'capitalOut'), voucherNumber ('RC-0001'…/'PV-0001'…/'RTN-0001'…/'DSC-0001'…/'CIN-0001'…/'COUT-0001'…، تلقائي لكل نوع عبر generateNextVoucherNumber()), date, partyId, method? ('cash'\|'bank'\|'transfer'\|'network' — لـ collection/payment/capitalIn/capitalOut), amount (دائمًا موجب), description, journalEntryId?, linkedRevenueId?/linkedPurchaseId? (فقط لـ return/discount), createdByUserId, createdByUserName` — سندات قبض/صرف ومرتجعات/خصومات (عميل/مورّد) + ضخ/سحب فلوس (شريك)، append فعليًا (بلا تعديل/حذف، بنفس مبدأ `CustodySettlements`)، تُنشأ حصرًا عبر `createCollection()`/`createPayment()`/`createReturnOrDiscount()`/`createCapitalTransaction()` في `party-service.js`. `collection`: مدين الصندوق/البنك حسب `method` (`PARTY_CASH_ACCOUNT_CODE_BY_METHOD` — ⚠️ "شبكة" تُسجَّل على حساب البنك 11102 لعدم وجود حساب مخصّص)، دائن `112`. `payment`: مدين `211`، دائن الصندوق/البنك. `return`/`discount`: تُنشئ أيضًا سجل `Revenues`/`Purchases` **حقيقي بمبلغ سالب** (انظر تحذير `amount` السالب أعلاه) بقيد مخصّص (مدين `49`/دائن `112` للعميل، مدين `211`/دائن `81` للمورّد) بدل المرور عبر `syncRevenueJournalEntry`/`syncPurchaseJournalEntry` العاديتين (تفترضان مبلغًا موجبًا). `capitalIn` (ضخ فلوس من شريك): مدين نقد/بنك (`partnerCapitalInCash`/`partnerCapitalInBank`)، دائن حساب الشريك (`partyAccount:<id>` أو `partnerControl`، افتراضيًا `3010`). `capitalOut` (سحب فلوس بواسطة شريك): عكسها تمامًا (مدين حساب الشريك، دائن `partnerCapitalOutCash`/`partnerCapitalOutBank`). صفحات الإنشاء: `parties/customer-collection.html` (`collection`)، `parties/supplier-payment.html` (`payment`)، `parties/return-discount.html` (`return`/`discount` — صفحة إدارة مستقلة بالتنقل من القائمة الجانبية فقط، تعرض عملاء وموردين معًا مع فلتر نوع الطرف، بنفس نمط الإدارة الكاملة لسندات القبض/الصرف؛ لم تعد تُنشأ من نافذة منبثقة داخل `party-statement.html`)، و`parties/partner-capital.html` (`capitalIn`/`capitalOut` معًا، بحقل "نوع العملية") |
| `InventoryItems` | `name, category ('fodder'\|'medicine'\|'equipment'\|'other' — قائمة ثابتة في `inventory-service.js`، لا علاقة لها بحسابات "البند" في `ChartOfAccounts` رغم تشابه الاسم), unit (نص حر مثل 'كجم'/'لتر'), reorderThreshold? (رقم، null = بلا تنبيه نقص), notes, status?` — كتالوج أصناف المخزون، يُدار من `inventory/inventory-items.html` (CRUD بـ Modal) عبر `inventory-service.js`. لا حقل سعر مخزَّن على الصنف نفسه — عمود "آخر سعر شراء" المعروض في `inventory-items.html`/`inventory-reports-page.js` محسوب حيًا عبر `getLastPurchasePrice(item, movements)` (أحدث حركة "وارد" لهذا الصنف تحمل `unitCost`، وليس بالضرورة أرخص/أغلى سعر) |
| `InventoryMovements` | `itemId, direction ('in'\|'out'), quantity, date, reasonType ('purchase'\|'consumption'\|'damage'\|'adjustment'\|'other'), unitCost? (اختياري، معروض في جداول `inventory-movements.html`/`inventory-reports-page.js` كـ"تكلفة الوحدة"، لكن **لا يدخل** في حساب "صافي الربح" تفاديًا لازدواج الاحتساب مع `Expenses`/`Purchases` — إعلامي فقط ماليًا رغم عرضه إلا في حالة الربط بدفعة تسمين أدناه)، notes, status?, relatedExpenseId?, relatedPurchaseId?, linkedFatteningBatchId?` — حركات وارد/صادر، تُدار من `inventory/inventory-movements.html` عبر `inventory-service.js`. `linkedFatteningBatchId?` (اختياري، من نموذج الحركة نفسه — انظر صف `FatteningBatches` أعلاه) يربط حركة **صادر** بدفعة تسمين مباشرة (صرف من مخزون علف/أدوية موجود أصلاً بلا مصروف جديد)؛ تكلفتها (`quantity × unitCost`) تدخل ضمن التكلفة الجماعية للدفعة عبر `computeBatchLinkedCost()` في `fattening-service.js` — مستقل تمامًا عن `Expenses.linkedFatteningBatchId` ويمكن استخدام الاثنين معًا لنفس الدفعة. الرصيد الحالي لكل صنف **محسوب حيًا وليس مخزَّنًا** عبر `computeItemStockLevel(item, allMovements)` (وارد − صادر مقارنةً بـ `reorderThreshold`)، بنفس مبدأ `computeBulkBatchTotals` — يمكن أيضًا حساب الرصيد "كما في تاريخ سابق" بتمرير مجموعة حركات مفلترة بـ`date <= asOfDate` بدل كل الحركات (مستخدم في `inventory-reports-page.js`). `createInventoryMovement()` تُنشئ إشعارًا `type: 'inventory'` فقط عند **تحوّل** الصنف إلى حالة "منخفض" (وليس عند كل حركة)، ولا تُعاد إن وُجد إشعار غير مقروء بالفعل لنفس الصنف — تعديل/حذف حركة موجودة لا يعيد حساب/يلغي التنبيهات بأثر رجعي. `relatedExpenseId?`/`relatedPurchaseId?` (لا تجتمعان أبدًا على نفس الحركة) تُضبطان فقط للحركات المُنشأة تلقائيًا من ربط مصروف/مشترى (انظر `Expenses.linkedInventoryItemId`/`Purchases.linkedInventoryItemId` أعلاه) — تُستخدمان عبر `getInventoryMovementByExpenseId()`/`getInventoryMovementByPurchaseId()` لإيجاد الحركة وتحديثها/حذفها إن عُدِّل/حُذف السجل الأصلي. ⚠️ تعديل/حذف هذه الحركة يدويًا من `inventory-movements.html` مباشرة (بدل تعديل المصروف/المشترى نفسه) يُفقد التزامن بصمت — لا حماية آلية ضد ذلك حاليًا. **عمود "رقم القيد"** في `inventory-movements.html` (`inventory-movement-list-page.js`) يعرض رقم القيد المحاسبي للمصروف/المشترى المصدر لأي حركة تلقائية (رابط مباشر لـ`journal-entry-form.html?id=`، نفس نمط `revenue-list-page.js`) — عبر `relatedExpenseId`/`relatedPurchaseId` ثم `Expenses.journalEntryId`/`Purchases.journalEntryId`؛ حركة يدوية بلا ربط تعرض '-' |
| `FixedAssets` | `assetCode ('FA-0001'…، تلقائي), name, assetType ('land'\|'building'\|'vehicle'\|'equipment'\|'device'\|'furniture'\|'other'), category? (تصنيف فرعي حر، بلا أثر محاسبي), purchaseDate, usageStartDate (بداية احتساب الإهلاك), vendor, partyId? (رابط `Parties` مورّد، نفس نمط `Purchases.vendor`/`partyId`), invoiceNumber, locationId? (رابط `Locations`), responsibleEmployeeId? (رابط `Employees`), purchaseCost, acquisitionAdditionalCosts, improvementsTotal (مجموع تراكمي لتكاليف كل التحسينات اللاحقة، يُحدَّث من `createFixedAssetImprovement`), paymentMethod ('cash'\|'transfer'\|'cheque'\|'credit'\|'partnerContribution'), usefulLifeYears? (null للأراضي), residualValue, accumulatedDepreciation (متراكم، صفر دومًا للأراضي), costCenter, serialNumber, notes, assetStatus ('active'\|'sold'\|'disposed'\|'underMaintenance'، افتراضي 'active' — **منفصل عمدًا عن `status`** المحجوز للحذف الناعم، نفس مبدأ `Parties.accountStatus`؛ 'sold'/'disposed' تُضبطان فقط من `sellFixedAsset`/`disposeFixedAsset` ولا تعودان أبدًا)، journalEntryId? (قيد الاقتناء الأصلي فقط)` — سجل الأصل الثابت الرئيسي، يُدار عبر `fixed-asset-service.js` (`fixed-assets/asset-form.html`، لا Modal). إنشاء/تعديل الأصل يُرحِّل تلقائيًا قيد اقتناء (مدين حساب الأصل حسب `assetType` / دائن صندوق أو بنك أو `211` ذمم دائنة إن `paymentMethod: 'credit'`) عبر `syncFixedAssetJournalEntry()` (نفس نمط `syncPurchaseJournalEntry`). ⚠️ `paymentMethod: 'partnerContribution'` (مساهمة أصل من شريك، يتطلب `partyId` لشريك مسجّل — نفس حارس `credit`): الدائن هنا حساب الشريك المخصص (`partyAccount:<partyId>` بأولوية على `partnerControl`/`3010`) بدل مفتاح ثابت واحد كبقية طرق الدفع، فيدخل ضمن `computePartyBalance`/`getPartyLedgerEntries` الخاصين بذلك الشريك (انظر صف `Parties` أعلاه) كأنه "ضخ فلوس" عيني. التكلفة الدفترية = `purchaseCost + acquisitionAdditionalCosts + improvementsTotal` (`computeAssetCostBasis`)، القيمة الدفترية = التكلفة − `accumulatedDepreciation` (`computeAssetBookValue`) — كلتاهما حيّتان اعتمادًا على حقول مخزَّنة ومُحدَّثة تراكميًا (وليستا محسوبتين بالكامل من صفر من سجل المعاملات). بيع الأصل (`sellFixedAsset`، `fixed-assets/asset-sale.html`) يُنشئ قيدًا مركّبًا يُصفّي حساب الأصل ومجمع إهلاكه ويثبت ربحًا (`82101`) أو خسارة (`81101`) بالفرق عن القيمة الدفترية، ثم يضبط `assetStatus: 'sold'` نهائيًا (لا عملية أخرى تُقبل بعدها). استبعاد الأصل (`disposeFixedAsset`، `fixed-assets/asset-disposal.html`) نفس القيد لكن دون طرف استلام (القيمة الدفترية كاملة خسارة) — **مقصور على `resolveRoleKey(role) === 'systemAdmin'`** (نفس مبدأ تسوية "إعدام أو فاقد" في `CustodySettlements`)، ويضبط `assetStatus: 'disposed'`. نقل الأصل (`createFixedAssetTransfer`، `fixed-assets/asset-transfer.html`) بلا أثر محاسبي — تحديث `locationId`/`responsibleEmployeeId` فقط + تسجيل حدث. ⚠️ **بيع/استبعاد الأصل يُخفيان زر عمليات المرحلة الثانية بالكامل من `asset-card.html`** (لا نقل/تحسين/بيع/استبعاد ثانٍ على أصل منتهٍ) |
| `FixedAssetTransactions` | `assetId, type ('depreciation'\|'improvement'\|'maintenance'\|'transfer'\|'sale'\|'disposal'\|'countAdjustment' — 'countAdjustment' معرَّف بالاتحاد لكن غير مستخدم فعليًا؛ الجرد يكتب 'transfer' فقط عند اكتشاف نقل فعلي، انظر `FixedAssetCounts`)، date, period? (فقط لـ'depreciation'، صيغة 'YYYY-MM'، يمنع ترحيل إهلاك مزدوج لنفس الأصل ونفس الشهر), amount, notes, journalEntryId?` — سجل أحداث append-only لكل أصل (نفس مبدأ `CustodySettlements`/`PartyTransactions`)، يبني السجل الزمني في `fixed-assets/asset-card.html` (كل نوع بأيقونة/عنوان مختلف عبر `FIXED_ASSET_TXN_TYPE_META` في `asset-card-page.js` — ⚠️ قبل إضافة النوع `'maintenance'` كانت الدالة تفترض "إهلاك دوري" لكل الأنواع بلا تمييز، خطأ عرض قديم صُحِّح بالتزامن). الإهلاك محسوب بالقسط الثابت فقط حاليًا (`computeMonthlyDepreciation`) ويُرحَّل يدويًا دفعة واحدة لكل الأصول المستحقة عبر `fixed-assets/asset-depreciation-run.html` (`runDepreciationForPeriod`/`reverseDepreciationPeriod` في `fixed-asset-service.js`) — لا يوجد ترحيل تلقائي دوري لعدم وجود خادم/cron في هذا التطبيق. ⚠️ **`'maintenance'` (صيانة) ≠ `'improvement'` (تحسين)**: كلاهما بقيد محاسبي حقيقي (`createFixedAssetMaintenance()`/`createFixedAssetImprovement()` في `fixed-asset-service.js`، نفس البنية: `amount, date, paymentMethod, partyId?, description`)، لكن التحسين يُرسمَل مدينًا لحساب **الأصل نفسه** (يزيد `improvementsTotal`/التكلفة الدفترية)، بينما الصيانة مصروف عادي يُرسمَل مدينًا لحساب **مصروف صيانة الأصول** (مفتاح ربط `assetMaintenanceExpense`، بلا افتراضي فعلي حاليًا — `defaultCode: '74107'` كود متقاعد مثل بقية المفاتيح الدفاعية، يتطلب من صاحب المشروع إنشاء حساب "صيانة الأصول" من شجرة الحسابات وربطه من `account-mappings.html` أولاً، وإلا `createFixedAssetMaintenance()` ترمي رسالة خطأ عربية واضحة) — بلا أي أثر على التكلفة الدفترية أو `improvementsTotal`، فقط للتتبع التاريخي. يُفتَح من زر "🔧 صيانة" في `asset-card.html` (`fixed-assets/asset-maintenance.html`/`asset-maintenance-page.js`، نفس نمط `asset-improvement.html` تمامًا) ويظهر أيضًا كـKPI "إجمالي تكلفة الصيانة" حيّ (مجموع كل معاملات `'maintenance'` للأصل) في بطاقة الأصل. مفاتيح الربط الثلاثة الأخرى (`assetMaintenancePayCash`/`Bank`/`Credit`، دفاترها 11101/11102/211) لطريقة الدفع، بنفس نمط `assetPayCash`/`Bank`/`Credit` لاقتناء الأصل. ⚠️ **بيع أصل آجلاً لا يربط `partyId` هيكليًا** (اسم المشتري نص حر فقط في `notes`) — لا يظهر في `computePartyBalance()` الخاص بالعميل، انظر تعليق `FIXED_ASSET_SALE_ACCOUNT_CODE_BY_METHOD` في `fixed-asset-service.js`؛ نفس القيد ينطبق على `partyId` في الصيانة/التحسين (يُفرَض فقط كتحقق للدفع الآجل، بلا تخزين/استخدام فعلي في حل الحساب) |
| `FixedAssetCounts` | `date, notes, lines: [{assetId, resultStatus ('matched'\|'missing'\|'damaged'\|'relocated'), locationId? (الموقع الفعلي المكتشف، فقط لـ'relocated'), notes?}], createdByUserId, createdByUserName` — جلسة جرد دورية واحدة (نمط `StockCounts`، لكن **توثيق فقط بلا تسوية قيمة/كمية** — لا أثر محاسبي)، تُدار عبر `fixed-asset-service.js` (`createFixedAssetCount`) و`fixed-assets/asset-count.html`. سطر بحالة `'relocated'` مع `locationId` فعلي مختلف عن `FixedAssets.locationId` الحالي **يُحدِّث الموقع تلقائيًا** + يسجّل معاملة `'transfer'` مرجعها الجرد نفسه في `FixedAssetTransactions` — أي حالة أخرى (`missing`/`damaged`) توثيق بحت بلا أي إجراء تلقائي لاحق (لا استبعاد آلي لأصل "مفقود"؛ يتطلب قرارًا يدويًا عبر `disposeFixedAsset`) |
| `AccountingPeriods` | `periodKey ('YYYY-MM'), isClosed, closedAt?, closedByUserId?, closedByUserName?, reopenedAt?, reopenedByUserId?, reopenedByUserName?, reopenReason?, history: [{action:'closed'\|'reopened', at, byUserId, byUserName, reason?}]` — سجل واحد لكل شهر، يُدار عبر `period-service.js` و`accounting/period-lock.html` (مقصور إنشاؤه/تعديله على `resolveRoleKey(role) === 'systemAdmin'`، نفس مبدأ `disposeFixedAsset`/تسويات `writeOff`/`adminWaiver`). **غياب سجل لشهر معيّن يعني أنه مفتوح ضمنًا** (نفس اتفاقية "الغياب = وصول كامل" في `Users.permissions`) — لا تُزرع سجلات افتراضية لأي شهر. `guardPeriodOpenForSave(dateStr)` هي الدالة الحارسة المُستهلَكة من صفحات النماذج (مدير النظام يتجاوزها دومًا)؛ الإنفاذ **على مستوى الصفحة لا داخل `accounting-service.js`** عمدًا (لتفادي كسر ترحيل `seed.js` التلقائي الذي قد يكتب قيودًا بتواريخ ماضية). النطاق الحالي (قد يتوسّع لاحقًا لوحدات أخرى): `Expenses`, `Revenues`, `Purchases`, `JournalEntries` اليدوية، وتحويل الصندوق↔البنك — **لا يشمل بعد** العهد/الذمم/الجملة/الأصول الثابتة رغم مرورها جميعًا بقيود محاسبية أيضًا |
| `FiscalYears` | `yearKey ('YYYY'), isClosed, closedAt?, closedByUserId?, closedByUserName?, closingJournalEntryId?, appropriationJournalEntryId?, totalRevenue?, totalExpense?, netIncome?, reopenedAt?, reopenedByUserId?, reopenedByUserName?, reopenReason?, history: [{action:'closed'\|'reopened', at, byUserId, byUserName, reason?}]` — "إقفال السنة المالية" (`fiscal-year-service.js`, `accounting/fiscal-year-close.html`)، مقصور على `resolveRoleKey(role) === 'systemAdmin'` (نفس مبدأ `period-lock.html`). سجل واحد لكل سنة ميلادية كاملة (`YYYY-01-01`←`YYYY-12-31`)؛ **غياب سجل لسنة معيّنة يعني أنها مفتوحة ضمنًا** (نفس اتفاقية `AccountingPeriods`/`Users.permissions`). `closeFiscalYear(yearKey, user)` تُنشئ قيد إقفال حقيقي يُصفّر كل حسابات الإيرادات والمصروفات المرحّلة (`getPostedJournalEntries()`) ضمن السنة (سطر معاكس لكل حساب غير صفري، عبر `computeIncomeStatement()` الموجودة أصلاً — فتبقى نتيجة الإقفال مطابقة تمامًا لما تعرضه قائمة الدخل لنفس السنة) وتنقل الفرق (صافي الربح/الخسارة) إلى `32103 صافي ربح السنة الحالية`؛ إن كان لهذا الحساب رصيد متبقٍ من إقفال سنة أقدم لم يُرحَّل بعد، يُنقَل أولاً بقيد مستقل (`appropriationJournalEntryId`، مؤرَّخ أول يوم في السنة الجديدة) إلى `32101 أرباح سنوات سابقة` — بذلك يبقى 32103 دومًا يعكس نتيجة "آخر سنة أُقفلت فقط" و32101 يتراكم كل ما قبلها. **لا تعديل على `computeBalanceSheet()`/`computeIncomeStatement()` الحيّتين في `accounting-service.js`**: صافي الدخل الحيّ (تراكمي منذ البداية) يعود تلقائيًا يعكس نتيجة السنة غير المُقفلة الحالية فقط بمجرد وجود قيد الإقفال (يُصفّر حسابات الإيرادات/المصروفات للسنوات المُقفلة). كل هذا **بلا أي أثر على أي رصيد آخر** (القطيع/المخزون/الذمم/العهد/النقدية) — تلك تستمر في نفس دفتر الأستاذ المستمر برصيدها الحالي كما هي، لا "نسخ"/"ترحيل" فعلي لبياناتها. `closeFiscalYear()` تقفل أيضًا الأشهر الاثني عشر للسنة تلقائيًا (`closePeriod()` من `period-service.js`) لمنع أي إضافة/تعديل لاحق بتاريخ ضمنها؛ `reopenFiscalYear(yearKey, user, reason)` تعكس ذلك بالكامل (حذف ناعم لقيدي الإقفال/الترحيل + إعادة فتح الأشهر). **الإقفال وإعادة الفتح إلزاميًا بالترتيب الزمني** (لا يمكن إقفال سنة قبل التي تسبقها إن كان لها سجل، ولا إقفال/فتح سنة بمعزل عن سنة لاحقة مغلقة فعليًا) تفاديًا لتضارب سلسلة 32101↔32103 — سنة بلا سجل إطلاقًا لا تُعامَل عائقًا (لا يُفرَض إقفال كل التاريخ القديم عند أول استخدام لهذه الميزة). |
| `Adjustments` | `type ('accruedExpense'\|'accruedRevenue'\|'prepaidExpense'\|'deferredRevenue'\|'cashDifference'), date, amount, description, categoryAccountId? (حساب مصروف/إيراد — يُحدَّد عند الإنشاء لنوعَي accrued*، وعند التسوية لنوعَي prepaid/deferred)، cashAccountCode? ('11101'\|'11102' — عكس ذلك تمامًا: يُحدَّد عند الإنشاء لـ prepaid/deferred/cashDifference، وعند التسوية لـ accrued*; القيم القديمة '1000'/'1010' تُرحَّل تلقائيًا عبر `_migrateCashAccountCodeStrings` في `seed.js`), direction? ('shortage'\|'overage', لـcashDifference فقط), status ('open'\|'settled'), journalEntryId, settledDate?, settledJournalEntryId?, settledByUserId?, settledByUserName?, createdByUserId, createdByUserName` — "التسويات المحاسبية" (`adjustment-service.js`, `accounting/adjustments.html`)، تغطي حصرًا الفجوة التي لم يكن لها أي مقابل سابقًا في التطبيق: محاسبة الاستحقاق والدفع المقدم (accrual accounting) + فرق صندوق سريع. **لا تكرار** لآليات تسوية موجودة فعليًا في وحدات أخرى (فروقات الجرد ← `stock-count-service.js`، تسويات العهد ← `custody-service.js`، تسويات العملاء/الموردين ← `party-service.js` → `createReturnOrDiscount()`) — الصفحة تكتفي بروابط سريعة (`kpi-bar.js`) إليها بدل إعادة بنائها. نفس نمط "فتح ← تسوية لاحقة، كل مرحلة قيدها الخاص" المتّبع في `CustodyItems`/`CustodySettlements`: الأنواع الأربعة الأولى تُنشأ `status:'open'` (قيد أول فقط، ضد حساب تحكّم مخصّص لكل نوع: `21205` مصروفات مستحقة، `11203` ذمم مدينة أخرى، `12101` مصروفات مدفوعة مقدمًا، `21205` للإيرادات المقدمة — تقريب موثّق لعدم وجود ورقة "إيرادات مقدمة" مخصصة في الشجرة) ثم تُغلَق لاحقًا عبر `settleAdjustment()` (قيد معاكس يكمل الطرف الناقص)؛ `cashDifference` يُغلَق فورًا `status:'settled'` بقيد واحد (مدين `81104` فروقات جرد أو دائن `82103` أرباح أخرى حسب `direction`، مقابل الصندوق/البنك المختار) بلا تسوية لاحقة. حسابات التحكّم هذه جزء من الشجرة الهرمية الجديدة مباشرة (بلا دالة بذر منفصلة). `createAdjustment()`/`settleAdjustment()` تستدعيان `guardPeriodOpenForSave()` (من `period-service.js`، انظر صف `AccountingPeriods`) قبل الترحيل — نفس إنفاذ قفل الفترات المطبَّق على المصروفات/الإيرادات/المشتريات |
| `CashCounts` | `date, accountCode ('11101'\|'11102' — قيم قديمة '1000'/'1010' تُرحَّل تلقائيًا عبر `_migrateCashAccountCodeStrings` في `seed.js`), systemAmount (لقطة رصيد النظام وقت الجرد عبر computeAccountBalance كما في date)، countedAmount (المبلغ المعدود فعليًا), difference (countedAmount − systemAmount), notes, adjustmentId? (يشير لسجل Adjustments النوع cashDifference — فقط لو difference ≠ 0), createdByUserId, createdByUserName` — "جرد الصندوق ومحضر فرق الصندوق" (`cash-count-service.js`, `accounting/cash-count.html`). `createCashCount()` يحسب الفرق تلقائيًا وينشئ تسوية `Adjustments(cashDifference)` مقابلة عند وجود فرق (إعادة استخدام كاملة لآلية التسويات، بلا إدخال يدوي مزدوج)؛ `deleteCashCount()` يحذف التسوية المرتبطة معه. سجل الجرد نفسه هو "محضر فرق الصندوق" — قابل للعرض/الطباعة منفردًا عبر `cash-count.html?id=<id>` (وضع "محضر" يستبدل النموذج/السجل بعرض تفاصيل الجرد فقط). محكوم بـ `guardPeriodOpenForSave()` قبل الحفظ |
| `BankStatementLines` | `date, description, amount, direction ('debit'\|'credit' — من منظور كشف البنك نفسه: credit = إيداع/دخول للبنك، debit = سحب/خروج), reference?, reconciled (boolean), matchedEntryId? (يشير لسجل JournalEntries عند المطابقة), createdByUserId, createdByUserName` — "تسوية البنوك" (`bank-reconciliation-service.js`, `accounting/bank-reconciliation.html`). ⚠️ **إدخال يدوي فقط، بلا رفع/تحليل ملف كشف حساب** (قرار عمل: لا خادم ولا مكتبة تحليل موحّدة لصيغ كشوف البنوك المختلفة؛ قابل للتوسعة لاحقًا). المطابقة يدوية بضغطة واحدة: `suggestSystemMatches()` يقترح حركات نظام غير مطابَقة بنفس المبلغ تمامًا (بلا ضبابية تاريخ)، `matchBankStatementLine()`/`unmatchBankStatementLine()` يضبطان `reconciled`/`matchedEntryId`. `computeReconciliation()` تُصنّف حيًا (بلا تخزين) حركات فترة معيّنة إلى متطابقة/حركات نظام معلَّقة (لم تظهر بالبنك بعد)/حركات كشف غير مسجَّلة بالنظام بعد — كلاهما مبنيّان على `computeAccountLedgerRows()` الجديدة في `accounting-service.js` (دفتر تفصيلي سطرًا سطرًا لحساب واحد، بنفس مبدأ `computeAccountBalance` الموجودة لكن بتفاصيل الأسطر لا الإجمالي فقط — يُستخدم أيضًا في `cash-ledger-page.js`) |
| `AccountMappings` | `key (مفتاح الربط من ACCOUNT_MAPPING_DEFINITIONS), accountCode (كود الحساب المختار من الشجرة), updatedByUserId?, updatedByUserName?` — "ربط العمليات بالحسابات" (`account-mapping-service.js`, `accounting/account-mappings.html`): كل نقطة ترحيل تلقائي في النظام (صندوق/بنك/ذمم/مرتجعات/عهد/مخزون/جملة/أصول ثابتة/تسويات/جاري شريك — مفاتيح ثابتة معرفة في `ACCOUNT_MAPPING_DEFINITIONS` بافتراضيات من الشجرة الجديدة، منها تفصيل **طريقة الدفع لكل عملية**: مصروف/مشترى/إيراد/اقتناء أصل/بيع أصل/**صيانة أصل**/سند قبض/سند صرف × نقدي أو تحويل/بنك أو آجل/معلّق (`assetMaintenanceExpense`/`assetMaintenancePayCash`/`Bank`/`Credit` للصيانة تحديدًا — انظر `FixedAssetTransactions` أعلاه)، `animalSaleSheep`/`animalSaleGoat` للبند المقترح تلقائيًا عند بيع حيوان فردي، و`partnerControl`/`partnerCapitalInCash`/`partnerCapitalInBank`/`partnerCapitalOutCash`/`partnerCapitalOutBank` لجاري الشريك) **+ مفاتيح مركّبة ديناميكية** (خارج التعريفات، تُدار من نفس الشاشة في أقسام "تكويد تفصيلي لكل بند" و"حسابات الأطراف"): `catInventory:<accountId>` (مخزون مرتبط ببند مشترى — إن حُدِّد يُرسمَل الشراء على المخزون بدل المصروف)، `catPayCash/Bank/Credit:<accountId>` و`catReceiveCash/Bank/Credit:<accountId>` (حساب الدفع/التحصيل لكل بند × طريقة)، و`partyAccount:<partyId>` (حساب ذمم مخصص لطرف). **التراجع التدريجي في الخدمات** (`getMappedAccountWithFallback`): حساب الطرف > حساب البند للطريقة > المفتاح العام > الافتراضي. **الغياب = الافتراضي** (نفس اتفاقية `Users.permissions`)، واختيار كود الافتراضي نفسه يمسح السجل (`setAccountMapping`). القيود الجديدة فقط تتأثر (القيود المرحّلة القديمة لا تُعاد كتابتها). التغيير ينعكس أيضًا على خيارات حساب الصندوق/البنك في `cash-ledger.html`/`cash-count.html`/`custody-issue.html`/`adjustments.html`/`bank-reconciliation.html`/تنبيه انخفاض النقدية في `alerts-service.js` — أي صفحة تحمل خدمة ترحيل تلقائي يجب أن تحمّل `account-mapping-service.js` قبلها |
| `WeeklyBatches` | `batchNumber ('WK-0001'…، تلقائي عبر generateNextWeeklyBatchCode()), dateFrom, dateTo, notes, entryIds: number[] (لقطة معرّفات القيود المؤرشفة وقت الإنشاء), entryCount, totalDebit, totalCredit (لقطة إجمالي مدين/دائن كل القيود المؤرشفة وقت الإنشاء، لا تُعاد حسابهما لاحقًا), createdByUserId, createdByUserName` — "الأرشفة الأسبوعية للقيود" (`weekly-batch-service.js`, `accounting/weekly-batches.html`): **طبقة أرشفة/تجميع فوق `JournalEntries` الموجودة فعليًا فقط — لا تُنشئ أي قيد محاسبي جديد ولا تدمج قيودًا متعددة في قيد واحد**. `createWeeklyBatch({dateFrom, dateTo, notes})` تُنفّذ خطوتين: (1) ترحّل أي قيد `status:'draft'` ضمن الفترة (نفس `postJournalEntry()`، بفحص `guardPeriodOpenForSave` الصامت لكل قيد على حدة — أي قيد بفترة مغلقة يُتخطى بصمت ويُحتسب في `blockedByPeriod` ويبقى متاحًا لأرشفة لاحقة)، ثم (2) تضبط `JournalEntries.weeklyBatchId` (حقل جديد، يشير لسجل هنا) على كل قيد ضمن الفترة لم يُؤرشف من قبل (`!weeklyBatchId`). بذلك يبقى لكل قيد أثره المحاسبي المستقل بالضبط كما هو (دفتر الصندوق/البنك، تسوية البنوك، ميزان المراجعة، قائمة الدخل — كلها تعمل بلا أي تغيير)، وفي نفس الوقت تحصل على رقم مرجعي واحد تُطبع/تُصدَّر تحته كل قيود الفترة دفعة واحدة من صفحة الأرشفة (`getWeeklyBatchDetails(id)`). `deleteWeeklyBatch()` حذف ناعم للأرشفة + تصفير `weeklyBatchId` عن كل قيودها (لا يمس القيود نفسها) فتعود قابلة لأرشفة لاحقة. ⚠️ **`batchNumber` يُستهلَك أيضًا خارج وحدة المحاسبة**: عمود "رقم الأرشفة" في `expense-list.html`/`revenue-list.html`/`purchase-list.html` (`expense-list-page.js`/`revenue-list-page.js`/`purchase-list-page.js`) يعرضه حيًا لكل سجل عبر `journalEntryId` → `JournalEntries.weeklyBatchId` → `WeeklyBatches.batchNumber` — استبدل زر "📎 مرفقات" الرقمي القديم لهذه الثلاثة تحديدًا (انظر ملاحظة `attachment-manager.js` أعلاه)؛ '-' لسجل قيده غير مُرحَّل أو لم يُدرَج بعد ضمن أرشفة أسبوعية. الثلاث صفحات تحمّل `weekly-batch-service.js` لهذا الغرض فقط (بلا واجهة إنشاء/حذف أرشفة من صفحاتها — تلك حصرًا من `accounting/weekly-batches.html`) |
| `AnimalCounts` | `date, notes, lines: [{animalId, animalCode, type, gender, breed, estimatedValue, resultStatus ('present'\|'missing')}], createdByUserId, createdByUserName` — "الجرد الحيواني" (`animal-count-service.js`, `herd/animal-count.html`): جلسة جرد دورية (عادة نهاية السنة المالية) لكل حيوانات القطيع الحية، بقيمة تقديرية لكل حيوان وحالة موجود/مفقود — **توثيقي فقط بلا أثر محاسبي** (نفس مبدأ `FixedAssetCounts`؛ القيمة التقديرية تُستخدم في محضر الجرد ومرجعًا لتقييم المخزون الحيواني بلا قيد تلقائي حاليًا). كل سطر يُخزَّن مع لقطة من بيانات الحيوان وقت الجرد (الكود/النوع/الجنس/السلالة) حتى يبقى المحضر مقروءًا لو حُذف الحيوان لاحقًا. الجلسة قابلة للعرض/الطباعة/التصدير (محضر) والحذف الناعم |
| `Attachments` | `recordType ('expense'\|'revenue'\|'purchase'\|'bulkPurchase'\|'bulkSale'\|'custody'\|'custodySettlement'\|'partyTransaction'\|'fixedAsset'), recordId, fileName, fileType (mime), fileSize (bytes), blob (Blob الملف الفعلي)` — ⚠️ `'bulkBatch'` كانت القيمة المستخدمة قبل إعادة هيكلة وحدة الجملة لشراء/بيع مستقلّين؛ أي مرفقات قديمة بهذه القيمة أُعيد توجيهها تلقائيًا لـ`'bulkPurchase'` عبر `_migrateBulkBatchesToPurchaseSaleIfNeeded()` في `seed.js` — أرشفة صور/PDF الفواتير عبر `attachment-service.js` (`getAttachmentsForRecord`, `addAttachment`, `deleteAttachment`) و`attachment-manager.js`. **الحقل `blob` هو الاستثناء الوحيد في كل المخازن الذي ليس نصًا/رقمًا/boolean** — عادي داخل IndexedDB (يدعم Structured Clone)، لكنه غير قابل لـ`JSON.stringify` مباشرة، لذا نسخة `settings-page.js` الاحتياطية (`_exportBackup`/`_replaceDatabaseWithBackup`) تحوّله Base64 ذهابًا وإيابًا خصيصًا لهذا المخزن فقط عبر `_blobToBase64`/`_base64ToBlob` — أي كود جديد يتعامل مع `Attachments` بمعزل عن هاتين الدالتين (مثل نسخ احتياطي مخصص أو تصدير آخر) سيفقد محتوى الملف بصمت إن مرّ عبر `JSON.stringify` مباشرة. لا حذف تلقائي للمرفقات عند حذف السجل الأصل (Expenses/Revenues/Purchases تستخدم حذفًا ناعمًا `dbSoftDelete` فتبقى السجلات والمرفقات معًا قابلة للاسترجاع) |

كل السجلات تحصل تلقائيًا على `id` (autoIncrement)، `createdAt` (عند الإضافة)، و`updatedAt` (عند أي تعديل) — لا داعي لضبطها يدويًا.

## خريطة الوحدات الوظيفية

| الوحدة | صفحات HTML | JS الصفحة | الخدمة (service) |
|---|---|---|---|
| الدخول | `index.html` (وضع ليلي بزر عائم + "تذكرني" يحفظ اسم المستخدم/كلمة المرور محليًا لعدة حسابات معًا (`localStorage['savedLoginAccounts']`، مصفوفة `{username, password}[]` نصًا صريحًا — نفس مستوى أمان الجلسة المُقرّ به في auth-service.js، تطبيق محلي أحادي الجهاز) + قائمة منسدلة تظهر عند الوقوف على/الكتابة في حقل اسم المستخدم لاختيار حساب محفوظ (يعبّئ الحقلين معًا) أو حذفه (✕ لكل عنصر)؛ تعبئة تلقائية + رسالة ترحيب فقط لو كان المحفوظ حسابًا واحدًا بالضبط؛ ترحيل تلقائي لمرة واحدة من الشكل القديم أحادي الحساب `localStorage['rememberedCredentials']` + خطوة تحقق بخطوتين اختيارية عند تفعيلها) | `login-page.js` | `auth-service.js` (+`seed.js` للبيانات التجريبية) + `totp-service.js` للخطوة الثانية (2FA، انظر صف `Users` أعلاه) |
| لوحة التحكم | `dashboard.html` | `dashboard-page.js` | تجمع بيانات من كل الخدمات |
| القطيع | `herd/herd-list.html`, `herd/herd-form.html`, `herd/mating.html`, `herd/pregnancy.html`, `herd/births.html`, `herd/deaths.html`, `herd/health.html`, `herd/vaccinations.html`, `herd/animal-count.html` (الجرد الحيواني لنهاية السنة — جلسة جرد لكل الحيوانات الحية بقيمة تقديرية لكل حيوان، توثيقي بلا أثر محاسبي، انظر صف `AnimalCounts` في جدول نموذج البيانات)، `herd/fattening.html` (سجل التسمين — دفعات تسمين جماعية لتوزيع تكلفة العلف والأدوية/الفيتامينات المشتركة على كل رأس، انظر صف `FatteningBatches` أعلاه؛ تدعم `?animalId=` لعرض دفعات حيوان واحد فقط، رابط في `herd-form.html`) (نمط "نموذج إدخال أعلى الصفحة + جدول سجلات سابقة" نفسه المستخدم في كل هذه الصفحات — لا صفحة `form.html` منفصلة ولا Modal؛ تعديل/حذف سجل يتم من Modal يُفتح بالنقر على الصف. تدعم `?animalId=` لعرض/تسجيل سجلات حيوان واحد فقط؛ روابط "سجل التلقيح"/"سجل الحمل"/"السجل الصحي"/"التحصينات"/"سجل التسمين" في `herd-form.html` تفتحها بهذا الوسيط، الأولان يظهران فقط للإناث) | `herd-list-page.js`, `herd-form-page.js`, `mating-page.js`, `pregnancy-page.js`, `births-page.js`, `deaths-page.js`, `health-page.js`, `animal-count-page.js`, `fattening-page.js` | `animal-service.js`, `mating-service.js`, `pregnancy-service.js`, `birth-service.js`, `death-service.js`, `health-service.js`, `animal-count-service.js`, `fattening-service.js` |
| المصروفات | `expenses/expense-list.html` (عمود "رقم الأرشفة" لكل صف — محسوب حيًا من الأرشفة الأسبوعية للقيود لا حقلًا مُدخَلاً، انظر صف `WeeklyBatches` أعلاه)، `expenses/expense-form.html` (قسم اختياري "ربط بصنف مخزون" ينشئ حركة "وارد" مطابقة تلقائيًا في `InventoryMovements`، انظر `Expenses.linkedInventoryItemId` في جدول نموذج البيانات؛ + قسم اختياري مستقل "ربط بدفعة تسمين" لوسم المصروف بدفعة `FatteningBatches` بلا أي أثر تلقائي آخر، انظر `Expenses.linkedFatteningBatchId`؛ + قسم اختياري ثالث مستقل "ربط بحيوان" لوسم المصروف كتكلفة فردية مباشرة على حيوان بعينه (قائمة الحيوانات الحية فقط لسجل جديد)، انظر `Expenses.linkedAnimalId`) | `expense-list-page.js`, `expense-form-page.js` | `expense-service.js`. `expense-form-page.js` يستدعي أيضًا `inventory-service.js` لمزامنة الحركة المرتبطة، `fattening-service.js` لتعبئة قائمة دفعات التسمين النشطة، و`animal-service.js` لتعبئة قائمة الحيوانات الحية عند الربط الفردي |
| الإيرادات | `revenues/revenue-list.html` (عمود "رقم الأرشفة" كذلك)، `revenues/revenue-form.html` | `revenue-list-page.js`, `revenue-form-page.js` | `revenue-service.js` |
| المشتريات | `purchases/purchase-list.html` (عمود "رقم الأرشفة" + عمودا **"رقم القيد"** و**"تكلفة الوحدة"**، نفس نمط `revenue-list.html` — انظر ملاحظة عمود "رقم القيد" أعلاه)، `purchases/purchase-form.html` (قسم اختياري "ربط بصنف مخزون" ينشئ حركة "وارد" مطابقة تلقائيًا في `InventoryMovements`، انظر `Purchases.linkedInventoryItemId` في جدول نموذج البيانات) | `purchase-list-page.js`, `purchase-form-page.js` | `purchase-service.js` — نسخة طبق الأصل من `expense-service.js` على مخزن `Purchases`. `purchase-form-page.js` يستدعي أيضًا `inventory-service.js` لمزامنة الحركة المرتبطة (`_syncPurchaseInventoryLink`) |
| الشراء والبيع الجماعي | شراء وبيع الحيوانات بالجملة **بدون** تسجيل كل رأس فرديًا في `Animals` — الآن عمليتان مستقلّتان تمامًا (لم تعودا سجلاً واحدًا مدمجًا)، كل عملية بقيدها المحاسبي الخاص: `bulk/bulk-purchases.html` (قائمة عمليات الشراء، نمط 1 من نمطي الـCRUD — تصدير Excel/PDF + زر مرفقات `recordType: 'bulkPurchase'`)، `bulk/bulk-purchase-form.html` (نموذج عملية شراء: بند واحد أو أكثر بتاريخ واحد للعملية)، `bulk/bulk-sales.html` (قائمة عمليات البيع، بنفس النمط، `recordType: 'bulkSale'`)، `bulk/bulk-sale-form.html` (نموذج عملية بيع — لكل بند معاينة حيّة لمتوسط تكلفة الشراء الحالي والكمية المتاحة لنفس التركيبة، تُحسب عبر `getGroupAverageCost()`)، `bulk/bulk-summary.html` (ملخّص أرصدة/متوسط تكلفة/ربح لكل تركيبة نوع/جنس/سلالة، عرض فقط بلا CRUD)، و`bulk/transfer.html` (تحويل حيوان فردي ↔ مخزون التجارة — كل تحويل ينشئ بنفسه عملية شراء/بيع مستقلة `source: 'transfer'` مستثناة من القيد المحاسبي). التنقل بين هذه الصفحات حصريًا عبر القائمة الجانبية (أُزيل شريط التبويبات العلوي المحلي السابق) | `bulk-purchase-list-page.js`, `bulk-purchase-form-page.js`, `bulk-sale-list-page.js`, `bulk-sale-form-page.js`, `bulk-summary-page.js`, `transfer-page.js` | `bulk-batch-service.js` (رغم الاسم — لم يتغيّر لتفادي تعديل كل وسوم `<script>` التي تحمّله؛ محتواه أُعيد كتابته بالكامل: CRUD مزدوج للشراء/البيع + `computeBulkGroupBalances(purchases, sales, asOfDate?)` لكل الأرصدة/متوسط التكلفة + `syncBulkPurchaseJournalEntry`/`syncBulkSaleJournalEntry` للترحيل المحاسبي المنفصل)، و`transfer-service.js` لعمليتي التحويل |
| المحاسبة | `accounting/chart-of-accounts.html` (شجرة الحسابات — **تحل الآن محل شاشة "تكويدات البنود" الملغاة**، انظر ملاحظة "البند = حساب" في جدول نموذج البيانات أعلاه)، `accounting/account-mappings.html` (ربط العمليات بالحسابات — تغيير حسابات الترحيل التلقائي، انظر صف `AccountMappings` أعلاه)، `accounting/journal-entries.html` (سجل القيود اليدوية + التلقائية معًا)، `accounting/weekly-batches.html` (الأرشفة الأسبوعية للقيود — تجميع قيود فترة معيّنة تحت رقم مرجعي واحد للطباعة/الأرشفة بلا أي تغيير في القيود نفسها، انظر صف `WeeklyBatches` أعلاه)، `accounting/cash-transfer.html` (تحويل صندوق↔بنك)، `accounting/period-lock.html` (قفل الفترات المحاسبية — إغلاق/فتح شهر، مقصور على مدير النظام، انظر صف `AccountingPeriods` في جدول نموذج البيانات أعلاه)، `accounting/fiscal-year-close.html` (إقفال السنة المالية — قيد إقفال حقيقي يُصفّر الإيرادات/المصروفات وينقل صافي الربح/الخسارة لحقوق الملكية + قفل أشهرها تلقائيًا، مقصور على مدير النظام، انظر صف `FiscalYears` أعلاه)، `accounting/adjustments.html` (التسويات المحاسبية — مصروف/إيراد مستحق أو مقدم + فرق صندوق، بروابط سريعة لتسويات الجرد/العهد/الذمم الموجودة فعليًا في وحداتها، انظر صف `Adjustments` أعلاه)، `accounting/cash-ledger.html` (دفتر الصندوق والبنك — كشف حساب حيّ لحساب `11101`/`11102` باختيار، بنفس نمط `party-statement.html`)، `accounting/cash-count.html` (جرد الصندوق ومحضر فرق الصندوق، انظر صف `CashCounts` أعلاه)، `accounting/bank-reconciliation.html` (تسوية البنوك — إدخال يدوي فقط، بلا رفع ملف، انظر صف `BankStatementLines` أعلاه) | `chart-of-accounts-page.js`, `account-mappings-page.js`, `journal-entry-list-page.js`, `journal-entry-form-page.js`, `weekly-batches-page.js`, `cash-transfer-page.js`, `period-lock-page.js`, `fiscal-year-close-page.js`, `adjustments-page.js`, `cash-ledger-page.js`, `cash-count-page.js`, `bank-reconciliation-page.js` | `accounting-service.js` (شجرة حسابات هرمية + قيود يومية + `getCategoryAccounts`/`getCategoryAccountNames`/`getPostableAccounts`/`isAnimalSaleAccount` + `getAccountHierarchy`/`computeAccountSubtreeTotals`/`computeAccountOwnTotals` (للتقارير الهرمية في `accounting-reports-page.js`) + `computeAccountLedgerRows`) — تستهلكه أيضًا كل خدمات الترحيل التلقائي الأخرى (`expense-service.js`, `revenue-service.js`, `purchase-service.js`, `bulk-batch-service.js`, `custody-service.js`, `party-service.js`, `stock-count-service.js`, `adjustment-service.js`, `cash-count-service.js`, `bank-reconciliation-service.js`)؛ `period-lock-page.js` يستهلك `period-service.js` بدلاً منها (`getAllPeriodRecords`/`closePeriod`/`reopenPeriod`)، `fiscal-year-close-page.js` يستهلك `fiscal-year-service.js` (`computeFiscalYearClosingPreview`/`closeFiscalYear`/`reopenFiscalYear`/`getAllFiscalYearRecords`)، الذي يعتمد بدوره على `accounting-service.js` (`computeIncomeStatement`/`computeAccountBalance`/`createJournalEntry`/`deleteJournalEntry`) و`period-service.js` (`closePeriod`/`reopenPeriod`/`getPeriodRecord`) لقفل/فتح أشهر السنة تلقائيًا مع إقفالها، و`adjustments-page.js`/`cash-count-page.js` يستهلكان `adjustment-service.js` (`cash-count-service.js` يستدعي `createAdjustment`/`deleteAdjustment` منها مباشرة عند وجود فرق جرد) + `period-service.js` (`guardPeriodOpenForSave`)، و`bank-reconciliation-page.js` يستهلك `bank-reconciliation-service.js` فقط (لا علاقة له بالتسويات/قفل الفترات — بلا قيود محاسبية جديدة، مجرد مطابقة مرجعية). `guardPeriodOpenForSave()` من `period-service.js` مُستدعاة أيضًا من صفحات نماذج `Expenses`/`Revenues`/`Purchases`/`JournalEntries`/`cash-transfer`/`Adjustments`/`CashCounts` (انظر صف `AccountingPeriods`)؛ `weekly-batches-page.js` يستهلك `weekly-batch-service.js` (`createWeeklyBatch`/`previewWeeklyBatchRange`/`getWeeklyBatchDetails`/`deleteWeeklyBatch`) الذي يعتمد بدوره على `accounting-service.js` (`getAllJournalEntries`/`postJournalEntry`) و`period-service.js` (`isPeriodClosed`، بفحص صامت مطابق لـ`guardPeriodOpenForSave` لكن لكل قيد على حدة بلا Toast متكرر) |
| العملاء والموردون والشركاء (إدارة الذمم + جاري الشريك) | `parties/parties-hub.html` (مدخل الوحدة — رابط الشريط الجانبي `parties` يشير هنا مباشرة، بنفس نمط `custody-hub.html`: بطاقات عمليات + ملخّص سريع (عدد العملاء/الموردين/الشركاء، إجمالي الذمم المدينة/الدائنة، صافي جاري الشركاء) + تنبيهات حيّة)، `parties/parties.html` (CRUD بـ Modal لتكويد عميل/مورّد/شريك + عمود "الرصيد الحالي" الحيّ + شريط تنبيهات + أزرار "+ سند قبض"/"+ سند صرف"/"📄 كشف حساب"/"🪪 بطاقة" لكل صف)، `parties/customer-collection.html` (سند قبض من عميل: رقم تلقائي/تاريخ/عميل/طريقة (نقد-بنك-تحويل-شبكة)/مبلغ/بيان + سجل سندات سابقة append-only، زر مرفقات لكل صف)، `parties/supplier-payment.html` (نفس الشيء لسند صرف لمورّد)، `parties/partner-capital.html` (ضخ فلوس أو سحب فلوس من/إلى شريك — نموذج بحقل "نوع العملية" + سجل append-only لكل حركات جاري الشريك، انظر `createCapitalTransaction()` في صف `PartyTransactions` أعلاه)، `parties/party-statement.html` (كشف حساب زمني كامل لطرف واحد — عميل/مورّد/شريك: رصيد أول المدة/فواتير آجلة/سندات قبض أو صرف أو ضخ/سحب فلوس/مرتجعات وخصومات مع رصيد تراكمي، فلتر فترة، طباعة، تصدير Excel/PDF)، `parties/return-discount.html` (**صفحة إدارة كاملة مستقلة** لمرتجعات/خصومات العملاء والموردين معًا — فلتر نوع الطرف/الطرف/نوع العملية (مرتجع أو خصم)/تاريخ/مبلغ/بيان + تلميح أثر العملية على رصيد الطرف + سجل append-only بكل السندات السابقة، بنفس نمط `customer-collection.html`/`supplier-payment.html`؛ الوصول حصرًا عبر رابط القائمة الجانبية `parties-return-discount`، بعد أن كانت تُنشأ من نافذة منبثقة داخل `party-statement.html`)، `parties/party-card.html` (`?partyId=&type=` أو شبكة اختيار بمؤشر لوني 🟢🟡🔴 حسب الرصيد/التأخر — بيانات أساسية + KPIs (إجمالي مبيعات أو مشتريات/إجمالي تحصيل أو سداد/الرصيد الحالي/عدد الفواتير) + آخر عملية بيع أو شراء/تحصيل أو سداد + سجل زمني كامل بمرفقات لكل حركة، بنفس نمط `employee-wallet.html`، وفرع مخصص لملخّص/سجل جاري الشريك بدل `getPartyRegistrySummary` المقصورة على عميل/مورّد) | `parties-hub-page.js`, `parties-page.js`, `customer-collection-page.js`, `supplier-payment-page.js`, `partner-capital-page.js`, `party-statement-page.js`, `return-discount-page.js`, `party-card-page.js` | `party-service.js` (انظر تفصيل الأتمتة المحاسبية في صفَّي `Parties`/`PartyTransactions` أعلاه؛ يعتمد أيضًا على `revenue-service.js`/`purchase-service.js`/`accounting-service.js` — الأخير فقط للدوال الكاتبة `create*`/`_postParty*`، أما دوال القراءة الحيّة `computePartyBalance`/`getPartyRegistrySummary`/`computeDebtAging`/`isPartyOverdue`/`getPartyLedgerEntries` فبلا اعتماد على `accounting-service.js` إطلاقًا — صفحات القراءة فقط مثل `party-card.html`/تبويبات `party-reports.html` الجديدة لا تحمّله؛ `computePartyBalance`/`getPartyLedgerEntries` يعتمدان اختياريًا على `computeAssetCostBasis` من `fixed-asset-service.js` لدقة رصيد الشريك فقط — أي صفحة تعرض رصيد/كشف شريك يجب أن تحمّل `fixed-asset-service.js` أيضًا، وإلا يتدهور الحساب بأمان لتكلفة الشراء فقط بلا التكاليف الإضافية/التحسينات). تقرير أقدم منفصل (بلا مفهوم ذمم/آجل): `reports/party-reports.html` (`party-reports-page.js`) — 4 أوضاع الآن: "سجل عميل/مورّد واحد" و"أعلى العملاء/الموردين" (الأصليان، مجمّعان من `Revenues`/`Purchases`/بنود `BulkPurchaseBatches`/`BulkSaleBatches` معًا، غير متأثرين بمفهوم الآجل/الذمم إطلاقًا)، + **"سجل الأرصدة"** (جدول لكل الأطراف: الكود/الاسم/الجوال/عدد الفواتير/الإجمالي/المسدد/المتبقي/الحالة، بفلتر مدينون أو دائنون/متأخرون أو مستحق سدادهم عبر `getPartyRegistrySummary`+`isPartyOverdue`؛ نقر الصف ⇐ `party-card.html`) و**"أعمار الديون"** (تخصيص FIFO حقيقي عبر `computeDebtAging` — يبني قائمة أحداث زمنية موحّدة من الفواتير الآجلة + المرتجعات/الخصومات + التحصيل/السداد، ويستهلك كل حدث سالب أقدم دَين مفتوح أولاً، ثم يُصنَّف المتبقي في 5 أعمدة: لم يحن الاستحقاق/1-30/31-60/61-90/أكثر من 90 يومًا كما في تاريخ محدد؛ **أدق بكثير من التقريب المستخدم في `isPartyOverdue`** الذي يعتمد فقط "أقدم فاتورة + مدة السداد" لأغراض التنبيه السريع) |
| المخزون والمستلزمات | `inventory/inventory-items.html` (كتالوج الأصناف: الاسم/التصنيف/الوحدة/الرصيد الحالي/**آخر سعر شراء**/حد الطلب/الحالة، CRUD بـ Modal + تصدير Excel/PDF)، `inventory/inventory-movements.html` (حركات وارد/صادر: التاريخ/الصنف/الاتجاه/الكمية/**تكلفة الوحدة**/السبب/دفعة تسمين مرتبطة/**رقم القيد** (رابط لقيد المصروف/المشترى المصدر لحركة تلقائية، انظر `InventoryMovements` أعلاه)/ملاحظات — ⚠️ **بلا زر إضافة يدوية إطلاقًا** (`hasActionPermission('inventory','add')`/زر "+ تسجيل حركة جديدة" أُزيلا بالكامل من هذه الصفحة): كل حركة "وارد"/"صادر" حقيقية تُنشأ حصرًا تلقائيًا من `purchase-form-page.js`/`expense-form-page.js`، وكل حركة `reasonType:'adjustment'`/`'custody'` من `stock-count-service.js`/`custody-service.js`. `onRowClick` يحجب فتح نافذة التعديل تمامًا لأي حركة تحمل `relatedExpenseId`/`relatedPurchaseId` (Toast يوجّه المستخدم لتعديلها من سجلها المصدر بدلاً من ذلك) — تعديل/حذف عبر نافذة منبثقة يبقى متاحًا فقط للحركات **غير المرتبطة** (تسوية جرد/عهدة/سجلات يدوية قديمة من قبل هذا التغيير)، بتصدير Excel/PDF) — التنقل بينهما وبين `inventory/stock-counts.html` حصريًا عبر القائمة الجانبية (أُزيل شريط التبويبات العلوي المحلي السابق). تقاريرها منفصلة: `reports/inventory-reports.html` (`inventory-reports-page.js`) — فلتر صنف (`report-item`، افتراضي "كل الأصناف") + فلتر فترة (من/إلى) لعدد حركات الوارد/الصادر؛ اختيار صنف معيّن يقصر كل أقسام الصفحة (KPIs/رسم التوزيع/جدول الأرصدة/سجل الحركات) عليه وحده — بفلترة الحركات مبكرًا في `_renderInventoryReport()` قبل أي حساب لاحق، فينعكس تلقائيًا على الكل بلا تكرار الفلتر لكل قسم على حدة + جدول لكل صنف (وارد/صادر خلال الفترة، الرصيد **كما في تاريخ "إلى"** أو اليوم، وآخر سعر شراء — يجيب على "كم الكمية المتوفرة وبكام في أي وقت سابق") + سجل الحركات خلال الفترة (بتكلفة الوحدة لكل حركة + **رقم القيد**، نفس عمود `inventory-movements.html`/`InventoryMovements` أعلاه، بلا `event.stopPropagation()` لأن هذا الجدول بلا `onRowClick` أصلاً) + رسم توزيع حسب سبب الحركة. **زرّا التصدير هنا يصدّران الجدولين معًا في ملف واحد** عبر `exportSectionsToExcel`/`exportSectionsToPdf` (انظر `export-utils.js` أعلاه) — Excel: ورقتان "الرصيد لكل صنف" و"سجل الحركات"؛ PDF: قسمان متتاليان بعنوان فرعي لكل منهما | `inventory-item-list-page.js`, `inventory-movement-list-page.js`, `inventory-reports-page.js` | `inventory-service.js` (CRUD + `computeItemStockLevel(item, movements)` للرصيد الحي أو "كما في تاريخ" + `getLastPurchasePrice(item, movements)` + إشعار نقص مخزون تلقائي + `getInventoryMovementByExpenseId()`/`getInventoryMovementByPurchaseId()`). مدمجة أيضًا في `dashboard-page.js` (بطاقة KPI + تنبيهات سريعة)، `overview-report.html` (جدول "مستوى المخزون الحالي"، رصيد تراكمي بمعزل عن فلتر الفترة)، `expense-form-page.js` (ربط اختياري وارد أو صادر — شراء أو استهلاك — ينشئ حركة مخزون تلقائيًا من أي مصروف، انظر صف "المصروفات" أعلاه)، و`purchase-form-page.js` (ربط اختياري وارد فقط من أي مشترى، انظر صف "المشتريات" أعلاه). `inventory-movement-list-page.js` يحمّل أيضًا `expense-service.js`/`purchase-service.js`/`accounting-service.js` (بلا استخدامها للكتابة، فقط لبناء خريطة رقم القيد لعمود "رقم القيد") |
| 🏢 الأصول الثابتة (أراضٍ/مبانٍ/سيارات/معدات/أجهزة/أثاث — تسجيل/بطاقة/إهلاك/نقل/تحسينات/صيانة/بيع/استبعاد/جرد، كلها مكتملة) | `fixed-assets/assets-hub.html` (مدخل الوحدة، نمط `custody-hub.html`)، `fixed-assets/asset-list.html` (سجل الأصول، نمط 1 من نمطي الـCRUD + تصدير Excel/PDF)، `fixed-assets/asset-form.html` (تسجيل/تعديل أصل — إنشاء/تعديل يُرحِّل قيد الاقتناء تلقائيًا)، `fixed-assets/asset-card.html` (بطاقة أصل واحد: بيانات + KPIs (شاملة **إجمالي تكلفة الصيانة** الحيّ) + سجل زمني من `FixedAssetTransactions` + مرفقات `recordType: 'fixedAsset'` + شبكة عمليات تُخفى بالكامل لأصل مباع/مستبعد)، `fixed-assets/asset-depreciation-run.html` (تشغيل إهلاك شهر كامل يدويًا + تراجع)، `fixed-assets/asset-transfer.html`/`asset-improvement.html`/`asset-maintenance.html`/`asset-sale.html`/`asset-disposal.html` (عمليات تتطلب `?assetId=` إلزاميًا — تُفتَح حصرًا من أزرار بطاقة الأصل، بلا رابط شريط جانبي مستقل؛ `asset-maintenance.html` نسخة طبق الأصل من `asset-improvement.html` في الشكل، لكن قيدها يمس حساب مصروف لا حساب الأصل نفسه — انظر `FixedAssetTransactions` أعلاه)، `fixed-assets/asset-count.html` (جلسة جرد دورية لكل الأصول النشطة + سجل جلسات سابقة قابل للتفصيل بـ Modal) | `assets-hub-page.js`, `asset-list-page.js`, `asset-form-page.js`, `asset-card-page.js`, `asset-depreciation-run-page.js`, `asset-transfer-page.js`, `asset-improvement-page.js`, `asset-maintenance-page.js`, `asset-sale-page.js`, `asset-disposal-page.js`, `asset-count-page.js` | `fixed-asset-service.js` (انظر تفصيل الأتمتة المحاسبية في صفوف `FixedAssets`/`FixedAssetTransactions`/`FixedAssetCounts` أعلاه، بما فيها `createFixedAssetMaintenance()`). تقاريرها ضمن `reports/asset-reports.html` (`asset-reports-page.js`، 8 تبويبات: سجل الأصول/حسب النوع/حسب الموقع/مجمع الإهلاك/القيمة الدفترية/الإهلاك الشهري/المباعة والمستبعدة/تقرير الجرد — ⚠️ **لا تبويب مخصص للصيانة بعد**، سجلاتها تظهر فقط في السجل الزمني لكل أصل ضمن `asset-card.html`) |
| الموظفون | `employees/employee-list.html` (CRUD بـ Modal، الآن مع صورة/فرع/قسم — انظر صف `Employees` أعلاه) | `employee-list-page.js` | لا يوجد — يستخدم `dbGetAll/dbAdd/dbUpdate('Employees', …)` مباشرة |
| إدارة العهد | `custody/custody-hub.html` (مدخل الوحدة — رابط الشريط الجانبي `custody` يشير هنا مباشرة؛ 7 بطاقات + تنبيهات حيّة + حقل "حد التنبيه المالي" محفوظ في `localStorage['custodyAlertThreshold']`)، `custody/custody-issue.html` (إصدار عهدة نقدية أو عينية)، `custody/custody-liquidate.html` (**تصفية عهدة** — الإغلاق الطبيعي المتوقّع: فواتير معتمدة و/أو نقد مرتجع معًا لعهدة نقدية بنفس المرجع `voucherNumber`، أو رد أصناف كاملًا/جزئيًا لعهدة عينية؛ يقبل `?custodyId=`)، `custody/custody-settle.html` (**تسوية عهدة** — معالجة استثناءات/فروقات فقط: تحويل لمصروف/خصم راتب/تحميل على الموظف/نقل أو تسوية رصيد لعهدة أخرى، و(مدير النظام فقط) إعدام أو فاقد/إلغاء بقرار الإدارة؛ يقبل `?custodyId=` أو اختيار موظف/عهدة يدويًا؛ خيارات نوع التسوية تُفلتَر حسب نقدية/عينية ودور المستخدم — ⚠️ منذ فصل التصفية عن التسوية لم تعد هذه الشاشة تعرض رد نقدية/رد أصناف/تسوية بفواتير، تلك حصرًا في `custody-liquidate.html`)، `custody/custody-list.html` (سجل كل العهد + ملخّص أرصدة حسب الموظف؛ صف → نافذة تفاصيل بها أزرار تصفية/تسوية/إلغاء/مرفقات، زر مرفقات لكل صف بـ`recordType: 'custody'`)، `custody/employee-statement.html` (كشف حساب موظف واحد بشكل بنكي: تاريخ/**مرجع**/بيان/مدين/دائن/رصيد جارٍ، طباعة/تصدير)، `custody/employee-wallet.html` (`?employeeId=` أو شبكة اختيار — محفظة بمؤشر لوني 🟢🟡🔴 وسجل زمني كامل (يعرض `voucherNumber` ضمن تفاصيل كل حدث تسوية)، مرفقات لكل حركة بـ`recordType: 'custody'`/`'custodySettlement'`) | `custody-hub-page.js`, `custody-issue-page.js`, `custody-liquidate-page.js`, `custody-settle-page.js`, `custody-list-page.js`, `employee-statement-page.js`, `employee-wallet-page.js` | `custody-service.js` (CRUD + `createCustodyIssue`/`settleCustody`/`cancelCustodyItem`/`transferCustodyBalance`/`generateNextCustodyVoucherNumber` + `getEmployeeCustodySummary` — انظر تفاصيل الأتمتة المحاسبية في صفَّي `CustodyItems`/`CustodySettlements` أعلاه). تقاريرها ضمن `reports/custody-reports.html` (انظر صف "التقارير" أدناه، غير متأثرة بفصل التصفية/التسوية — تعمل على مستوى `CustodyItems` فقط) |
| مركز التنبيهات | `notifications/alerts-center.html` (رابط شريط جانبي علوي مستقل، `module: 'notifications'` — الصفحة الوحيدة التي تستهلك هذه الصلاحية بعد إزالة صفحة "الإشعارات" القديمة). عشر فئات مجمّعة (انظر قسم "التنبيهات" أدناه) — حقل حد تنبيه انخفاض الصندوق/البنك (`localStorage['cashAlertThreshold']`) انتقل إلى قسم "🔔 تنبيهات القطيع" في `settings/settings.html` | `alerts-center-page.js` | `alerts-service.js` (`computeAllAlerts()` — نفس المصدر تستهلكه أيضًا "تنبيهات سريعة" بلوحة التحكم) |
| التقارير | `reports/herd-reports.html` (تبويبات: إحصائيات/مواليد/مواليد حسب الأم/نفوق/حسب النوع/حسب السلالة)، `reports/health-reports.html` (تبويبات: إحصائيات عامة/السجلات خلال فترة/حسب الحيوان/حسب الطبيب البيطري — الإحصائيات العامة تخلط بين عدد سجلات مفلتر بالفترة وأعداد حيوانات لحظية حالية، انظر ملاحظة `resultingHealthStatus` في جدول "نموذج البيانات" أعلاه)، `reports/revenue-reports.html`، `reports/expense-reports.html`، `reports/purchase-reports.html` — الثلاثة الأخيرة فيها وضعان: "تقرير فترة واحدة" (الافتراضي) و"مقارنة بين فترتين" (بند/نوع محدد أو الكل، مع فترتين تاريخ منفصلتين وفرق/نسبة تغيّر). ⚠️ **`expense-reports.html`/`purchase-reports.html` تحديدًا**: وضع "تقرير فترة واحدة" في كليهما فيه أيضًا فلتر بند مستقل (`report-category` للمصروفات، `period-category` للمشتريات — منفصل في الاثنين عن فلتر بند وضع المقارنة `compare-category`) — "كل البنود" (الافتراضي) يعرض جدول "مقارنة حسب البند" المعتاد (صف لكل بند + رسم بياني)؛ اختيار بند معيّن يستبدله بجدول **سجلات تفصيلية** لكل معاملة على حدة (`_renderExpenseCategoryRecords()`/`_renderPurchaseCategoryRecords()`، نسخة طبق الأصل من بعضهما): التاريخ/**البيان** (`notes` إن وُجدت، وإلا `vendor`، وإلا اسم البند نفسه)/المبلغ/**رقم القيد** (رابط لقيد المصروف أو المشترى، نفس نمط عمود "رقم القيد" في `revenue-list-page.js`؛ يتطلب تحميل `accounting-service.js` لـ`getAllJournalEntries()`) — نقر الصف يفتح `expense-form.html?id=`/`purchase-form.html?id=` مباشرة للتعديل. نفس النمط قابل للتكرار على `revenue-reports.html` عند الطلب. `reports/sales-report.html` (`sales-report-page.js` — قسمان: (1) "سجل المبيعات" (المبيعات الموحّدة) — بيع حيوان فردي (`Revenues.animalId`) + بيع جملة (`BulkSaleBatches.lines`، مستبعدًا `source:'transfer'` وأسطر السعر صفر) في جدول واحد، مفلتر بفترة من/إلى، **بعمودي تكلفة/ربح لكل سطر** (`_animalCostBreakdown()` لتكلفة الحيوان المباع فرديًا بنفس منطق قسم "تكلفة الرأس" أدناه، أو `line.cogsAmount`/`avgCostAtSale` المحفوظتان وقت البيع لسطر الجملة — انظر `BulkSaleBatches` في جدول "نموذج البيانات") تُصدَّر مع باقي الأعمدة عبر Excel/PDF؛ (2) "تكلفة الرأس" — **رصيد تراكمي دائمًا لكل حيوان بمعزل عن فلتر الفترة، لكن قابل للفلترة بحالة الحيوان** (قائمة `#cost-status-filter`: الكل/حي/مباع/نافق/تحويل لمخزون تجارة، منفصلة عن فلتر الفترة الخاص بقسم المبيعات): تكلفة الاقتناء (`Animals.purchasePrice`، صفر للمولود) + نصيبه من تكلفة العلف/الأدوية الجماعية لأي دفعة تسمين دخلها (مجموع `allocatedCost` من `computeFatteningBatchResults()` عبر كل `FatteningBatches` التي كان ضمنها، بتمرير `deathRecords` فتتوقف تلقائيًا عند تاريخ نفوقه لو نفق وهو لسه بالدفعة — انظر صف `FatteningBatches` أعلاه؛ حيوان لم يدخل أي دفعة تسمين لا يظهر له نصيب) + تكلفة فردية مباشرة (`HealthRecords.cost` + `Expenses.linkedAnimalId` معًا عبر `getAnimalLinkedExpensesTotal()`، كل السجلات بلا حصر بفترة). ثلاث حالات للربح/الخسارة: **مباع** (`Revenues.animalId` مطابق موجود) → ربح/خسارة حقيقي (سعر البيع − إجمالي التكلفة) + **نسبة الربح من الإيراد %** (`الربح ÷ سعر البيع × 100`)؛ **نافق بلا بيع** (`Animals.status === 'dead'` وبلا إيراد بيع مطابق) → **خسارة كاملة** = إجمالي التكلفة بالسالب (بلا نسبة من إيراد، لأن الإيراد صفر)، معروضة بوسم "(خسارة نفوق)"، وتُجمَّع في KPI منفصلين ("عدد حالات النفوق المحتسبة"/"إجمالي خسائر النفوق") بمعزل عن "متوسط ربح الرأس المباع" حتى لا تُشوِّه متوسط أرباح المبيعات الفعلية؛ **حي/محوَّل لمخزون تجارة بلا بيع** → لا ربح/خسارة محسومة بعد (`-`))، `reports/party-reports.html` (كشف حساب عميل/مورّد واحد + ترتيب أعلى العملاء/الموردين، انظر صف "العملاء والموردون" أعلاه)، `reports/inventory-reports.html` (فترة + رصيد "كما في تاريخ"، انظر صف "المخزون والمستلزمات" أعلاه)، `reports/asset-reports.html` (`asset-reports-page.js` — 8 تبويبات: سجل الأصول/حسب النوع/حسب الموقع/مجمع الإهلاك/القيمة الدفترية/الإهلاك الشهري (بفلتر شهر)/المباعة والمستبعدة (ربح أو خسارة كل عملية)/تقرير الجرد (آخر نتيجة جرد لكل أصل نشط، أو "لم يُجرد بعد")، انظر صف "🏢 الأصول الثابتة" أعلاه)، و`reports/overview-report.html` (تقرير شامل بلا تبويبات فرعية: ملخّص لحظي للقطيع الحي + حركة مواليد/نفوق **وشراء/بيع فردي للقطيع** (`purchasedAnimalsInPeriod`: `Animals.source === 'purchased'` بتاريخ اقتناء `birthDate` ضمن الفترة؛ `soldAnimalsInPeriod`: `Revenues.animalId` بتاريخ ضمن الفترة — مستقلان تمامًا عن الشراء والبيع الجماعي أدناه) بصافي تغيّر محسوب (`netHerdChange = (مواليد + شراء فردي) − (نفوق + بيع فردي)`، KPI بعلامة +/− حسب الاتجاه) وماليات إيرادات/مصروفات/مشتريات **و الجملة** (تكلفة شراء الجملة إعلامية فقط هنا لأنها تُرسمَل كأصل، وتكلفة البضاعة المباعة عند البيع هي ما يدخل صافي الربح فعليًا؛ كل عملية شراء/بيع تُفلتر بتاريخها الموحّد الخاص بها) مفلترة بفترة واحدة من/إلى + **"ملخّص الضريبة"** (`_computeTaxSummaryRow()` في `overview-report-page.js`، فواتير `hasTaxInvoice: true` فقط من `Revenues`/`Purchases`/`Expenses` خلال نفس فلتر الفترة — لا `BulkPurchaseBatches`/`BulkSaleBatches`، بلا حقول فاتورة ضريبية أصلًا): جدول بثلاث صفوف (المبيعات/المشتريات/المصروفات) بعدد الفواتير الضريبية/المبلغ غير شامل الضريبة/قيمة الضريبة/المبلغ شامل الضريبة لكل مصدر + صف إجمالي، مع 3 بطاقات KPI فوقه: ضريبة المبيعات المُحصَّلة، ضريبة المشتريات والمصروفات المدفوعة، و**صافي الضريبة المستحقة أو المستردة** (`ضريبة المبيعات − ضريبة المشتريات − ضريبة المصروفات`، نفس مبدأ إقرار ضريبة القيمة المضافة المعتاد — موجب = مستحق للمصلحة، سالب = مسترد)، بالإضافة لجدولي "الرؤوس المتبقية من الشراء والبيع الجماعي" و"مستوى المخزون الحالي" (كل صنف مخزون ورصيده الحي عبر `computeItemStockLevel`) مفصّلين — كلا الجدولين **رصيد تراكمي دائمًا** (بلا فلترة فترة، بنفس مبدأ "القطيع الحالي" اللحظي) وليسا مقيّدين بـ`report-from`/`report-to` كبقية أقسام الصفحة. **قسم "الأصول الثابتة"** (يحمّل `fixed-asset-service.js` لهذا الغرض تحديدًا): KPI عدد الأصول النشطة/إجمالي القيمة الدفترية الحالية (لحظيان دومًا، `computeAssetBookValue`، بنفس مبدأ "القطيع الحالي") + تكلفة أصول مُقتناة/إجمالي الإهلاك المُرحَّل (كلاهما ضمن الفترة المحددة تحديدًا — `FixedAssets.purchaseDate` للاقتناء، `FixedAssetTransactions` بنوع `'depreciation'` للإهلاك) وتحتها جدول "حركات الأصول الثابتة خلال الفترة" (كل `FixedAssetTransactions` ضمن الفترة بأنواعها الستة معًا — إهلاك/تحسين/صيانة/نقل/بيع/استبعاد — بعمود التاريخ/الأصل/نوع الحركة/المبلغ/ملاحظات، بلا رابط لقيدها المحاسبي، بعكس تبويبات `asset-reports.html` المخصصة). مع جدول ملخّص قابل للتصدير يدمج كل ما سبق (بما فيه كل حركة أصل كسطر مستقل))، `reports/custody-reports.html` (`custody-reports-page.js` — 9 تبويبات داخلية `.report-tab[data-tab]` على نفس مجموعة `CustodyItems`: جميع العهد/المفتوحة/المتأخرة/حسب الموظف/حسب نوع العهدة/حسب الفترة/عهد المعدات (كل الأنواع العينية)/العهد النقدية/الأرصدة المستحقة)، و`reports/fattening-reports.html` (`fattening-reports-page.js` — بلا تبويبات فرعية: فلتر دفعة واحدة/الكل + حالة نشطة/مغلقة/الكل، KPI موجز، ثم قسمان دائمًا معًا — "ملخص الدفعات" (تكلفة العلف/الأدوية المشتركة، التكلفة الفردية المباشرة (صحية + مصروفات مربوطة مباشرة) مجمّعة، إجمالي التكلفة، الزيادة في الوزن، متوسط تكلفة الكيلو، المبيعات والربح لكل دفعة) و"تفصيل كل حيوان" (نفس الأعمدة على مستوى الحيوان الواحد، بنفس حقول `computeFatteningBatchResults()` في `fattening-service.js` — انظر صف `FatteningBatches` أعلاه)، مُصدَّران معًا بضغطة واحدة عبر `exportSectionsToExcel`/`exportSectionsToPdf` بنفس نمط `inventory-reports.html`). التنقل بين كل صفحات التقارير حصريًا عبر القائمة الجانبية (أُزيل شريط التبويبات العلوي المشترك السابق) — الشريط الوحيد الباقي بكلاس `.report-tab` هو شريط التبويبات **الداخلية** لكل صفحة (`.report-tab[data-tab]`/`[data-mode]`: وضعية الفترة/المقارنة أو تبويبات الأقسام)، وأي كود جديد يتلاعب بـ`.report-tab` في هذه الصفحات يتعامل مع التبويبات الداخلية فقط | `herd-reports-page.js`, `health-reports-page.js`, `revenue-reports-page.js`, `expense-reports-page.js`, `purchase-reports-page.js`, `sales-report-page.js`, `party-reports-page.js`, `inventory-reports-page.js`, `asset-reports-page.js`, `overview-report-page.js`, `custody-reports-page.js`, `fattening-reports-page.js` | تُعيد استخدام خدمات القطيع/السجل الصحي/الإيرادات/المصروفات/المشتريات (`overview-report-page.js` وحدها تضيف أيضًا `bulk-batch-service.js` و`inventory-service.js` لقسمي الجملة والمخزون، و`fixed-asset-service.js` لقسم الأصول الثابتة؛ `sales-report-page.js` تضيف `bulk-batch-service.js` (قسم المبيعات الموحّدة) + `expense-service.js`/`weight-service.js`/`inventory-service.js`/`death-service.js`/`fattening-service.js` معًا (لحساب نصيب كل حيوان من تكلفة دفعات التسمين، متوقفًا عند تاريخ النفوق إن وُجد، وحساب خسارة النفوق الكاملة في قسم تكلفة الرأس)؛ `party-reports-page.js` تضيف `party-service.js`؛ `inventory-reports-page.js` تستخدم `inventory-service.js` فقط؛ `asset-reports-page.js` تستخدم `fixed-asset-service.js` فقط؛ `custody-reports-page.js` تستخدم `custody-service.js` فقط؛ `fattening-reports-page.js` تضيف `death-service.js` مع `fattening-service.js` (نفس منطق توقف تكلفة العلف عند تاريخ النفوق)) |
| الإعدادات | `settings/settings.html`، `settings/audit-log.html` (سجل التدقيق الشامل التلقائي — مدير النظام فقط، انظر شرح `AuditLog`/`_logAudit()` أعلاه) | `settings-page.js`, `audit-log-page.js` | تصدير/استيراد نسخة احتياطية كاملة (JSON لكل المخازن) + تصفير `AlGhazeezFarmDB` بالكامل + إدارة المستخدمين والصلاحيات (مدير النظام فقط، عبر `dbAdd/dbUpdate('Users', …)` مباشرة، لا خدمة منفصلة) + قسم "🔔 تنبيهات القطيع" (حد تنبيه انخفاض رصيد الصندوق/البنك `localStorage['cashAlertThreshold']` عبر `getCashAlertThreshold`/`setCashAlertThreshold` — نُقل من مركز التنبيهات إلى هنا؛ + تفضيلات الفئات 11/12/13/14 الاختيارية في مركز التنبيهات — بلوغ السن/إحصائية المواليد/إحصائية الوفيات/تغيّر عدد القطيع، انظر قسم "التنبيهات" أدناه — عبر `getAlertsPreferences`/`saveAlertsPreferences`/`ALERT_AGE_GROUPS` من `alerts-service.js`، كلها بحفظ واحد مشترك)؛ `audit-log-page.js` يستهلك `audit-service.js` (قراءة فقط) |

⚠️ **عمود "رقم القيد" القابل للنقر (يفتح `accounting/journal-entry-form.html?id=`)**: مُضاف حيثما كان صف التقرير = عملية واحدة بقيد محاسبي مباشر (نفس نمط `party-statement.html`، انظر صف `AccountMappings`/`Parties` أعلاه) — `reports/sales-report.html` (قسم "سجل المبيعات" فقط: `Revenues.journalEntryId` للبيع الفردي، `BulkSaleBatches.journalEntryId` للبيع الجماعي؛ "تكلفة الرأس" مستثنى، رصيد تراكمي بلا قيد واحد)، `reports/custody-reports.html` (تبويبات التفصيل الستة: `CustodyItems.journalEntryId` — قيد الإصدار فقط لا التسويات؛ التبويبات المجمّعة الثلاثة مستثناة)، و`reports/asset-reports.html` (تبويبات "سجل الأصول/الإهلاك/القيمة الدفترية" بـ`FixedAssets.journalEntryId`، "الإهلاك الشهري"/"المباعة والمستبعدة" بـ`FixedAssetTransactions.journalEntryId` للحركة نفسها؛ التبويبات المجمّعة و"تقرير الجرد" مستثناة). **لم يُضَف** لتقارير المصروفات/الإيرادات/المشتريات (`expense-reports.html`/`revenue-reports.html`/`purchase-reports.html`) عمدًا — صفوفها مجمّعة حسب البند (لا معاملة واحدة لكل صف)، فـ"رقم القيد" بلا معنى هناك. **أُضيف بدلاً منه فعليًا في `revenues/revenue-list.html`** (`revenue-list-page.js`، رابط `<a>` داخل خلية "رقم القيد" بـ`event.stopPropagation()` بدل `onRowClick` على مستوى الجدول — نقر الصف نفسه مخصّص لفتح نموذج تعديل الإيراد `revenue-form.html?id=`، فلا تعارض؛ نفس نمط زر "📎 مرفقات" الموجود أصلاً في نفس الجدول)، **وبنفس النمط بالضبط في `purchases/purchase-list.html`** (`purchase-list-page.js`) لاحقًا — عمود إضافي **"تكلفة الوحدة"** بجانبه هناك أيضًا (`amount / linkedQuantity` لمشترى مرتبط بصنف مخزون، نفس حساب `_syncPurchaseInventoryLink()` في `purchase-form-page.js`، بلا استعلام إضافي عن حركة المخزون نفسها؛ '-' لمشترى غير مرتبط بمخزون)، **وأخيرًا في `expenses/expense-list.html`** (`expense-list-page.js`، رقم القيد فقط بلا تكلفة وحدة إذ لا مفهوم كمية/صنف في `Expenses` نفسها) — الثلاثة الآن بنفس النمط (`event.stopPropagation()` على رابط `<a>` داخل خلية "رقم القيد"، `onRowClick` على مستوى الجدول يفتح نموذج التعديل).

## التنبيهات (مركز التنبيهات الحيّ)
⚠️ **كان يوجد سابقًا نظامان متوازيان**: مخزن `Notifications` دائم (يُغذّى عبر `createNotification()` من 10 نقاط مختلفة في الكود: صحة حيوان، نفوق، ماليات معلّقة، عهد، مخزون، ذمم، تحصين قادم، قرب ولادة) تُعرضه صفحة `notifications/notifications.html`، إلى جانب مركز التنبيهات الحي أدناه. **أُزيلت صفحة الإشعارات القديمة وخدمتها وكل نقاط `createNotification()` بالكامل** (الاعتماد الآن حصريًا على الحساب الحي)، فلم يعد هناك سوى آلية واحدة. تنبيه واحد فقدته هذه الإزالة بلا بديل مباشر: "حيوان مريض/تحت علاج" — لم يعد يُنشئ أي تنبيه فعلي (يبقى ظاهرًا فقط كعدد ثابت ضمن بطاقات KPI بلوحة التحكم ووحدة القطيع، بلا إشعار مستقل).

`computeAllAlerts()` في `js/services/alerts-service.js` هي **المصدر الوحيد** لمنطق حساب التنبيهات — تُستهلك من مكانين يعرضان نفس البيانات بعمقين مختلفين:
- `dashboard-page.js` → `renderQuickAlerts()` (بطاقة "تنبيهات سريعة"): أول 6 فقط، مع رابط "عرض كل التنبيهات ←" إلى مركز التنبيهات.
- `notifications/alerts-center.html` (`alerts-center-page.js`, `requireAuth('notifications')`): كل التنبيهات مجمّعة في بطاقات حسب الفئة (`ALERT_CATEGORY_META`/`ALERT_CATEGORY_ORDER`) + ملخّص KPI بعدد كل درجة خطورة (`red`/`warning`/`blue`) + عدد غير المقروء + زر "✓ تعليم الكل كمقروء" وزر "✓ مقروء" لكل تنبيه على حدة (حقل حد تنبيه الصندوق/البنك `localStorage['cashAlertThreshold']` لم يعد هنا — انتقل لصفحة الإعدادات، انظر أدناه).

⚠️ **حقل `date`**: كل تنبيه يحمل أيضًا `date` (ISO، معروض عبر `formatDateArabic()` بجانب نص كل تنبيه في كلا الموضعين) — تاريخ الحدث الفعلي المرتبط به إن وُجد (`nextDueDate`/`expectedBirthDate`/`deathDate`/`expectedReturnDate` للفئات 1-5)، وإلا تاريخ اليوم (`todayIso()`) للفئات التجميعية التي لا ترتبط بسجل تاريخ واحد بعينه (نفاد الأعلاف، الأرصدة/الائتمان/الصندوق، الاعتماد، والفئات الأربع الاختيارية 11-14 كلها).

⚠️ **تتبّع "مقروء/غير مقروء"**: كل تنبيه يحمل `id` ثابت مبني من الفئة + مفتاح مميّز (معرّف سجل/طرف/مفتاح تجميع، أو العدد نفسه لفئة `herdCountChange` أدناه) و`isRead` (محسوبة من `localStorage['alertsReadIds']` عبر `getReadAlertIds()`/`markAlertAsRead(id)`/`markAllAlertsAsRead(ids)` في `alerts-service.js`؛ تنظيف تلقائي لأي id لم يعد ضمن التنبيهات الحيّة الحالية يحدث داخل `computeAllAlerts()` نفسها، يمنع تضخّم القائمة بلا حدود). `computeAllAlerts()` نفسها **تُعيد كل التنبيهات دومًا** (مقروءة وغير مقروءة معًا، فيما عدا `herdCountChange` المشروحة أدناه) مرتّبة: غير المقروء أولاً، ثم severity، ثم ترتيب الفئة — القرار بإخفاء المقروء أو لا هو مسؤولية كل مستهلك (طبقة عرض، لا طبقة بيانات):
- `alerts-center-page.js`: **تُخفي المقروء افتراضيًا** (checkbox "عرض المقروءة أيضًا" لإظهارها عند الحاجة، `_showRead` — مطفأ افتراضيًا) — بنفس منطق صناديق الوارد المعتادة (Gmail/WhatsApp): "تعليم كمقروء" يعني اختفاء فوري من القائمة، لا مجرد تعتيمه مع بقائه ظاهرًا (كان هذا السلوك الأول قبل التصحيح، وسبب التباس "الزر ما بيأثرش"). زر "✓ تعليم الكل كمقروء" يُطبَّق على التنبيهات **المعروضة فعليًا وقت الضغط** فقط (غير المقروءة، أو الكل لو الـtoggle مفعّل) لا كل تنبيه محسوب في آخر تحديث حتى لو كان مخفيًا أصلاً.
- `dashboard-page.js` → `renderQuickAlerts()`: تُفلتر `!isRead` أيضًا قبل أخذ أول 6 (بنفس المبدأ).
- جرس الهيدر (`header.js`, `refreshAlertsBadge()`): عدد التنبيهات غير المقروءة (`!isRead`) كشارة حمراء حيّة — انظر قسم `sidebar.js`/`header.js` أعلاه.

فئة `herdCountChange` تحديدًا استثناء على مستوى **الخدمة نفسها** (لا طبقة العرض): `computeAllAlerts()` تستبعدها من نتيجتها نهائيًا بمجرد قراءتها (بعكس بقية الفئات التي تبقى ضمن النتيجة الكاملة بـ`isRead: true` وتُخفيها كل صفحة بنفسها) — انظر شرحها في الفئة 14 أدناه؛ لذلك تبقى مخفية حتى لو فعّل المستخدم "عرض المقروءة أيضًا" في مركز التنبيهات، وتعود للظهور فقط عند تغيّر العدد.

الفئات العشر الأصلية (بالترتيب الثابت في `ALERT_CATEGORY_ORDER`)، كل فئة برابط `href` (عبر `getRootPath()`) لصفحتها المصدر:
1. **نفاد الأعلاف** — أصناف `InventoryItems.category === 'fodder'` وصلت لحد الطلب (`computeItemStockLevel`)؛ أحمر إن `currentQty <= 0` فعليًا، تحذيري غير ذلك. ⚠️ مقصورة على تصنيف "أعلاف" فقط بقرار عمل صريح — نقص أصناف أخرى (أدوية/معدات) لا يظهر هنا (لا يزال محسوبًا ضمن بطاقة KPI "أصناف تحت حد الطلب" في `dashboard-page.js` بشكل منفصل).
2. **قرب التحصين** — `Vaccinations.nextDueDate` خلال 7 أيام/متجاوز، لحيوان حي.
3. **قرب الولادة** — `PregnancyRecords` (`status: 'pregnant'`) خلال 14 يومًا/متجاوز.
4. **وجود نفوق** — `DeathRecords` بتاريخ خلال آخر 5 أيام.
5. **عهد متأخرة** — `CustodyItems` مفتوحة/جزئية بحالة حيّة `getEffectiveCustodyStatus() === 'overdue'`.
6/7. **عملاء متأخرون / موردون مستحقون** — `isPartyOverdue()` من `party-service.js`، مصنَّفة حسب `party.type`.
8. **تجاوز الحد الائتماني** — عملاء فقط، `computePartyBalance() > creditLimit`.
9. **انخفاض رصيد الصندوق/البنك** — يقارن رصيد حسابي `11101`/`11102` (`computeAccountBalance`) بـ`localStorage['cashAlertThreshold']` (يُضبط من قسم "🔔 تنبيهات القطيع" في `settings/settings.html`، انظر أدناه)؛ **معطّلة كليًا** إن لم يُحدَّد حد (0/فارغ).
10. **عمليات تحتاج اعتماد** — `Expenses`/`Purchases` بحالة `pending` (نفس منطق التنبيهات السريعة الأصلي).

⚠️ **3 فئات إضافية اختيارية** (مُلحَقة بنهاية `ALERT_CATEGORY_ORDER`، بلا تغيير في ترتيب/فهرسة العشرة أعلاه)، تُفعَّل وتُضبط حصرًا من قسم **"🔔 تنبيهات القطيع"** في `settings/settings.html` (`settings-page.js`، بلا قيد صلاحية `systemAdmin` — تفضيل شخصي، نفس القسم الذي يضم أيضًا حقل `cashAlertThreshold` أعلاه) وتُحفظ في `localStorage['alertsPreferences']` عبر `getAlertsPreferences()`/`saveAlertsPreferences()` (`alerts-service.js`، القراءة الوحيدة الصحيحة لهذا المفتاح؛ غياب/تعطيل أي بند = تلك الفئة الفرعية معطّلة، نفس اتفاقية `cashAlertThreshold`):
11. **بلوغ السن المحدد** — أربع تركيبات نوع/جنس مستقلة (`ALERT_AGE_GROUPS`: أغنام ذكور/إناث، ماعز ذكور/إناث)، كل واحدة بمفتاح تفعيل + سن بالأشهر خاص بها. لكل تركيبة مفعّلة، تنبيه واحد بعدد الحيوانات الحية من `Animals` (`type`+`gender` مطابقين) التي بلغ عمرها (`calculateAgeInMonths(birthDate)` في `formatters.js` — نفس منطق `calculateAgeLabel` الموجودة لكن يُعيد رقم أشهر بدل نص) السن المحدد أو تجاوزه.
12. **إحصائية المواليد** — مفتاحا تفعيل مستقلان: مجموع `BirthRecords.offspringCount` خلال الشهر الحالي (من أول الشهر)، ومجموعها خلال آخر 3 أشهر تقويمية؛ كل واحد تنبيه منفصل إن كان المجموع > 0.
13. **إحصائية الوفيات** — مفتاحا تفعيل مستقلان (بنفس نمط فئة 12): عدد `DeathRecords` بتاريخ خلال الشهر الحالي، وعددها خلال آخر 3 أشهر تقويمية — منفصلة عمدًا عن فئة "وجود نفوق" رقم 4 أعلاه (التي تبقى ثابتة، غير قابلة للتعطيل، ومحصورة بآخر 5 أيام لكل حالة على حدة بلا تجميع شهري).
14. **تغيّر عدد القطيع** — مفتاح تفعيل واحد (`herdCountChange`). لكل تركيبة من `ALERT_AGE_GROUPS` الأربع، تنبيه بالعدد الحيّ الحالي (`Animals.status === 'alive'`) إن كان > 0، بـ `id` يتضمن العدد نفسه (`herdCountChange:${key}:${count}`) — فمولود جديد/شراء (يزيد العدد) أو بيع/نفوق (ينقصه) يُنتج `id` مختلفًا لم يُقرأ من قبل فيظهر تلقائيًا كتنبيه غير مقروء جديد، بلا أي مقارنة/لقطة (snapshot) يدوية محفوظة بمعزل عن آلية القراءة نفسها. ⚠️ الفئة الوحيدة التي **تختفي بالكامل عند تعليمها مقروءة** (بدل البقاء ظاهرة بأولوية أقل كبقية الفئات) — إشعار لحظي بفرق العدد لا حالة دائمة يجب عرضها باستمرار؛ تعود للظهور فقط إن تغيّر العدد مجددًا. `births-page.js`/`deaths-page.js` يستدعيان `refreshAlertsBadge()` (`header.js`) مباشرة بعد نجاح الحفظ لتحديث شارة الجرس فورًا بلا انتظار تنقّل لصفحة أخرى (الصفحتان لا تُعيدان التوجيه بعد الحفظ، بعكس نماذج القطيع/الإيرادات التي تنتقل لصفحة القائمة خلال 400ms فيُعاد حساب الشارة تلقائيًا مع تحميل الصفحة الجديدة أصلاً).

## ملاحظات المستخدم
_(هذا القسم لصاحب المشروع فقط — أضف/عدّل هنا مباشرة في أي وقت. أي تحديث لاحق لهذا الملف من Claude يحافظ على هذا القسم كما هو دون حذف أو إعادة كتابة.)_

-
