# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## كيفية استخدام هذا الملف
اقرأ هذا الملف أولاً في أي محادثة جديدة حول هذا المشروع — يكفي عادة لفهم البنية الكاملة دون قراءة كل الملفات من جديد. ارجع لقراءة ملف معيّن فقط عند الحاجة لتفاصيله الدقيقة قبل تعديله مباشرة.

قسم **"ملاحظات المستخدم"** في نهاية هذا الملف مخصص لصاحب المشروع للتعديل اليدوي مباشرة، بمعزل عن أي اقتراحات. **أي تحديث لاحق لهذا الملف (من Claude) يجب أن يحافظ على ذلك القسم كما هو، ولا يعيد كتابته أو يحذفه.**

## نظرة عامة على المشروع
**مزرعة الغزيز (Al-Ghazeez Farm)** — تطبيق ويب لإدارة مزرعة أغنام وماعز: القطيع، الولادات، المصروفات، الإيرادات، الموظفون، العهد النقدية، الإشعارات، التقارير، الإعدادات.

- عربي بالكامل، RTL (`dir="rtl"` في كل صفحة)، خط Cairo من Google Fonts.
- تطبيق **من جهة العميل فقط (client-side)** بالكامل — لا يوجد أي خادم أو باك-إند أو API.
- لا يوجد `package.json`، لا أدوات بناء (build)، لا bundler، لا linter، لا اختبارات تلقائية. الكود ملفات HTML/CSS/JS ثابتة تُحمَّل عبر وسوم `<script>` عادية (دوال/ثوابت عامة على `window`، بدون ES modules).
- سجل التقدم الحالي للمشروع (بالعربي، يُحدَّث يدويًا) موجود في `PROJECT_PROGRESS.md` — راجعه لمعرفة آخر حالة/خطوة بدل الافتراض من هذا الملف.

## التشغيل
لا يوجد أي أمر بناء أو تشغيل عبر npm/أدوات مشابهة:
- افتح `index.html` مباشرة في المتصفح، أو (أفضل) قدّمه عبر خادم استضافة ثابت محلي بسيط (مثل `npx serve .` أو `python -m http.server`) بدل فتحه كـ `file://` مباشرة، تفاديًا لأي تضارب محتمل في IndexedDB أو تحميل خط Cairo/Chart.js من CDN بدون اتصال.
- حسابان افتراضيان (role: `manager` و role: `owner`) يُنشآن تلقائيًا عند أول تشغيل عبر `js/db/seed.js` — الاسم وكلمة المرور مكتوبان في ذلك الملف. لا تنسخ قيمهما إلى هذا الملف أو أي توثيق آخر (المستودع قد يُشارك). `seed.js` **لا يدرج أي بيانات تشغيلية تجريبية** (قطيع/مصروفات/إيرادات) — قاعدة البيانات تبدأ نظيفة عدا الحسابين وبنود التكويدات الافتراضية.
- لإعادة ضبط البيانات: زر "حذف البيانات وإعادة البداية" في صفحة الإعدادات، أو امسح يدويًا قاعدة IndexedDB باسم `AlGhazeezFarmDB` من أدوات المطوّر بالمتصفح.

## البنية المعمارية

### طبقة البيانات — كل شيء يمر من `js/db/db.js`
قاعدة IndexedDB واحدة (`AlGhazeezFarmDB`, `DB_VERSION = 8` حاليًا) تحوي 20 مخزن معرّفة في مصفوفة `OBJECT_STORES`: `Users, Animals, BirthRecords, DeathRecords, HealthRecords, Expenses, Revenues, Purchases, Employees, CustodyItems, Notifications, Categories, Attachments, BulkPurchases, BulkSales, BulkExpenses, BulkBatches, Parties, InventoryItems, InventoryMovements`. كل مخزن `keyPath: 'id'` مع `autoIncrement`.

⚠️ **`BulkPurchases`/`BulkSales`/`BulkExpenses` قديمة/غير مستخدمة لبيانات جديدة**: كانت هذه المخازن الثلاثة نموذج "الشراء والبيع الجماعي" الأصلي (سجل مستقل لكل معاملة)، واستُبدلت بمخزن `BulkBatches` (انظر جدول "نموذج البيانات" أدناه). بقيت الثلاثة في `OBJECT_STORES` فقط لأن `_migrateLegacyBulkDataIfNeeded()` في `seed.js` تقرأ منها مرة واحدة (عند أول تشغيل بعد هذا التحديث) لترحيل أي بيانات قديمة إلى دفعات `BulkBatches` جديدة، ولأن النسخ الاحتياطي في `settings-page.js` يكرر على `OBJECT_STORES` بالكامل — لا صفحة أو خدمة حالية تكتب فيها بيانات جديدة.

كل الوصول لقاعدة البيانات يمر عبر دوال `db.js` العامة:
- `dbAdd(storeName, record)` — يضيف `createdAt` تلقائيًا إن لم يكن موجودًا.
- `dbGet(storeName, id)` / `dbGetAll(storeName)` / `dbQuery(storeName, filterFn)`
- `dbUpdate(storeName, id, fields)` — يدمج الحقول ويضيف `updatedAt` تلقائيًا.
- `dbSoftDelete(storeName, id)` — يضبط `status: 'deleted'` و`isDeleted: true` معًا.
- `dbHardDelete(storeName, id)` — حذف فعلي. مستخدم حاليًا فقط في `deleteAttachment()` (`attachment-service.js`) لحذف مرفقات الفواتير نهائيًا؛ لا يُستخدم لأي مخزن آخر (البقية تعتمد الحذف الناعم `dbSoftDelete`).

**لإضافة مخزن جديد**: أضف اسمه إلى `OBJECT_STORES` وارفع `DB_VERSION` — حلقة `onupgradeneeded` تُنشئ أي مخزن جديد تلقائيًا، لا حاجة لأي تعديل آخر في `db.js`. ملاحظة: رفع `DB_VERSION` وحده لا يملأ المخزن الجديد بأي بيانات لقواعد المستخدمين الموجودة مسبقًا — إن احتجت تهيئة بيانات افتراضية للمخزن الجديد فأضف دالة تهيئة منفصلة في `seed.js` مفحوصة بمعزل عن شرط "هل Users فارغ؟" (مثال: `_seedDefaultCategoriesIfEmpty`)، وإلا ستبقى قواعد المستخدمين القدامى بمخزن فارغ.

⚠️ **تناقض قائم في الكود**: بعض الخدمات تفلتر السجلات المحذوفة بـ `status !== 'deleted'` (Animals, Expenses, Revenues, Employees, CustodyItems) وبعضها بـ `!isDeleted` (BirthRecords, Notifications). كلا الحقلين يُضبطان معًا داخل `dbSoftDelete` فلا يوجد خلل فعلي حاليًا، لكن أي كود جديد يفلتر سجلات يجب أن يتأكد أي الحقلين تستخدمه الخدمة المقابلة في `js/services/`.

⚠️ **لا يوجد encapsulation حقيقي**: بما أن الملفات مجرد سكريبتات عامة (globals)، حتى المتغير `_dbInstance` في `db.js` (المفترض أنه خاص بحكم الشرطة السفلية) يمكن لأي سكريبت آخر الوصول له وتعديله مباشرة. `settings-page.js` يفعل ذلك فعليًا عند "حذف البيانات وإعادة البداية" (`_dbInstance.close(); _dbInstance = null;`) وأيضًا يفتح transactions مباشرة (`_clearStore`) بدل استخدام دوال `db.js` العامة — وهو الاستثناء الوحيد لقاعدة "كل الوصول عبر db.js".

### تحميل الملفات: لا ES modules، الترتيب يهم
كل صفحة HTML تحمّل مجموعة من ملفات JS بترتيب شبه ثابت:
1. Chart.js من CDN — فقط في الصفحات التي بها رسوم بيانية (`dashboard.html` وأغلب صفحات `reports/*.html`).
2. `js/db/db.js`
3. `js/services/auth-service.js` (و`seed.js` أيضًا — فقط في `index.html`).
4. باقي ملفات `js/services/*` المطلوبة لتلك الصفحة.
5. `js/utils/validators.js` — فقط في الصفحات التي بها نماذج تحقق (بما فيها Modals مثل الموظفين/العهد) — و`js/utils/formatters.js`.
6. مكوّنات `js/components/*` المطلوبة (`sidebar.js`, `header.js`, `modal.js`, `toast.js`, وأحيانًا `data-table.js`/`chart-renderer.js`).
7. سكريبت الصفحة نفسها من `js/pages/*` — دائمًا آخر عنصر، لأنه يعتمد على كل ما سبق.

عند إضافة صفحة/خدمة جديدة، انسخ ترتيب صفحة مشابهة موجودة بالضبط.

### بنية الصفحات (Multi-Page App) والمسارات النسبية
- الجذر يحوي `index.html` (تسجيل الدخول) و`dashboard.html` فقط.
- كل وحدة وظيفية لها مجلدها الخاص لصفحات HTML: `herd/`, `expenses/`, `revenues/`, `purchases/`, `bulk/`, `categories/`, `parties/`, `inventory/`, `employees/`, `custody/`, `notifications/`, `reports/`, `settings/` — وكلها تشير لملفات `../css/` و`../js/` (مستوى واحد للأعلى).
- **نفس قائمة أسماء المجلدات الثلاثة عشر مكرّرة يدويًا في 3 أماكن منفصلة**: `sidebar.js` (`knownFolders` داخل `getRootPath`)، `auth-service.js` (ثابت واحد مشترك `KNOWN_MODULE_FOLDERS` يغذّي كلاً من `logout()` و`requireAuth()`)، و`header.js` (منطق رابط جرس الإشعارات). أي مجلد وحدة جديد **يجب** إضافته في الأماكن الثلاثة معًا وإلا ستنكسر الروابط النسبية بعد التنقل أو تسجيل الخروج.

### المصادقة (بسيطة، محلية فقط) وصلاحيات الوحدات (module-level)
- `js/services/auth-service.js`: هاش كلمة المرور بـ SHA-256 عبر `crypto.subtle` (بدون salt)، تخزين الجلسة في `localStorage['currentUser']` كـ JSON عادي بدون توكن/تشفير — كافٍ لتطبيق محلي أحادي المستخدم، غير آمن لأي سيناريو حقيقي متعدد المستخدمين.
- كل صفحة محمية تستدعي `requireAuth(moduleKey)` أول شيء داخل `DOMContentLoaded` — `moduleKey` اختياري (مثل `'herd'`, `'expenses'`, ...) يطابق مفاتيح `SIDEBAR_LINKS`؛ `dashboard-page.js` وحدها تستدعيها بلا وسيط فتبقى متاحة دومًا لأي مستخدم مسجّل دخوله.
- `hasModuleAccess(moduleKey)` (في `auth-service.js`) هي مصدر الحقيقة الوحيد للتحقق: `role === 'manager'` يمنح وصولاً كاملاً دومًا؛ غير ذلك (`role === 'owner'`) تُفحص `Users.permissions` (انظر جدول "نموذج البيانات" أعلاه). تُستخدم في مكانين: داخل `requireAuth()` لمنع الدخول المباشر عبر الرابط، وداخل `sidebar.js` (`renderSidebar`) لإخفاء روابط الوحدات غير المسموحة من القائمة الجانبية.
- حقل `role` (`manager`/`owner`) لم يعد تجميليًا بالكامل: لا يزال يُستخدم لعرض تسمية مختلفة في الهيدر (`manager` ⇐ "مدير المزرعة"، غير ذلك ⇐ "صاحب الحلال")، لكنه الآن أيضًا الأساس الذي تُبنى عليه صلاحيات الوحدات — **عكس التسمية المتوقعة**: `manager` (مدير المزرعة) = وصول كامل دومًا بصرف النظر عن `permissions`، بينما `owner` (صاحب الحلال) هو الدور المقيَّد الذي يتحكم فيه `permissions` (قرار عمل: مدير المزرعة هو الطرف التشغيلي الذي يحتاج وصولاً كاملاً، بينما صاحب الحلال قد يكون مالكًا غير تشغيلي بصلاحيات محدودة). إدارة المستخدمين وصلاحياتهم تتم حصرًا من قسم "إدارة المستخدمين والصلاحيات" في `settings/settings.html` (يظهر لـ `role === 'manager'` فقط).

### نمطان مختلفان للـ CRUD — اختر المناسب عند إضافة ميزة جديدة
1. **صفحة قائمة + صفحة نموذج منفصلة** (القطيع، المصروفات، الإيرادات): `xxx-list.html` يعرض `renderDataTable` مع `onRowClick` يوجّه إلى `xxx-form.html?id=<id>`؛ صفحة النموذج تفرّق بين إنشاء/تعديل حسب وجود `?id=` في `URLSearchParams`.
2. **صفحة قائمة + Modal للإنشاء/التعديل** (الموظفون، العهد): زر "+ تكويد..." و`onRowClick` كلاهما يفتحان نفس `openModal(...)`، بدون صفحة `form` منفصلة، وبدون service منفصل — تُستخدم دوال `db.js` مباشرة (`dbGetAll/dbAdd/dbUpdate`) من داخل سكريبت الصفحة.

### المكوّنات المشتركة (`js/components/`)
- `data-table.js` — `renderDataTable(containerId, columns, rows, options)`: بحث + فرز + ترقيم صفحات، تُستخدم في كل جدول تقريبًا. لتحديث الصفوف بعد تعديل بدون إعادة تعريف الأعمدة: `updateDataTableRows(containerId, newRows, newFooterRow?)`. `options.footerRow` (اختياري): كائن بنفس مفاتيح `columns` يُعرض كصف إجمالي ثابت في `<tfoot>` أسفل الجدول — لا يتأثر ببحث/فرز/ترقيم الجدول الداخلي لأنه يُحسب من الطرف المستدعي على كامل مجموعة الصفوف المفلترة (لا صفوف الصفحة الحالية فقط). مستخدم في جداول تقارير المصروفات/الإيرادات/المشتريات/القطيع لعرض صف "الإجمالي".
- `modal.js` — `openModal(html, options)` و`confirmDelete(message, onConfirmed)` (اختصار جاهز لتأكيد الحذف، مستخدم في كل صفحات الحذف).
- `toast.js` — `showToast(message, 'success'|'error'|'warning'|'info')`.
- `chart-renderer.js` — غلاف فوق Chart.js: `renderLineChart` / `renderBarChart` / `renderPieChart(canvasId, ...)`. يحتفظ بمرجع كل رسم في `_chartInstances[canvasId]` ويدمّره قبل أي إعادة رسم — أي canvas جديد يحتاج `id` فريد، ويتطلب تحميل Chart.js من CDN **قبل** هذا الملف في الصفحة. الخيارات تضبط `maintainAspectRatio: false` دائمًا، لذا **كل canvas يجب أن يُلفّ بـ `<div class="chart-box"><canvas id="..."></canvas></div>`** (تعريف `.chart-box` في `components.css`, ارتفاع ثابت 300px / 240px على الشاشات الصغيرة) — بدون هذا اللف يفقد الرسم بُعده وقد ينهار أو يتمدد بلا حدود.
- `sidebar.js` / `header.js` — `renderSidebar(activePageKey)` و`renderHeader(pageTitle)` تُستدعيان أول كل صفحة محمية. `renderHeader` تستدعي تلقائيًا `updateNotificationBadge()` — أي صفحة تعرض الهيدر (وتحمّل `notification-service.js`) تحصل على شارة الإشعارات مجانًا دون كود إضافي.
- `attachment-manager.js` — أرشفة مرفقات الفواتير (صور/PDF حتى 10 ميجابايت، مخزَّنة كـ Blob في مخزن `Attachments`). `openAttachmentsModal(recordType, recordId, recordLabel?)` يفتح نافذة `openModal(..., { hideFooter: true })` تعرض قائمة مرفقات السجل (عرض/تنزيل/حذف) وحقل رفع ملف جديد؛ يتطلب تحميل `modal.js`, `toast.js`, و`attachment-service.js` قبل هذا الملف. مستخدم من أزرار "📎 مرفقات" في أعمدة جداول `expense-list.html`, `revenue-list.html`, `purchase-list.html`, `bulk-batches.html` (`recordType: 'bulkBatch'`) (زر داخل الصف يستدعي `event.stopPropagation()` لتفادي تفعيل `onRowClick` الذي يفتح صفحة التعديل). **قيد معماري مهم**: النافذة المنبثقة في هذا التطبيق لا تدعم التكديس (`openModal` يستبدل `#modal-root` بالكامل في كل استدعاء) — لذلك حذف مرفق يستخدم `window.confirm()` الأصلي للمتصفح بدل `confirmDelete()` المعتاد، تفاديًا لفتح نافذة تأكيد متداخلة تُغلق نافذة المرفقات نفسها. لا رابط مباشر بين المرفقات وصفحات النماذج (`expense-form.html` وغيرها) — الرفع يتم فقط من صفحة القائمة بعد وجود `id` فعلي للسجل، لتفادي مشكلة إرفاق ملفات بسجل لم يُحفظ بعد.
- `export-utils.js` — `exportRowsToExcel(filename, columns, rows)` (يتطلب تحميل مكتبة `xlsx` من CDN **قبل** هذا الملف) و`exportRowsToPdf(title, columns, rows)` (يفتح نافذة جديدة بجدول HTML ويستدعي `window.print()` — يعتمد على حوار "حفظ كـ PDF" في المتصفح، وليس مكتبة PDF حقيقية، لتفادي مشاكل عرض الخط العربي في مكتبات مثل jsPDF). `columns` بنفس شكل أعمدة `renderDataTable` (`{key, label}`)، و`rows` يجب أن تكون نفس الصفوف المعروضة فعليًا في الجدول. كل صفحة تحتفظ بآخر صفوف/أعمدة معروضة في متغيّرات page-level (مثل `_herdExportRows`، أو `_currentExportColumns`/`_currentExportRows` في صفحات التقارير متعددة التبويبات/الأوضاع حيث تُحدَّث هذه المتغيرات عند كل تبديل تبويب — وتُضبط `null` في تبويبات بلا جدول مثل "إحصائيات"/"حسب النوع" في `herd-reports.html`، فيعرض زرّا التصدير تحذيرًا عبر `showToast` بدل التصدير). **`exportSectionsToExcel(filename, sections)`/`exportSectionsToPdf(title, sections)`** (كل `section: {title, columns, rows}`) نسخة متعددة الأقسام لتصدير أكثر من جدول في نفس الملف بضغطة واحدة — Excel: كل قسم ورقة (sheet) منفصلة باسمه؛ PDF: كل قسم بعنوان فرعي خاص به قبل جدوله ضمن نفس المستند المتسلسل. `exportRowsToExcel`/`exportRowsToPdf` أصبحتا الآن مجرد غلاف رقيق يستدعي النسخة متعددة الأقسام بقسم واحد (`title: null` لـ PDF يحافظ على نفس المخرجات الأصلية بالضبط، بلا عنوان فرعي) — **لا تغيير في سلوك أي صفحة موجودة** تستخدم النسختين المفردتين. مستخدم حاليًا في: `herd-list.html`, `births.html`, `deaths.html` (القطيع)، `expense-list.html`, `revenue-list.html` (المصروفات/الإيرادات)، `custody-list.html` (سجل العهد وكشف حساب العهد، تصدير منفصل لكل جدول)، `bulk-batches.html`، `inventory-items.html`, `inventory-movements.html` (المخزون)، و`herd-reports.html`, `revenue-reports.html`, `expense-reports.html`, `purchase-reports.html`, `party-reports.html`, `overview-report.html` (التقارير، بما فيها وضع المقارنة بين فترتين)، و`inventory-reports.html` **وحدها تستخدم `exportSectionsToExcel`/`exportSectionsToPdf`** (قسمان: "الرصيد لكل صنف" + "سجل الحركات" في ملف واحد).

## نموذج البيانات (شكل كل مخزن)
> IndexedDB بلا مخطط ثابت (schemaless) — هذه الحقول اتفاقية استخدام فعلي مُستنتجة من `seed.js`/الخدمات/صفحات النماذج، وليست قيدًا مفروضًا من الكود.

| المخزن | الحقول |
|---|---|
| `Users` | `username, passwordHash, role ('manager'\|'owner'), fullName, phone, permissions?: string[]` — قائمة مفاتيح الوحدات المسموحة لدور `'owner'` (تطابق مفاتيح `SIDEBAR_LINKS` في `sidebar.js` عدا `dashboard` المتاح دومًا)؛ **غياب الحقل = وصول كامل** (توافق خلفي لحسابات موجودة مسبقًا قبل هذه الميزة)؛ لدور `'manager'` الحقل غير مؤثر لأن `hasModuleAccess()` في `auth-service.js` يمنحه وصولاً كاملاً دومًا بصرف النظر عن قيمته. تُدار الإضافة/التعديل من قسم "إدارة المستخدمين والصلاحيات" في `settings/settings.html`، ولا يظهر إلا لمستخدم `role === 'manager'` |
| `Animals` | `code ('GZ-0001'…), type ('sheep'\|'goat'), breed, gender ('male'\|'female'), birthDate (تاريخ الميلاد أو الاقتناء — نفس الحقل للحالتين), color, weight, healthStatus ('healthy'\|'sick'\|'underTreatment'\|'quarantine'), source ('born'\|'purchased'), purchasePrice, status ('alive'\|'dead'\|'sold'), notes, motherId?` — لا يوجد حقل عمر مخزَّن؛ `herd-list-page.js` يحسبه ديناميكيًا (`calculateAgeLabel` في `formatters.js`) من `birthDate` حتى اليوم، أو حتى `deathDate`/تاريخ إيراد البيع المرتبط عند `status` = `'dead'`/`'sold'` |
| `BirthRecords` | `motherId, birthDate, offspringCount, offspringDetails: [{gender, weight, animalId?, animalCode?}], veterinarianNotes` — `animalId`/`animalCode` تُملأ فقط عند تفعيل خيار "تكويد كل مولود تلقائيًا" في `births.html`؛ تبقى `null` لأي مولود لم يُسجَّل كحيوان |
| `DeathRecords` | `animalId, deathDate, cause ('disease'\|'accident'\|'old_age'\|'unknown'), details` — تُكتب من `herd/deaths.html` (`deaths-page.js`)، وتُحدّث تلقائيًا `Animals.status` إلى `'dead'` عبر `createDeathRecord` |
| `HealthRecords` | `animalId, date, diagnosis, treatment, veterinarianName, cost, resultingHealthStatus?` — سجل تاريخي لزيارات/حالات صحية لكل حيوان، يُدار من `herd/health.html` عبر `health-service.js`. `resultingHealthStatus` (اختياري، من نفس قيم `Animals.healthStatus` بما فيها `'healthy'` لتوثيق الشفاء) إذا حُدِّد يُحدِّث `Animals.healthStatus` لذلك الحيوان تلقائيًا، وإذا كانت النتيجة `sick`/`underTreatment` يُنشئ أيضًا إشعارًا (`type: 'health'`) — انظر قسم "الإشعارات التلقائية" أدناه. لا علاقة مباشرة بين هذا المخزن وحقل `Animals.healthStatus` نفسه (لا مرجع/مفتاح أجنبي حقيقي)، لكن التحديث ليس مقصورًا على الإنشاء: `updateHealthRecord()` يعيد حساب `healthStatus` الحالي للحيوان أيضًا، لكن **فقط** عند تعديل `resultingHealthStatus` **لأحدث سجل صحي لهذا الحيوان تحديدًا** (بالمقارنة بالتاريخ `date`) ولحيوان ما زال `status === 'alive'` — تعديل سجل أقدم من الأحدث، أو تعديل سجل لحيوان نافق/مباع، لا يغيّر `Animals.healthStatus` بصمت. `herd/health.html` نفسه يمنع اختيار حيوان `status !== 'alive'` من قائمة "الحيوان" عند تسجيل زيارة **جديدة** (الحيوان يختفي من القائمة المنسدلة) — تعديل/حذف سجلات قديمة لحيوان نافق/مباع يبقى ممكنًا لأغراض الأرشفة، فقط بلا أثر رجعي على حالته الصحية |
| `Expenses` | `category (نص حر يُختار من بنود `Categories` النوع `'expense'`), amount, date, hasTaxInvoice (boolean), amountBeforeTax, taxAmount, paymentMethod ('cash'\|'transfer'\|'cheque'), vendor, status ('approved'\|'pending'), notes, linkedInventoryItemId?, linkedQuantity?` — `category` مجرد نص منسوخ وقت الحفظ، **ليس مرجعًا** لسجل `Categories`؛ حذف/تعديل بند لاحقًا لا يغيّر مصروفات سابقة. `linkedInventoryItemId?`/`linkedQuantity?` (اختياريان، من قسم "ربط بصنف مخزون" في `expense-form.html` — يُستخدم عمليًا لمصروفات مثل شراء الأعلاف/الأدوية) يُنشئان تلقائيًا حركة "وارد" مطابقة في `InventoryMovements` عند الحفظ (`_syncExpenseInventoryLink()` في `expense-form-page.js`، نفس مبدأ ربط `animalId` بالإيراد في `revenue-form-page.js`) — بذلك تكلفة الصنف تدخل في "صافي الربح" كمصروف واحد **و** تزيد رصيد المخزون ككمية، بلا ازدواج (تكلفة الوحدة `unitCost` في الحركة الناتجة إعلامية فقط، محسوبة من `amount / linkedQuantity`). تعديل/حذف المصروف يزامن (يحدّث/يحذف) الحركة المرتبطة تلقائيًا عبر `getInventoryMovementByExpenseId()` |
| `Revenues` | `category (نص حر يُختار من بنود `Categories` النوع `'revenue'`), amount, date, hasTaxInvoice (boolean), amountBeforeTax, taxAmount, receiveMethod ('cash'\|'transfer'), client, notes, animalId?` — عند `category === 'بيع حيوان'`، اختيار `animalId` في `revenue-form.html` يضبط تلقائيًا `Animals.status = 'sold'` لذلك الحيوان (ويُعاد إلى `'alive'` عند حذف الإيراد أو تغيير الحيوان المختار). ملاحظة: هذا الفحص يقارن نص الفئة حرفيًا بـ `'بيع حيوان'`، فتغيير اسم هذا البند تحديدًا من صفحة `categories/categories.html` سيكسر ربط البيع بالحيوان بصمت |
| `Purchases` | `category (نص حر يُختار من بنود `Categories` النوع `'purchase'`), amount, date, hasTaxInvoice (boolean), amountBeforeTax, taxAmount, paymentMethod ('cash'\|'transfer'\|'cheque'), vendor, status ('approved'\|'pending'), notes` — نفس شكل `Expenses` تمامًا (وحدة منفصلة مقصودة: مشتريات/تكاليف مقابل مصروفات تشغيلية عامة). تُحتسب ضمن "صافي الربح" في `dashboard-page.js` بنفس طريقة المصروفات — الصيغة الكاملة الآن (بعد إضافة وحدة الجملة أدناه): `صافي الربح = إيرادات + مبيعات جملة − مصروفات − مشتريات − مشتريات جملة − مصاريف جملة`. ⚠️ بعكس `Expenses`، **لا** ربط بالمخزون هنا (قرار عمل: ربط المخزون مقصور على `Expenses` لأن الأعلاف/الأدوية تُسجَّل هناك فعليًا) |

**فاتورة ضريبية (`hasTaxInvoice`/`amountBeforeTax`/`taxAmount`)**: مشتركة بين `Expenses`/`Revenues`/`Purchases`. الحقل `amount` المُدخل في النموذج هو **دائمًا المبلغ شامل الضريبة** (لم يتغيّر معناه ولا استخدامه في أي مكان آخر بالتطبيق — التقارير/لوحة التحكم/صافي الربح كلها تستمر بالجمع على `amount` كما كانت). عند اختيار "نعم" لفاتورة ضريبية، تُحسب `amountBeforeTax`/`taxAmount` تلقائيًا بمعدل ثابت 15% عبر `calculateTaxBreakdown()` في `formatters.js` وتُخزَّن وقت الحفظ؛ عند "لا" يبقى الحقلان `null`. سجلات موجودة قبل هذه الميزة ستُعامَل ضمنيًا كـ "لا" (`hasTaxInvoice` غير موجود = falsy) في كل الأماكن التي تعرض/تجمع هذا الحقل.
| `BulkBatches` | `code ('DF-0001'…، تلقائي عبر generateNextBulkBatchCode()), purchaseDate (تاريخ شراء الدفعة، يوم واحد للدفعة كلها), purchaseLines: [{type, gender, breed, count, unitPrice}], saleLines: [{date, type, gender, breed, count, unitPrice}], expenseLines: [{date, description, amount}], notes` — سجل "دفعة" واحد لشراء/بيع الحيوانات بالجملة **بدون** تسجيلها فرديًا في `Animals` (لا `code` حيوان/لا سجل قطيع دائم لكل رأس). دفعة واحدة قد تضم عدة تركيبات نوع/جنس/سلالة في الشراء دفعة واحدة، ثم تُعدَّل لاحقًا (نفس السجل) لإضافة بنود بيع/مصاريف تدريجيًا — لكل بند بيع/مصروف تاريخه الخاص لأن البيع قد يحدث على دفعات زمنية مختلفة، بعكس بنود الشراء التي تشترك في `purchaseDate` واحد. **البيع حرّ تمامًا**: بنود `saleLines` لا يلزم أن تطابق تركيبة/عدد بنود `purchaseLines` في نفس الدفعة — لا تحقق آلي مانع. كل المجاميع (تكلفة الشراء، إيراد البيع، المصاريف، صافي الإيراد، العدد المتبقي) **محسوبة حيًا وليست مخزَّنة**، عبر `computeBulkBatchTotals(batch)` في `bulk-batch-service.js` (يُعاد استخدامها في صفحة القائمة، الداشبورد، والتقرير الشامل). لا حقول فاتورة ضريبية (لم تُطلب لهذه الوحدة). صافي إيراد كل الدفعات (بيع − شراء − مصاريف) يُحتسب ضمن "صافي الربح" في `dashboard-page.js` **ويظهر أيضًا كبطاقة KPI منفصلة** باسم "صافي إيراد الشراء والبيع الجماعي" |
| `Employees` | `fullName, employeeCode, phone, jobTitle (من JOB_TITLES), hireDate, salary, status ('active'\|'leave'\|'terminated')` |
| `CustodyItems` | `employeeId, reference, amount, description, settledAmount, status ('open'\|'partially_settled'\|'settled')` |
| `Notifications` | `type ('health'\|'custody'\|'finance'\|'death'\|'summary'\|'inventory'), title, message, relatedEntityId, isRead` |
| `Categories` | `type ('expense'\|'revenue'\|'purchase'), name, status?` — "تكويدات" بنود المصروفات/الإيرادات/المشتريات، تُدار من `categories/categories.html`. تحل محل الثوابت الثابتة `EXPENSE_CATEGORIES`/`REVENUE_CATEGORIES` (أُزيلت من `expense-service.js`/`revenue-service.js`) — أي صفحة تحتاج القائمة تستدعي `getCategoryNames('expense'\|'revenue'\|'purchase')` من `category-service.js` بدلاً من ثابت عام |
| `Parties` | `type ('client'\|'supplier'), name, category?, phone?, address?, locationUrl?, status?` — "تكويدات" عملاء/موردين، تُدار من `parties/parties.html` (CRUD بـ Modal، نفس نمط `category-service.js`) عبر `party-service.js`. `Revenues`/`Purchases`/بنود `BulkBatches.saleLines`/`purchaseLines` تحمل `partyId?` اختياريًا يشير لسجل هنا؛ `party-reports-page.js` يبني من ذلك كشف حساب مفصّل لكل طرف (كل معاملاته + إجمالي) وترتيب "أعلى العملاء/الموردين" خلال فترة — هذه هي وحدة "المحاسبة المبسّطة" لكشوف حساب الأطراف في هذا التطبيق (بلا قيد مزدوج/دفتر أستاذ حقيقي) |
| `InventoryItems` | `name, category ('fodder'\|'medicine'\|'equipment'\|'other' — قائمة ثابتة في `inventory-service.js`، ليست مرتبطة بمخزن `Categories`), unit (نص حر مثل 'كجم'/'لتر'), reorderThreshold? (رقم، null = بلا تنبيه نقص), notes, status?` — كتالوج أصناف المخزون، يُدار من `inventory/inventory-items.html` (CRUD بـ Modal) عبر `inventory-service.js`. لا حقل سعر مخزَّن على الصنف نفسه — عمود "آخر سعر شراء" المعروض في `inventory-items.html`/`inventory-reports-page.js` محسوب حيًا عبر `getLastPurchasePrice(item, movements)` (أحدث حركة "وارد" لهذا الصنف تحمل `unitCost`، وليس بالضرورة أرخص/أغلى سعر) |
| `InventoryMovements` | `itemId, direction ('in'\|'out'), quantity, date, reasonType ('purchase'\|'consumption'\|'damage'\|'adjustment'\|'other'), unitCost? (اختياري، معروض في جداول `inventory-movements.html`/`inventory-reports-page.js` كـ"تكلفة الوحدة"، لكن **لا يدخل** في حساب "صافي الربح" تفاديًا لازدواج الاحتساب مع `Expenses`/`Purchases` — إعلامي فقط ماليًا رغم عرضه)، notes, status?, relatedExpenseId?` — حركات وارد/صادر، تُدار من `inventory/inventory-movements.html` عبر `inventory-service.js`. الرصيد الحالي لكل صنف **محسوب حيًا وليس مخزَّنًا** عبر `computeItemStockLevel(item, allMovements)` (وارد − صادر مقارنةً بـ `reorderThreshold`)، بنفس مبدأ `computeBulkBatchTotals` — يمكن أيضًا حساب الرصيد "كما في تاريخ سابق" بتمرير مجموعة حركات مفلترة بـ`date <= asOfDate` بدل كل الحركات (مستخدم في `inventory-reports-page.js`). `createInventoryMovement()` تُنشئ إشعارًا `type: 'inventory'` فقط عند **تحوّل** الصنف إلى حالة "منخفض" (وليس عند كل حركة)، ولا تُعاد إن وُجد إشعار غير مقروء بالفعل لنفس الصنف — تعديل/حذف حركة موجودة لا يعيد حساب/يلغي التنبيهات بأثر رجعي. `relatedExpenseId?` يُضبط فقط للحركات المُنشأة تلقائيًا من ربط مصروف (انظر `Expenses.linkedInventoryItemId` أعلاه) — يُستخدم عبر `getInventoryMovementByExpenseId()` لإيجاد الحركة وتحديثها/حذفها إن عُدِّل/حُذف المصروف الأصلي. ⚠️ تعديل/حذف هذه الحركة يدويًا من `inventory-movements.html` مباشرة (بدل تعديل المصروف نفسه) يُفقد التزامن مع `Expenses` بصمت — لا حماية آلية ضد ذلك حاليًا |
| `Attachments` | `recordType ('expense'\|'revenue'\|'purchase'), recordId, fileName, fileType (mime), fileSize (bytes), blob (Blob الملف الفعلي)` — أرشفة صور/PDF الفواتير عبر `attachment-service.js` (`getAttachmentsForRecord`, `addAttachment`, `deleteAttachment`) و`attachment-manager.js`. **الحقل `blob` هو الاستثناء الوحيد في كل المخازن الذي ليس نصًا/رقمًا/boolean** — عادي داخل IndexedDB (يدعم Structured Clone)، لكنه غير قابل لـ`JSON.stringify` مباشرة، لذا نسخة `settings-page.js` الاحتياطية (`_exportBackup`/`_replaceDatabaseWithBackup`) تحوّله Base64 ذهابًا وإيابًا خصيصًا لهذا المخزن فقط عبر `_blobToBase64`/`_base64ToBlob` — أي كود جديد يتعامل مع `Attachments` بمعزل عن هاتين الدالتين (مثل نسخ احتياطي مخصص أو تصدير آخر) سيفقد محتوى الملف بصمت إن مرّ عبر `JSON.stringify` مباشرة. لا حذف تلقائي للمرفقات عند حذف السجل الأصل (Expenses/Revenues/Purchases تستخدم حذفًا ناعمًا `dbSoftDelete` فتبقى السجلات والمرفقات معًا قابلة للاسترجاع) |

كل السجلات تحصل تلقائيًا على `id` (autoIncrement)، `createdAt` (عند الإضافة)، و`updatedAt` (عند أي تعديل) — لا داعي لضبطها يدويًا.

## خريطة الوحدات الوظيفية

| الوحدة | صفحات HTML | JS الصفحة | الخدمة (service) |
|---|---|---|---|
| الدخول | `index.html` | `login-page.js` | `auth-service.js` (+`seed.js` للبيانات التجريبية) |
| لوحة التحكم | `dashboard.html` | `dashboard-page.js` | تجمع بيانات من كل الخدمات |
| القطيع | `herd/herd-list.html`, `herd/herd-form.html`, `herd/births.html`, `herd/deaths.html`, `herd/health.html` (نمط "نموذج إدخال أعلى الصفحة + جدول سجلات سابقة" نفسه المستخدم في `births.html`/`deaths.html` — لا صفحة `form.html` منفصلة ولا Modal؛ تعديل/حذف سجل يتم من Modal يُفتح بالنقر على الصف. تدعم `?animalId=` لعرض/تسجيل سجلات حيوان واحد فقط، رابط "السجل الصحي" في `herd-form.html` يفتحها بهذا الوسيط) | `herd-list-page.js`, `herd-form-page.js`, `births-page.js`, `deaths-page.js`, `health-page.js` | `animal-service.js`, `birth-service.js`, `death-service.js`, `health-service.js` |
| المصروفات | `expenses/expense-list.html` (زر "📎 مرفقات" لكل صف يفتح أرشيف فواتير السجل، انظر `attachment-manager.js`), `expenses/expense-form.html` (قسم اختياري "ربط بصنف مخزون" ينشئ حركة "وارد" مطابقة تلقائيًا في `InventoryMovements`، انظر `Expenses.linkedInventoryItemId` في جدول نموذج البيانات) | `expense-list-page.js`, `expense-form-page.js` | `expense-service.js`. `expense-form-page.js` يستدعي أيضًا `inventory-service.js` لمزامنة الحركة المرتبطة |
| الإيرادات | `revenues/revenue-list.html` (زر مرفقات لكل صف كذلك)، `revenues/revenue-form.html` | `revenue-list-page.js`, `revenue-form-page.js` | `revenue-service.js` |
| المشتريات | `purchases/purchase-list.html` (زر مرفقات لكل صف كذلك)، `purchases/purchase-form.html` | `purchase-list-page.js`, `purchase-form-page.js` | `purchase-service.js` — نسخة طبق الأصل من `expense-service.js` على مخزن `Purchases`. لا ربط بالمخزون هنا (انظر صف "المصروفات" أعلاه) |
| الشراء والبيع الجماعي | `bulk/bulk-batches.html` (قائمة الدفعات، نمط 1 من نمطي الـCRUD — تصدير Excel/PDF + زر مرفقات لكل صف بـ`recordType` جديد `'bulkBatch'`)، `bulk/bulk-batch-form.html` (صفحة نموذج منفصلة تفرّق إنشاء/تعديل بـ`?id=`؛ أقسام بنود شراء/بيع/مصاريف قابلة للتكرار بنفس نمط `offspring-row` في `births.html` لكن بحقول أكثر، وملخّص حيّ لصافي الإيراد)، و`bulk/bulk-summary.html` (عرض فقط، بلا CRUD) — الثلاثة تتشارك شريط تبويبات علوي محلي (`.report-tab`: "الدفعات" / "ملخص الأرصدة"). شراء/بيع دفعات جملة من الحيوانات **بدون** تسجيل كل رأس فرديًا في `Animals`؛ سجل "دفعة" واحد يحوي شراء (متعدد التركيبات، يوم واحد) + بيع/مصاريف (يُضافان لاحقًا لنفس السجل، كل بند بتاريخه) | `bulk-batch-list-page.js`, `bulk-batch-form-page.js`, `bulk-summary-page.js` (يحسب `remainingCount` = فرق تراكمي بسيط `purchasedCount − soldCount` مجمّعًا حسب نوع/جنس/سلالة من بنود كل الدفعات، **قيمة محسوبة وليست مخزَّنة**، بلا ربط دفعة بيع بدفعة شراء معينة) | `bulk-batch-service.js` (CRUD + `generateNextBulkBatchCode()` + `computeBulkBatchTotals(batch)`) |
| تكويدات (بنود المصروفات/الإيرادات/المشتريات) | `categories/categories.html` (CRUD بـ Modal) | `categories-page.js` | `category-service.js` — تستهلكه أيضًا صفحات نماذج/قوائم/تقارير المصروفات والإيرادات والمشتريات (تسعة ملفات) بدلاً من ثوابت عامة |
| العملاء والموردون | `parties/parties.html` (CRUD بـ Modal لتكويد عميل/مورّد) | `parties-page.js` | `party-service.js`. تقاريرها منفصلة: `reports/party-reports.html` (`party-reports-page.js`) — كشف حساب تفصيلي لطرف واحد (فترة محددة) + ترتيب أعلى العملاء/الموردين، مجمّعة من `Revenues`/`Purchases` (حقل `partyId?`) وبنود `BulkBatches.saleLines`/`purchaseLines` معًا |
| المخزون والمستلزمات | `inventory/inventory-items.html` (كتالوج الأصناف: الاسم/التصنيف/الوحدة/الرصيد الحالي/**آخر سعر شراء**/حد الطلب/الحالة، CRUD بـ Modal + تصدير Excel/PDF)، `inventory/inventory-movements.html` (حركات وارد/صادر: التاريخ/الصنف/الاتجاه/الكمية/**تكلفة الوحدة**/السبب/ملاحظات، CRUD بـ Modal + تصدير Excel/PDF) — يتشاركان شريط تبويبات علوي محلي (`.report-tab`). تقاريرها منفصلة: `reports/inventory-reports.html` (`inventory-reports-page.js`) — فلتر فترة (من/إلى) لعدد حركات الوارد/الصادر + جدول لكل صنف (وارد/صادر خلال الفترة، الرصيد **كما في تاريخ "إلى"** أو اليوم، وآخر سعر شراء — يجيب على "كم الكمية المتوفرة وبكام في أي وقت سابق") + سجل الحركات خلال الفترة (بتكلفة الوحدة لكل حركة) + رسم توزيع حسب سبب الحركة. **زرّا التصدير هنا يصدّران الجدولين معًا في ملف واحد** عبر `exportSectionsToExcel`/`exportSectionsToPdf` (انظر `export-utils.js` أعلاه) — Excel: ورقتان "الرصيد لكل صنف" و"سجل الحركات"؛ PDF: قسمان متتاليان بعنوان فرعي لكل منهما | `inventory-item-list-page.js`, `inventory-movement-list-page.js`, `inventory-reports-page.js` | `inventory-service.js` (CRUD + `computeItemStockLevel(item, movements)` للرصيد الحي أو "كما في تاريخ" + `getLastPurchasePrice(item, movements)` + إشعار نقص مخزون تلقائي + `getInventoryMovementByExpenseId()`). مدمجة أيضًا في `dashboard-page.js` (بطاقة KPI + تنبيهات سريعة)، `overview-report.html` (جدول "مستوى المخزون الحالي"، رصيد تراكمي بمعزل عن فلتر الفترة)، و`expense-form-page.js` (ربط اختياري ينشئ حركة "وارد" تلقائيًا من أي مصروف — انظر صف "المصروفات" أعلاه) |
| الموظفون | `employees/employee-list.html` (CRUD بـ Modal) | `employee-list-page.js` | لا يوجد — يستخدم `dbGetAll/dbAdd/dbUpdate('Employees', …)` مباشرة |
| العهد | `custody/custody-list.html` (CRUD بـ Modal) | `custody-list-page.js` | لا يوجد — نفس النمط أعلاه على `CustodyItems`. زر "تسوية شاملة" في جدول "كشف حساب العهد" (يظهر فقط عند رصيد موظف > 0) يفتح Modal يسوّي دفعة واحدة أكثر من عهدة قائمة لنفس الموظف معًا (كل عهدة محدَّدة بشكل افتراضي بكامل متبقيها، قابل للتعديل/الإلغاء لكل صف) — مفيد لتصفية بواقي صغيرة متفرقة (5-10 ريال) بدل فتح كل عهدة على حدة من التسوية الفردية العادية |
| الإشعارات | `notifications/notifications.html` | `notifications-page.js` | `notification-service.js` |
| التقارير | `reports/herd-reports.html` (تبويبات: إحصائيات/مواليد/مواليد حسب الأم/نفوق/حسب النوع/حسب السلالة)، `reports/health-reports.html` (تبويبات: إحصائيات عامة/السجلات خلال فترة/حسب الحيوان/حسب الطبيب البيطري — الإحصائيات العامة تخلط بين عدد سجلات مفلتر بالفترة وأعداد حيوانات لحظية حالية، انظر ملاحظة `resultingHealthStatus` في جدول "نموذج البيانات" أعلاه)، `reports/revenue-reports.html`، `reports/expense-reports.html`، `reports/purchase-reports.html` — الثلاثة الأخيرة فيها وضعان: "تقرير فترة واحدة" (الافتراضي) و"مقارنة بين فترتين" (بند/نوع محدد أو الكل، مع فترتين تاريخ منفصلتين وفرق/نسبة تغيّر)، `reports/party-reports.html` (كشف حساب عميل/مورّد واحد + ترتيب أعلى العملاء/الموردين، انظر صف "العملاء والموردون" أعلاه)، `reports/inventory-reports.html` (فترة + رصيد "كما في تاريخ"، انظر صف "المخزون والمستلزمات" أعلاه)، و`reports/overview-report.html` (تقرير شامل بلا تبويبات فرعية: ملخّص لحظي للقطيع الحي + حركة مواليد/نفوق وماليات إيرادات/مصروفات/مشتريات **و الجملة** (بنود الشراء تُفلتر بـ`purchaseDate` الدفعة، وبنود البيع/المصاريف كل بند بتاريخه الخاص) مفلترة بفترة واحدة من/إلى، بالإضافة لجدولي "الرؤوس المتبقية من الشراء والبيع الجماعي" و"مستوى المخزون الحالي" (كل صنف مخزون ورصيده الحي عبر `computeItemStockLevel`) مفصّلين — كلا الجدولين **رصيد تراكمي دائمًا** (بلا فلترة فترة، بنفس مبدأ "القطيع الحالي" اللحظي) وليسا مقيّدين بـ`report-from`/`report-to` كبقية أقسام الصفحة، مع جدول ملخّص قابل للتصدير). الثمانية تتبادل الربط فيما بينها عبر شريط تبويبات علوي مشترك (كلاس `.report-tab`؛ في `herd-reports.html` و`health-reports.html` تحديدًا هذا الشريط منفصل عن شريط تبويباتها الداخلية الذي يستخدم بدل ذلك `.report-tab[data-tab]` — أي كود جديد يتلاعب بـ`.report-tab` في هاتين الصفحتين يجب أن يفرّق بين الاثنين وإلا سيُفعّل روابط التنقل العلوية بالخطأ كتبويب داخلي) | `herd-reports-page.js`, `health-reports-page.js`, `revenue-reports-page.js`, `expense-reports-page.js`, `purchase-reports-page.js`, `party-reports-page.js`, `inventory-reports-page.js`, `overview-report-page.js` | تُعيد استخدام خدمات القطيع/السجل الصحي/الإيرادات/المصروفات/المشتريات (`overview-report-page.js` وحدها تضيف أيضًا `bulk-batch-service.js` و`inventory-service.js` لقسمي الجملة والمخزون؛ `party-reports-page.js` تضيف `party-service.js`؛ `inventory-reports-page.js` تستخدم `inventory-service.js` فقط) |
| الإعدادات | `settings/settings.html` | `settings-page.js` | تصدير/استيراد نسخة احتياطية كاملة (JSON لكل المخازن) + تصفير `AlGhazeezFarmDB` بالكامل + إدارة المستخدمين والصلاحيات (manager فقط، عبر `dbAdd/dbUpdate('Users', …)` مباشرة، لا خدمة منفصلة) |

## الإشعارات التلقائية (أحداث حقيقية تستدعي `createNotification()`)
`createNotification()` (`notification-service.js`) لم تعد تُستخدم فقط في `seed.js` — الأحداث التالية تُنشئ إشعارًا فعليًا في مخزن `Notifications` (يظهر في مركز الإشعارات وشارة العداد بالهيدر):
- **صحة حيوان**: حفظ حيوان بحالة `sick`/`underTreatment` جديدة في `herd-form-page.js` (فقط عند **تحوّل** الحالة الصحية إليها، وليس عند كل حفظ لحيوان موجود مسبقًا بنفس الحالة — لتفادي التكرار)، وكذلك عند تسجيل سجل صحي جديد بنفس النتيجة عبر `createHealthRecord()` في `health-service.js`.
- **نفوق**: كل `createDeathRecord()` (`death-service.js`) ينشئ إشعار `type: 'death'`.
- **ماليات**: حفظ مصروف أو مشترى بحالة `pending` جديدة (`expense-form-page.js` / `purchase-form-page.js`) — أيضًا بمنطق "تحوّل الحالة" لتفادي التكرار عند تعديل مصروف معلّق موجود مسبقًا.
- **عهد**: `custody-list-page.js` يفحص عند كل تحميل للصفحة (`_checkStaleCustodyNotifications`) العهد المفتوحة/المسوّاة جزئيًا منذ أكثر من 30 يومًا، وينشئ إشعار `type: 'custody'` لكل عهدة لم يُنشأ لها إشعار مماثل خلال آخر 30 يومًا (لتفادي إعادة التنبيه بنفس العهدة في كل زيارة للصفحة).
- **مخزون**: `createInventoryMovement()` (`inventory-service.js`) ينشئ إشعار `type: 'inventory'` فقط عند **تحوّل** رصيد الصنف إلى حالة "منخفض" (أي عبور `reorderThreshold` نزولاً بسبب هذه الحركة تحديدًا)، ولا يُعاد التنبيه إن وُجد إشعار غير مقروء بالفعل لنفس الصنف.

⚠️ "التنبيهات السريعة" في لوحة التحكم (`dashboard-page.js` → `renderQuickAlerts`) تبقى آلية منفصلة تمامًا: محسوبة حيًا من البيانات مباشرة عند كل تحميل للوحة، لا تُقرأ من مخزن `Notifications` ولا تُنشئ إشعارات فيه — تعمل الآليتان بالتوازي بقصد (تنبيه لحظي في اللوحة + سجل تراكمي دائم في مركز الإشعارات)، وليس تكرارًا يجب توحيده.

## ملاحظات المستخدم
_(هذا القسم لصاحب المشروع فقط — أضف/عدّل هنا مباشرة في أي وقت. أي تحديث لاحق لهذا الملف من Claude يحافظ على هذا القسم كما هو دون حذف أو إعادة كتابة.)_

-
